// shader: 8B31, 421CC489DA0D894C

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn1();
bool Vfn3();
bool Vfn8();
bool Vfn10();
bool Vfn5();
bool Vfn6();
bool Vfn7();
bool Vfn12();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn14();
bool Vfn0();
bool Vfn2();
bool Vfn9();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn1() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn3() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn10() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn7() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn12() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn11() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn0() {
uint jmp_to = 159u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn1();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
Vfn3();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
case 159u:
Vfn1();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn2();
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn5();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn8();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn9();
}
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn9();
}
Vfn10();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn8();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn5();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn5();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn8();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn2() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn9() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, 7DCF9122F7A193A0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 3113F77E3F9B1FAD

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn1();
bool Vfn3();
bool Vfn8();
bool Vfn10();
bool Vfn5();
bool Vfn6();
bool Vfn7();
bool Vfn12();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn14();
bool Vfn0();
bool Vfn2();
bool Vfn9();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn1() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn3() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn10() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn7() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn12() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn11() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn0() {
uint jmp_to = 155u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn1();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
Vfn3();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
case 155u:
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn1();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn2();
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn5();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn8();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn9();
}
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn9();
}
Vfn10();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn8();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn5();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn5();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn8();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn5();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn2() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn9() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, E9CE7AFC482AF804
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 1A6183C6E984248F

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn13();
bool Vfn8();
bool Vfn11();
bool Vfn2();
bool Vfn7();
bool Vfn3();
bool Vfn4();
bool Vfn6();
bool Vfn12();
bool Vfn5();
bool Vfn9();
bool Vfn10();
bool Vfn14();
bool Vfn1();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn13() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn11() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn4() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn6() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn12() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn5() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn9() {
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn10() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn14() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 218u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn13();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn13();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn14();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn14();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn11();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
case 218u:
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn1();
}
Vfn2();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn1();
}
Vfn2();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg12 = vs_pica.f[85];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn11();
tmp_reg12 = vs_pica.f[14];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn11();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, DBE3C296384EA0AC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, AFA1F04CAC9C714F

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn1();
bool Vfn3();
bool Vfn9();
bool Vfn11();
bool Vfn6();
bool Vfn7();
bool Vfn8();
bool Vfn2();
bool Vfn15();
bool Vfn12();
bool Vfn13();
bool Vfn14();
bool Vfn0();
bool Vfn4();
bool Vfn10();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn1() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn3() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn9() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn11() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn8() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn12() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn0() {
uint jmp_to = 144u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
case 144u:
Vfn1();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn3();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn1();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn4();
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn9();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn10();
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn10();
}
Vfn11();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn3();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn9();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn6();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn6();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn9();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn4() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn10() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, 5AB7315286C48B40
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, B1F187F2D4B74547

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn1();
bool Vfn8();
bool Vfn2();
bool Vfn12();
bool Vfn7();
bool Vfn3();
bool Vfn4();
bool Vfn6();
bool Vfn15();
bool Vfn5();
bool Vfn13();
bool Vfn14();
bool Vfn9();
bool Vfn11();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn1() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn2() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn12() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn4() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn6() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn5() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn9() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn11() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 423u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn1();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn1();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn9();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn2();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn2();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn2();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn2();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
case 423u:
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn2();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn2();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 989317BD58EE15F3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((last_tev_out.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 40065C472AE4DC56
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
if (last_tev_out.a <= alphatest_ref) discard;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DBA7C834B3E957DA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, EA36FEB391FDE835

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn7();
bool Vfn14();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn3();
bool Vfn1();
bool Vfn9();
bool Vfn2();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn3() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn2() {
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 366u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
case 366u:
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 5ECFC586D92FE738
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 090F6261D7EE040A

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn1();
bool Vfn14();
bool Vfn7();
bool Vfn2();
bool Vfn3();
bool Vfn6();
bool Vfn4();
bool Vfn9();
bool Vfn5();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn1() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn3() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn6() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn4() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn5() {
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 315u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn1();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn1();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
case 315u:
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn1();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 548123F32C891796
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, DB65EA2FBAD032F1

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn6();
bool Vfn14();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn7();
bool Vfn9();
bool Vfn1();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn7() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn1() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 288u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn6();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn6();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
case 288u:
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 6696EBFC0AC0717D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D511B25408440125
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 61A82C16044CBE52

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn13();
bool Vfn8();
bool Vfn11();
bool Vfn7();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn12();
bool Vfn1();
bool Vfn9();
bool Vfn10();
bool Vfn14();
bool Vfn6();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn13() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn11() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn12() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn9() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn10() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn14() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn6() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 230u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn13();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn13();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn14();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn14();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn11();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn6();
}
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
case 230u:
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn6();
}
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg12 = vs_pica.f[85];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn11();
tmp_reg12 = vs_pica.f[14];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn11();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 2086CB6D0D2EC954
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 478298841EDB4A61
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 15B6C909B7264459
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 619E562CDB89F275

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn7();
bool Vfn6();
bool Vfn12();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn15();
bool Vfn1();
bool Vfn13();
bool Vfn14();
bool Vfn9();
bool Vfn11();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn12() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn9() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn11() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 454u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn7();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn9();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn6();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn6();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
case 454u:
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 771030A3EE84D48A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CD0EB4DEA4AE12FC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 83C36A4A2949D5E1

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn7();
bool Vfn1();
bool Vfn12();
bool Vfn6();
bool Vfn2();
bool Vfn3();
bool Vfn5();
bool Vfn15();
bool Vfn4();
bool Vfn13();
bool Vfn14();
bool Vfn9();
bool Vfn11();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn1() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn12() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn3() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn5() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn4() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn9() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn11() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 427u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn9();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn1();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn1();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn6();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn6();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
case 427u:
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn1();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B31, A2B1C50622C4E336

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn4();
bool Vfn8();
bool Vfn1();
bool Vfn12();
bool Vfn7();
bool Vfn2();
bool Vfn3();
bool Vfn6();
bool Vfn15();
bool Vfn5();
bool Vfn13();
bool Vfn14();
bool Vfn9();
bool Vfn11();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn4() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn1() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn12() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn3() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn6() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn5() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn9() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn11() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 418u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn4();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn4();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn9();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn1();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn1();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
case 418u:
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn1();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 13D5052BA6BA4F03
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 90082AE6B5CE434D

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn7();
bool Vfn6();
bool Vfn12();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn15();
bool Vfn1();
bool Vfn13();
bool Vfn14();
bool Vfn9();
bool Vfn11();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn12() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn9() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn11() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 452u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn7();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn9();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn6();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn6();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
case 452u:
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, B4170363C2A69641
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (const_color[0].rgb);
float alpha_output_0 = (const_color[0].a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, D47C887971654D1D

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
vec4 vs_out_reg3;
vec4 vs_out_reg4;
vec4 vs_out_reg5;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn3();
bool Vfn1();
bool Vfn0();
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
} else {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn0() {
uint jmp_to = 476u;
while (true) {
switch (jmp_to) {
case 476u:
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
vs_out_reg2 = vs_pica.f[93].xxxx;
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 2F81AEF57032A452
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6A1B72A2FCCBE987
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 05E1A4F0384EBF7F

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn0();
bool Vfn1();
bool Vfn2();
bool Vfn9();
bool Vfn11();
bool Vfn8();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn15();
bool Vfn12();
bool Vfn13();
bool Vfn14();
bool Vfn6();
bool Vfn10();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn0() {
uint jmp_to = 0u;
while (true) {
switch (jmp_to) {
case 0u:
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn1();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn2();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn1();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn6();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn9();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn10();
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn10();
}
Vfn11();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn8();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn9();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn8();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn8();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn9();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn8();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn1() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn2() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn9() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn11() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn8() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn4() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn5() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn12() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn6() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn10() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, F35472FEFCCBE987
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 2E393891BC8F259B

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn7();
bool Vfn1();
bool Vfn12();
bool Vfn6();
bool Vfn2();
bool Vfn3();
bool Vfn5();
bool Vfn15();
bool Vfn4();
bool Vfn13();
bool Vfn14();
bool Vfn9();
bool Vfn11();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn1() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn12() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn3() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn5() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn4() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn9() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn11() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 470u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn5();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn9();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn1();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn11();
}
Vfn12();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn1();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn6();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn6();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn1();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
case 470u:
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B31, 82498C00693767D6

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn15();
bool Vfn11();
bool Vfn6();
bool Vfn8();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn10();
bool Vfn14();
bool Vfn9();
bool Vfn12();
bool Vfn13();
bool Vfn1();
bool Vfn0();
bool Vfn7();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn15() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn11() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn8() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn10() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn14() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn10();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn12() {
Vfn10();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn13() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn1() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn0() {
uint jmp_to = 198u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn11();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn10();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn15();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn10();
Vfn11();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn15();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn1();
Vfn11();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
case 198u:
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn6();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn7();
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn7();
}
Vfn8();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn10();
Vfn11();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
tmp_reg12 = vs_pica.f[85];
Vfn13();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn6();
tmp_reg12 = vs_pica.f[14];
Vfn13();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn7() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, 93A93BC09551D122
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F390C0835F45F9F9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4F16C56C361A572F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
if (last_tev_out.a <= alphatest_ref) discard;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, F0F03E5C52F45639

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn7();
bool Vfn14();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn3();
bool Vfn1();
bool Vfn9();
bool Vfn2();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn3() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn2() {
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 359u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
case 359u:
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B31, DEF016CB05491BB4

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn10();
bool Vfn2();
bool Vfn5();
bool Vfn15();
bool Vfn8();
bool Vfn6();
bool Vfn7();
bool Vfn1();
bool Vfn9();
bool Vfn11();
bool Vfn3();
bool Vfn4();
bool Vfn12();
bool Vfn14();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn10() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn2() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn5() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn15() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn8() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn7() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn1() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn9() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn11() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn3() {
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn4() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn12() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn14() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 265u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn6();
} else {
Vfn7();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn10();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn2();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn10();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn12();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn5();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn8();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
case 265u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
tmp_reg12 = vs_pica.f[85];
Vfn4();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn5();
tmp_reg12 = vs_pica.f[14];
Vfn4();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn8();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn5();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn9();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn9();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn9();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn9();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn9();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn9();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn9();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn9();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn8();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn5();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn5();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn8();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn5();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
Vfn8();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B31, 17FE472057E5AA57

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn13();
bool Vfn8();
bool Vfn11();
bool Vfn7();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn12();
bool Vfn1();
bool Vfn9();
bool Vfn10();
bool Vfn14();
bool Vfn6();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn13() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn11() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn12() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn9() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn10() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn14() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn6() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 228u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn13();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn13();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn14();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn14();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn11();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn6();
}
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
case 228u:
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn6();
}
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg12 = vs_pica.f[85];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn11();
tmp_reg12 = vs_pica.f[14];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn11();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B31, F015ECC7723E6782

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn6();
bool Vfn14();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn7();
bool Vfn9();
bool Vfn1();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn7() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn1() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 286u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn6();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn6();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
case 286u:
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn7();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 0682315691DF22F8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A8CF8742D3FFE418
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1F31D122238544D7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 89F4A144483FDCBF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4A53C178DA180688
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B213DBEA703F4DA9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E6DEB3A176267503
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1B1F65E69DCC2CF2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C6C266BB71A4780A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D7ABDD423A9FE0A5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C14C2A01FF8980CD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 084C260CDA2D8394
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7178057DA30C6B80
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((vec3(1) - texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, E7691288707F7809

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn15();
bool Vfn2();
bool Vfn7();
bool Vfn9();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn11();
bool Vfn14();
bool Vfn10();
bool Vfn12();
bool Vfn13();
bool Vfn1();
bool Vfn0();
bool Vfn8();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn15() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn2() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn9() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn11() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn14() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn10() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn12() {
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn13() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn1() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn0() {
uint jmp_to = 195u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn15();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn11();
Vfn2();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn15();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
case 195u:
Vfn1();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn8();
}
Vfn9();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn8();
}
Vfn9();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn11();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
tmp_reg12 = vs_pica.f[85];
Vfn13();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn13();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn14();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn12();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn8() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, 1F8B56B6A69D76F6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CCDD8A714D8FD59E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 20F89F0FD505854F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7741ED8274BA6CC9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7BA8F0614E2EFF77
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = (last_tev_out.rgb);
float alpha_output_1 = ByteRound(clamp((last_tev_out.a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D4762C3B0658B868
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BEF3C067C9E0A693
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = (last_tev_out.rgb);
float alpha_output_1 = ByteRound(clamp((last_tev_out.a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F25393052E5C67E0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A34EF2EBF6540E32
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 918C77499A4D914E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B1AE1D2ED3065873
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 093F1E3AA6E724B3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C852EFA5F1B65DC1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C8B980B12AB3A0E1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2BC9383774C63DD7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FFEE8AA3792C903B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 230A49A38D8D21DC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8081C8610DB056FA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CAB25DB4B91FEC6E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 856E75259E753956
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5C9841FF918E1C70
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 62DE8C3DC104C851
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 304A5CE25080BC6E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C1FE7459E199B532
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 358240CD08F369E0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7664CF1BF35D0097
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 528A088158F377D7

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn13();
bool Vfn8();
bool Vfn11();
bool Vfn2();
bool Vfn7();
bool Vfn3();
bool Vfn4();
bool Vfn6();
bool Vfn12();
bool Vfn5();
bool Vfn9();
bool Vfn10();
bool Vfn14();
bool Vfn1();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn13() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn8() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn11() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn4() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn6() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn12() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn5() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn9() {
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn10() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn14() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 237u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn3();
} else {
Vfn4();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn13();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn13();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn14();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn14();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn11();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn1();
}
Vfn2();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
case 237u:
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn1();
}
Vfn2();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg12 = vs_pica.f[85];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn11();
tmp_reg12 = vs_pica.f[14];
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn12();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn12();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn9();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn11();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn11();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn5();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 6B63BE42E2F6C431
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[3].position);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((texcolor2.rgb), (const_color[2].rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F935CDD1A6992EF1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (const_color[0].rgb);
float alpha_output_0 = (const_color[0].a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FC945F1AF9B5312E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 363FE38B1D0D9C46
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[3].position);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CB4482862F23E816
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[3].position);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((texcolor2.rgb), (const_color[2].rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EC99DA070AAE5A79
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[3].position);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3CCB3012880A4C66
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CDB452FE3708A1B9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4EFADB1ACF3A60B6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E3F3A886765A887F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9D61FEF0BD63709C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 60030B56D96F796A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AE43ACCC97EB4103
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 996EFADAB2994BFA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C729D1E6EFA44000
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 17604D84BCC15904
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 20922404C46D28CF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2E2BDC37BDE53717
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5B26FA61DC10A8A8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BBDB9E15B4150F1D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((last_tev_out.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BAF1C5B228F95206
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 03B41249843C92F4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (const_color[0].rgb);
float alpha_output_0 = (const_color[0].a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EB6DE1D05D2F8ED0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 47D87D9FDC580E42
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8230B968DACC8A3C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 188544D4C4C2AC0C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ACCD4E2B4D747FB2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0BC6E5CF774C3EF6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B40B1A0497443CCF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2C73831C512A6BC9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 91C71CBBFBBA9014
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FB992A7C3CB63639
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1DD3AAD26EC56F4C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1EDC5A830F863610
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4FED7CE8B7F510D1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4E2CA3FE5E1866A5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5B8E8CB2390BB6C9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AD9D32803DD4CB16
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 45C78DF33D884A46
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 733A91686C7182B4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AECADE9018F11BA1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 196F65C6FDAD61F4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8AF11215F3CACD04
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5AB587C98BDFBBDF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EC97F6751B686B22
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6BA6BFE2A4D83669
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ACFB812410DF3A82
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 638CB04DEF54CD26
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 58D30BCD74FF4DA6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DD976AC845F86ECA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BD2701B976A9B3CA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 26DE667CD9D94DFF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EEAEEF847BDA184B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6C16192D37DDE8F9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F7F3D1391CCA85F2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 07BC3C3DE1BCF81A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F8EBBF49424F6950
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 85FAF61880F90611
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FC72D604255CCBB3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5AE266D590887634
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7AF9009B5B7CCCDE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 4.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8404D6B6BD509D70
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5E323051F0B32632
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7DF3D08A53F23329
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 49DAF06BBC0888D0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0 * 2.0, alpha_output_0 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3B5A3BE54903618C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D523B1595F7A43C2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8CF1B785A1FB1E23
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 206CABF84BF3F7D6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor0.rgb) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor0.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((last_tev_out.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, E4B1717CEF51482E

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn7();
bool Vfn9();
bool Vfn6();
bool Vfn13();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn15();
bool Vfn8();
bool Vfn1();
bool Vfn14();
bool Vfn10();
bool Vfn12();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn7() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn9() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn13() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn8() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn1() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn10() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn12() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 408u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn9();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn7();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn9();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn7();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn10();
Vfn9();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn6();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn12();
}
Vfn13();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn8();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn12();
}
Vfn13();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn8();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn9();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn6();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
case 408u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn8();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn8();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, B1B791610D217D05
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2DDBCD4EC3302F18
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 81B4B56064F0494E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DFE481A2A5D547B7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 05872491DEABFB07
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AA86134C12919643
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6BCCD30BE25893A2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5B83F6948C4F1252
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B564B0B80C768F77
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 46A3E37791B2B5FF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 82CD91E5FEB0B622
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DA752D74AA8E4C2A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EEBD0A76CDF74133
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 864492417C9B95F1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CBB80EF95790E2FF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 48B65508C7BB4D2E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8EB655DE9270E92E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 380CB22EF5DCB418
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0EFDA5332B364838
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8083F4F55E4E1C7A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0E380EC384E3316F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E51156508E8F2BCB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 58F083A1507E0D30
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A1D47C6D50CD01C9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ADCE63B3904CE05A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 75B2F1F3650A49D9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 339CE30E3C6F156E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 94B3232E648BCFAB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FADF5F1EDC9A44CC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C2D140C3B6AD13C7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1C52DFF3C802031C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 25D440C063D25193
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7966BA6B82AA7A6E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 23FC06BB5547BA21
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E272017B162D53A5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BC8AF9CE25960F6F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0A07275C2603B901
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E7F57906CBA5BA60
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4D3CBC260B9A902D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FAD70185C76226C4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6E355ACEE1E8FEBD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4ECD907B877A2816
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2B740797D5E0C78A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EFCD6855F34ACA80
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1002D0E3753BB8D0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DBF8847431D3E735
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F38F6A8A17534F29
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 176F8B2D82CA2078
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0707194330CAF8B5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 149637F32845305A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3ABC9AEFC8622443
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D09B6CA36D624C77
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7BCC7E384316547B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1CF273A0E866E793
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B8D501FAAE5A1BDB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5CA5404BAA25FCBC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 032D8144B499502B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0DC0397A86741CA2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6A605BBC42AC52A3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C90A885CAC7E5F80
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 26B8C4D421ADF2B8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4E9853FC1D8EDF7E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EF6A803A96113B0F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 14D0DE2A69F00C69
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8C752F9065F81B43
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AA9FB349FB761294
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A4FAF336B79A04EF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 33514A55213666D1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 43D24D6DE00195F0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E28B81BC5C49BC05
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 20CABE90F4AF60F0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 71441D4873C08D21
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8A3255CECD6B7FEB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4E2EAEA44E10AE51
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7F8CBA1EE9526ABA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F6E4EA0AEA744A5F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FAB93561073001C2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 308CFEFF99927ACF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 76BA3EE1066A3754
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E895C073D6A1F89D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 71C23213692D1F87
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6CE8444273D08212
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DA7DD7FAD6457136
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9C336FFF08C5017C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F949643F7204FBE4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CE9777947027A713
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7B8566986E5321C8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F176590126400FF2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 08883FAC689573B4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E5A53BDCF8EACAB9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AFCC42A686FBAE64
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5BF83C9D8ECC5920
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 77F1F437895CF4C6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AF957CD4F6E644F8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1568B4770B642950
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5C3C63262995706B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E768A5DED2E2443C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 54AB2A40CB60072C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3E87BC4040FF630E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2F24B48DCFF04E80
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 32A32206BFD1741F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 049CB0B95DDB8C65
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D9E496FDCE397EB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 158D923174A94888
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 266031E67BF550E5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 709424DFEBB8AF06
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BDB8643E4BEDD21B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CA9B07F340DE7E4F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A2CB6DCAE82CC430
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 524B951A4A96EE6F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4BD990AFB58CD178
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 82A8704BC2BEB2A9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 545FABB0DEC58449
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0989F9700FB5FB22
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2FC9831FA22543CF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E88E34DB4A65BCEC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1305C0E7AD44070C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4AD3295792D27394
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A73569471F83475D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B979639B4E5CBE41
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 78B1FA7A1A353237
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 15C6F29BDF0BC5F2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 906446495FDDEBAA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A99534D533F2A0F3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3659F95569E669EC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C5205C16D07FE0CA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0AF8150D724C7A87
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 202074F342820CC4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 85CC0D0479A1DB91
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 55B840B50C94CAE4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D32BD220944870E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CE7D591AA3355056
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B02A2802634D81D5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E7FAD86521F6EEA5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 19E034050E6A2BA9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 662A5EAC86BB9B96
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AFC0604FCAC6F1FE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9637019C01AD2A67
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 96AFD1444CBCF159
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D5F2CDC066856118
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 549A8D71DAA01533
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E5D706BE44AA6B66
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FD81175823C4FC2F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E2C07F52C06C1C37
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A95AB0EC0B59B794
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C56F74D0D26793FB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 95A02E3F02A42602
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FEB095FE63F3D6A0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F0919164CCB23D4F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 647E168B12CD5722
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 36889CA514D1D0F5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 21835577E328B088
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4267FB1817ED056F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C2F79FE44B64DA0C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CF0668AAFF71AA90
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 951988FE690A0F2D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6EE7B3E41D236060
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0454952150222268
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DA2B88E6B82CE183
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F05D0C0F08BC6441
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CA0429874869A9FB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D9CCD7B3833D6DA2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8C883D39E271E590
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1E0F2C800609E257
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 65E9AC7439D183C0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BDD817E634126F83
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CEA70C7FFBAC405E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 35477ACB351E5232
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 17CF9326F21B6D80
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 537E2B69841FCB2F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3392AC068D8B2E0B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 15DD6DCDDD8CE00E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5878B1464FDD3833
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A4C9D64D0A2BB3FE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BBD2EECD76DCB652
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6C0A89FA92CE988B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7243AA812A3709DC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2F93B1A5914B0F85
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 13D87A405F0032B3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 74BE71040E787122
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6886AE9E0F63B38F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C7E759041668FE49
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 66451D2BCEAC217D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 463157331DA8A875
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A8467B5A02608776
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CCF8ED61EEFC5893
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 12317D031D7CB743
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 81D8F50D640C88BA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9AF622B3D42C08AB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 142777278370379C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6CD7DC86DA19A840
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5AC6A2C976FC455F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 96CB44EF746D174B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 408DAD05A6AE634E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 94B43838F0562E29
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 756BED717C247FA1

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn7();
bool Vfn14();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn3();
bool Vfn1();
bool Vfn9();
bool Vfn2();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn3() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn2() {
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 327u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
case 327u:
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B31, 31884F9ACC9BB5B8

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn7();
bool Vfn14();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn3();
bool Vfn1();
bool Vfn9();
bool Vfn2();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn3() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn2() {
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 344u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
case 344u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 335E815344D524F2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp(mix((vec3(1) - const_color[0].aaa), (texcolor0.rgb), (const_color[0].aaa)), vec3(0), vec3(1)));
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (rounded_primary_color.aaa), (rounded_primary_color.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp(mix((vec3(1) - const_color[3].aaa), (texcolor0.rgb), (const_color[3].aaa)), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(mix((last_tev_out.rgb), (combiner_buffer.rgb), (vec3(1) - last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C940ADE43C90D307
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3E5DF7E9441FD60A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 297520D746B4A868
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9593C6CC3E5DA92B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 87BA565CF2549F21
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D681FB06D083631F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4541E50EEF681897
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4F1EB6D2BF48848D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 20FBCF3960F5BFFB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D3A75FBD4FA22584
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp(mix((vec3(1) - const_color[0].aaa), (texcolor0.rgb), (const_color[0].aaa)), vec3(0), vec3(1)));
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (rounded_primary_color.aaa), (rounded_primary_color.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp(mix((vec3(1) - const_color[3].aaa), (texcolor0.rgb), (const_color[3].aaa)), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(mix((last_tev_out.rgb), (combiner_buffer.rgb), (vec3(1) - last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6FD3A46AE9973F33
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B178A293AC5C0318
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7B3D23038FC55AD4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7DD56F6A7907270D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B3D219A4148246E8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, E80FD09F14049207

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn3();
bool Vfn2();
bool Vfn9();
bool Vfn11();
bool Vfn6();
bool Vfn7();
bool Vfn8();
bool Vfn1();
bool Vfn15();
bool Vfn12();
bool Vfn13();
bool Vfn14();
bool Vfn0();
bool Vfn4();
bool Vfn10();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn3() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn2() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn9() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn11() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn6() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn8() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn1() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn15() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn12() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn13() {
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn14() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn0() {
uint jmp_to = 147u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn7();
} else {
Vfn8();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn1();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn3();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
case 147u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn2();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn3();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn4();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn4();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn9();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn10();
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn10();
}
Vfn11();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn2();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg12 = vs_pica.f[85];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn9();
tmp_reg12 = vs_pica.f[14];
Vfn14();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn6();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn15();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn13();
Vfn6();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn9();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
Vfn6();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
bool Vfn4() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn10() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
// shader: 8B30, BE947A91108276C3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 02AFFD614C4DF8E5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C37CFFD1CA301C7D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5D5C587782042F20
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CBD05DCDB517AE1B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6D1921E9DFC3BB48
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D31F462801822E2B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, AE17434EB03F29DD

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn11();
bool Vfn6();
bool Vfn9();
bool Vfn15();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn10();
bool Vfn1();
bool Vfn7();
bool Vfn8();
bool Vfn12();
bool Vfn14();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn11() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn9() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn15() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn10() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn7() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn8() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn12() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn14() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 258u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn6();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn11();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn6();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn11();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn12();
Vfn6();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn9();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
case 258u:
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn6();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn7();
tmp_reg12 = vs_pica.f[85];
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn9();
tmp_reg12 = vs_pica.f[14];
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn7();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn7();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn9();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, C05114BB49500BC1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1CB643C565E59B0D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2F2E6AB88BB97F23
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C65518668DF870D3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9590F2693805314C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 01C7C9F05ACC52F2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B742B3A7FC4B467A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8B88117287F768A7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5E40BE76AE0A316D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4E360EC9DA99F4D9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 637CC226B31D0F82
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C41D0F1C4B01C5F5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AB42AAF3228AA53E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F34E10C3284B598C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5823B2EB89C78DDC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CFB891FCA13B1FFD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5E2DD176DEB910ED
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 963D21C841ADF433
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BDAAF968EF1C596D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B39989DA976A047B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 23979FDE0F062640
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F8A6A9F0205E1CDB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5DC7CE8A5C135EF3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 60EA8DD3B6BE1CAB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4EE3168B12D38B36
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DE4294CDD4BBF522
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 59C6A31D83F60C84
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D5A7E4BC0EFB30EB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EF2B5A5F794C295D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 413FBD0E0AC02C6D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7CC3A7921991AF48
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A381EAFFDDDEA8A1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 29535BDB1427D02E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F1DFE006DD258F65
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 98B99EDA3AC776B1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9B31DF4E7C757612
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5F833221AEE441E6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9DF1844F38382C3E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C51586D9519191BA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FD5277BE4F0218B1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6336FCCFBCA46F36
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, A850295C2F806452

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn11();
bool Vfn6();
bool Vfn9();
bool Vfn15();
bool Vfn3();
bool Vfn4();
bool Vfn5();
bool Vfn2();
bool Vfn10();
bool Vfn1();
bool Vfn7();
bool Vfn8();
bool Vfn12();
bool Vfn14();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn11() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn6() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn9() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn15() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn3() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn5() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn10() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn1() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn7() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn8() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn12() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn14() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 256u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn6();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn4();
} else {
Vfn5();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn11();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn6();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn11();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn12();
Vfn6();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn9();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
case 256u:
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn6();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn7();
tmp_reg12 = vs_pica.f[85];
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn9();
tmp_reg12 = vs_pica.f[14];
Vfn8();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn7();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn3();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn10();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn10();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn7();
Vfn3();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn11();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn9();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn9();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn1();
Vfn3();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 68379D7C8FB23E36
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DD00AE9C683C8CF4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F814E23FA052DC08
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EF8D9B9E29D5DF68
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ACD5947A1CB25EC5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B4AEE3D1952324F0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 750C09A44B34AB21
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D8E9E79C78EFB0BA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A4B61A91080C2C1E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AA09CD166B10E237
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D61291CC9F431770
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A4F2A6A29454F3C2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 28EEF914E13040DF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C9AF16FEFC3AD4CE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D57B54F81A0AFFD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 428E0A43CB4FFD75
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1C7C13022829074A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 154DF525B4BBBAD5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 29EEAD1A0DF0AC9F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BC6B3727E713A158
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1BD4C152E73EBD38
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A59DF2A44E9856BB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 20C2766149E86370
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FBD507B70AC4D902
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FE8D017C697AE2E6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 468B0250A1E3CCEC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F1DF1404B39DB8F0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 809FD9DC1DC4E80E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D8E9FA7860070685
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C3BE0E8211B57787
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F1316143BEE6133E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B742B64B8C53AEB0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C3C007EE6FD8652F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D79649B76453A743
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 06616C9A824872BA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F948BB19702BB629
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7DA4BCB1A77FB6DC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F0279C0DF80A4653
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A3F529DB52C59A0E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D6BBCC3A8262DBC8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AFC86AA02A918360
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1A5525F30AD021DD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C4DAB03A05DD8D09
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0520A7DC982FAF89
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EA7B1232A923D447
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 94E2DFF8299B31E1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FCD8885AA985C9BF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DFA17D482E0EE59D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3A434AA5FE25A8E8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 79B777ACC885CB3E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1495438F6FF2E6D9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 96FCC944DA7E7893
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 04418BF3256A57E0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A59040C48B828489
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9FAD25E4FED9BF8F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 28F58812BB553227
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CA2CCE4B20904568
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 21448F7CBF0DF42C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0EC61424FADEBEFA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3D4478AC2C6636D3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 57EE5E6FFCBD85AD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 931285EA604BD7D2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CFC38AF4F54CEC7F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8E92B73E90BC849C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 332875D8C7390273
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 38C01D22E4BDAB00
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C179CB94438DB62D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 943CD26FF795D9AE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7907E4A3178DE13B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 08DC1E20BC79355C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9C7B88003532B9B6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 83A50C25C992E795
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C52FC3BB23F6A8FD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8CB4F3D4B925F978
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B2AD4961E977EAF1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DA48A50CF1E97BE6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E45CA51B5904E8BE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4B77215561510279
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 29D346F38E01A349
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AEED1D8629374311
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D56071BEC34529F6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9951E8E82C8CAC7B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D7302F7E37411493
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 11DB823D01D566BA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 024AEAB5C6855642
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9430D917EC40878C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2CB04DE5D9783F12
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1DFEA5C823778DB7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9B547CA640AF8B12
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BA551FE21EBA7A41
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4C9E726EF236C3B8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C5F3811EEAF6890B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 80725873B5F358E1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 456E36DAC51B5749
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D1DF920DA88CC46A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7E48C3120BCCBD19
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (const_color[0].rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (rounded_primary_color.aaa), (rounded_primary_color.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp(mix((vec3(1) - const_color[3].aaa), (texcolor0.rgb), (const_color[3].aaa)), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(mix((last_tev_out.rgb), (combiner_buffer.rgb), (vec3(1) - last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 32E7001A586B3B1D

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn9();
bool Vfn11();
bool Vfn4();
bool Vfn15();
bool Vfn7();
bool Vfn5();
bool Vfn6();
bool Vfn2();
bool Vfn8();
bool Vfn10();
bool Vfn1();
bool Vfn3();
bool Vfn12();
bool Vfn14();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn9() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn11() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn4() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn15() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn2() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn8() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn10() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn1() {
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn3() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn12() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn14() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 272u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn11();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn2();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn9();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn11();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn9();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn9();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn9();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn12();
Vfn11();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn12();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn4();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn14();
}
Vfn15();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn11();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
case 272u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg12 = vs_pica.f[85];
Vfn3();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn4();
tmp_reg12 = vs_pica.f[14];
Vfn3();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn8();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn8();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn8();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn8();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn8();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn8();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn1();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn9();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn10();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, FACB327D23073768
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F223F351E532AE2B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F32082337A14C8D0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 43A593E04DCE465C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C036C7E40938EF30
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C19F695D82A9C593
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AF3AED0565E94454
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1102F1F824CB480A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C2D4BB4EEB2ADDA7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DEC7917316B4C52E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8B1BF48B59DEFA88
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2A3940C338157CDF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 7642D41FDAB29524

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn1();
bool Vfn14();
bool Vfn7();
bool Vfn2();
bool Vfn3();
bool Vfn6();
bool Vfn4();
bool Vfn9();
bool Vfn5();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn1() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn7() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn2() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn3() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn6() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn4() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn5() {
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 322u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn2();
} else {
Vfn3();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn6();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn1();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn6();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn1();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn7();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
case 322u:
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn4();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn5();
Vfn7();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn1();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn1();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, F11B937C0C4A661B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((secondary_fragment_color.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(min((const_color[2].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2 * 2.0, alpha_output_2 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp(mix((const_color[3].rgb), (last_tev_out.rgb), (vec3(1) - const_color[3].aaa)), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(mix((const_color[3].a), (last_tev_out.a), (const_color[3].a)), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (texcolor1.rgb), vec3(0), vec3(1)));
float alpha_output_4 = ByteRound(clamp((last_tev_out.a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((rounded_primary_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 680AD87DDC7E44E1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((secondary_fragment_color.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(min((const_color[2].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2 * 2.0, alpha_output_2 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp(mix((const_color[3].rgb), (last_tev_out.rgb), (vec3(1) - const_color[3].aaa)), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(mix((const_color[3].a), (last_tev_out.a), (const_color[3].a)), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (texcolor1.rgb), vec3(0), vec3(1)));
float alpha_output_4 = ByteRound(clamp((last_tev_out.a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((rounded_primary_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1A07AFF423D75A41
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
if (gl_FragCoord.x < scissor_x1 || gl_FragCoord.y < scissor_y1 || gl_FragCoord.x >= scissor_x2 || gl_FragCoord.y >= scissor_y2) discard;
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FBCE9E78C807B463
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 09F666AF13FD31A7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 322AAC6867732982
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 001CB85ED1D72B72
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ACABC9428B29917B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2C4255E482B7EA6E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FAB37C66F14E9845
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A10D8869A98622A7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 63ECF0BCAE76FAFE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8040A15D3D695FB7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F37806C91BC80178
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C1DF8B201BCF7ACB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2BB7818AF0FD3F8C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7C07E4842584DE35
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3FE17C5D2ECA15EE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[0].rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((secondary_fragment_color.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((primary_fragment_color.a) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(min((const_color[2].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2 * 2.0, alpha_output_2 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp(mix((const_color[3].rgb), (last_tev_out.rgb), (vec3(1) - const_color[3].aaa)), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(mix((const_color[3].a), (last_tev_out.a), (const_color[3].a)), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp((last_tev_out.rgb) * (texcolor1.rgb), vec3(0), vec3(1)));
float alpha_output_4 = ByteRound(clamp((last_tev_out.a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((rounded_primary_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2D41AFC161647E3F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4BEA5E101089BDF3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8F1AF2680453B77B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CA79CC22AC305429
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 89077029751166C5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B4688894E0FAC49F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9A6879E511722ED7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E4B179E03F57C2BB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 515AF31B95378B78
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D33C90A66DA7F14
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 93A6CB48F0DFD045
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F3492EF3764FEACB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8290CCEFF4ABA1AF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 017FCCDFB8716E75
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EB52FDC99E70BB3A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8C5FD235F0FDF93E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 794DA3A3260DB2E4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C6F3DD28FA112E9F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C273F749D6013A6B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ED0567956E6E8132
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 02F32E03CFE4725A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C5DFD8D3F4F2331F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 37E9ECD9B68CE134
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 89A448C2DB45CF32
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 49195A0C1A14BD9A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2ECC77E633AAD41E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 40C9669508F55F47
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CE02743DEC978C46
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E895E1170A34F77D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 27F62A8C5583D196
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B63B3E795C19369E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 03AB830E067B163A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A78E93E8239708CB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CB5E439281C5C7BC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3C3869862C2E22C4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3E8CA7CCD022A5AF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0B82D5C9DA19BE96
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 51F5ED2F405DA13C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AD84118C72ED5E6B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6D6618E879297E31
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4D019CDC7C97A812
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8851387C00535DF3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9A580F61A565C482
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 18167A8F60EB97B8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D7404D4681D7A43
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A1C5D0AE56010655
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 496B286FC08240B7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0A67ABED75033327
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D828A5822DD9B34E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6EF564A951F5C91A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D7663F105992AC9D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CFEBBE0C520880BB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D75EEFD3473DC091
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 75C07F8885F3BA19
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4B4CCF8C7068368F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 54FB367DDAA96CDC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 25D89B17AFEAC1E2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FA42D8B00C8D6149
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 370C64AC7DC58D93
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 051130508E7E4420
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3AA3B1383D59987F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B14812D1CCB9D1CC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9D3C9A74276E0E8E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1 * 2.0, alpha_output_1 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 870E0276D5876E0A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 28FABD6F85F3BA19
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4B4CCF8CC16FFA3C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 54FB367D6BAEA06F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 25D89B17368C94B0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4B4514030C8D6149
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 4.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 860BA81F7DC58D93
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 4.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3D4AB8A0B43C66F7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0 * 2.0, alpha_output_0 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DB57D23A7E8575A3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2B1A35507E2E8C9C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 49333D4304DB6E4B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8BF9C4A1E3B6C57E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 114E27096CF825E1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 962EFE6335C46664
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 91EAA257DB033B4E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 52B6BB1A7D1CB5D3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 073DC5398D2F8209
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1D94191E4341D0F2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8710D1DCC507BC75
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1699353C9B03B4A8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E3F0A9539D8C7C4F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 834317A99C6DA784
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AFFC956955E56F89
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 548717A829039DB6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8BD601A5A8704438
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8A5080EA98922B67
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D50C55D4F07450BB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F8C2226608C9164D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E524B97BF75C82AB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 292F2CC68E8E5747
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D7EA1E3C15B273E2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BA8F8FE399851AC9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 35BB737C3ABE29A9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 92FE783EFAFE8EAC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3915A77639209D51
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 555682888EB1EAE2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DF345E9AD55C45CD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6C7DFAF7B803D79E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6C8E8ADFEC70D581
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8F45903288B91F9E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 15DDDC59351D711D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5A3E668AC796F6C6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A7255194768E81F9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A30328DE8C3E6F36
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C5EC31AEC4FAC163
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 21FD17261E430A75
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8E0C0C525145D199
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4484A1D5E64E0DE7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CED6F03C1733B8AC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BABD66045B0A5603
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 27D4FDED4572CFBB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D060D6AA74463537
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4C9555D8DD69C654
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1A2946A3B6F713EA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BF815A1972014F87
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CB5AAE515ECAE8A6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E6B86413F3C2E61E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BD674623A62D59C7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5F7B9092807A9333
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CF65C257682D6468
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6F64A103CAC41B04
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 149A9327C60CA02C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ACC200A0F35FCB81
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 67008C4C43447CEB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2C86B6173DA65F61
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D9B8235EF7485317
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6BB432E9EB04BE8F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 61A2479486114892
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6290CAF82BB234D8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1777D1866E4F85CC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B04B82F696F56A42
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3B40ADBBD52E0553
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 02E99D2C9D191AF1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EE74B5736FF89CAA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3533C9157D8BB9D0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5ECAD5405E3B1FF2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6F6DBFBB101B0ADB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 888E790014790C38
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 05D248F5E6D67189
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CDDCAF3122F48CE1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E3FC0AEA5FCE24AE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0735149896C68B68
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8EE6EEA02CCA1EC7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FCBC42D4C87DB5CA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AAACB3D564843C09
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 600B57AEA03959ED
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 094636D9CFE7B325
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[1].rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((const_color[3].a) * (combiner_buffer.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(mix((const_color[5].rgb), (last_tev_out.rgb), (vec3(1) - const_color[5].aaa)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 374FCC8C94F3E57E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E366BCCF4C3F719A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1FB9B6F635F91859
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 820FD0D20874ECC2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 29708F9D58BF3843
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5B122C59A056D78B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 01D660806356BD82
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3A30DA4208A60C25
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C51CEF1F7924659F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F54EE557FF46A1F3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, C90E614B810E1C6B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E7190379370572C5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BD19FA47DEF9CFBE
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FBF7197EACA2AE44
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FFC0E7C325142D8D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 72226B8157CE6E90
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 757296D64688F093
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 48A0676D0777AF18
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9B543CECF70FDC63
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 257C6F482E85BEE0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2BB91EBDC458987E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 84B00EAE0A60C158
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 43D33802BDDDF071
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, CDB7ECA28F51733F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 138A49A848FF1654
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 079C686D6B38F368
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (primary_fragment_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((texcolor2.rgb), (secondary_fragment_color.ggg), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((vec3(1) - last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 799849542ABBBA64
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
float fog_index = (1.0 - float(depth)) * 128.0;
float fog_i = clamp(floor(fog_index), 0.0, 127.0);
float fog_f = fog_index - fog_i;
vec2 fog_lut_entry = texelFetch(tex_lut_f, int(fog_i) + fog_lut_offset).rg;
float fog_factor = fog_lut_entry.r + fog_lut_entry.g * fog_f;
fog_factor = clamp(fog_factor, 0.0, 1.0);
last_tev_out.rgb = mix(fog_color.rgb, last_tev_out.rgb, fog_factor);
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B4C8413697EF26D8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 665D6B2478FDF251
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 333EC15450F8FA23
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EFB9FF95EA5BF4CF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D93C85249C334C7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * 1.0;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AE6941B132F4BED2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E16CA69F6C9C1295
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AE0327C31D93756B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 871105CF820C63B2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 84A6DF418793C6A1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9C5AC2F7A0BA0D3E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 763323870D554424
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E34B9BD15549161B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7162C2AA5E2244CB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 37BB3C5A7DB87D7A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 77B41F440BB8269F

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn7();
bool Vfn14();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn3();
bool Vfn1();
bool Vfn9();
bool Vfn2();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn3() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn2() {
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 392u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
case 392u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 6CC786040C326E32
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 668CD4052C0EF276
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 09CE2AD4CFEAD9C9
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1BB21AFF1EE80666
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 225ECED3E1C4AA08
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3E8EE698CF4629CD
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, AE5DFCC82BEF25A8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5AA1ED401D8EE355
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6B6E4A8EF9B7DAEC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 1339CA1AC2E7935A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E60A4946F232A1B7
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FBB495C0B05A0951
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A19EED6FB585A97F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4ADABABBD6734FD2
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 67A94625EABBF321
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 48473B67B9291A0E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E8E72786B610A479
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B31, 278EF0FA5A3ECBE8

#define mul_s(x, y) mix(x * y, vec4(0), isnan(x * y))
#define fma_s(x, y, z) (mul_s(x, y) + z)
#define dot_s(x, y) dot(x, y)
#define dot_3(x, y) dot(x, y)

#define rcp_s(x) (1.0 / x)
#define rsq_s(x) inversesqrt(x)

#define min_s(x, y) min(x, y)
#define max_s(x, y) max(x, y)

layout(binding=4, std140) uniform vs_config {
bool b[16];
uvec4 i[4];
vec4 f[96];
}vs_pica;
bool ExecVS();
layout(location=0) in vec4 vs_in_reg0;
vec4 vs_out_reg0;
layout(location=1) in vec4 vs_in_reg1;
vec4 vs_out_reg1;
layout(location=2) in vec4 vs_in_reg2;
vec4 vs_out_reg2;
layout(location=3) in vec4 vs_in_reg3;
vec4 vs_out_reg3;
layout(location=4) in vec4 vs_in_reg4;
vec4 vs_out_reg4;
layout(location=5) in vec4 vs_in_reg5;
vec4 vs_out_reg5;
layout(location=6) in vec4 vs_in_reg6;
layout(location=7) in vec4 vs_in_reg7;
layout(location=8) in vec4 vs_in_reg8;
layout(location=9) in vec4 vs_in_reg9;
layout(location=10) in vec4 vs_in_reg10;
layout(location=11) in vec4 vs_in_reg11;
layout(location=0) out vec4 primary_color;
layout(location=1) out vec4 texcoord0;
layout(location=2) out vec4 texcoord12;
layout(location=3) out vec4 normquat;
layout(location=4) out vec4 view;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};
void EmitVtx() {
vec4 vtx_pos = vec4(vs_out_reg0.x,vs_out_reg0.y,vs_out_reg0.z,vs_out_reg0.w);
gl_Position = vtx_pos;
#if !defined(CITRA_GLES) || defined(GL_EXT_clip_cull_distance)
gl_ClipDistance[0] = -vtx_pos.z;
gl_ClipDistance[1] = dot(clip_coef, vtx_pos);
#endif
normquat = vec4(vs_out_reg1.x,vs_out_reg1.y,vs_out_reg1.z,vs_out_reg1.w);
vec4 vtx_color = vec4(vs_out_reg2.x,vs_out_reg2.y,vs_out_reg2.z,vs_out_reg2.w);
primary_color = clamp(vtx_color,vec4(0),vec4(1));
texcoord0 = vec4(vs_out_reg3.x,vs_out_reg3.y,vs_out_reg3.w,1);
texcoord12 = vec4(vs_out_reg5.x,vs_out_reg5.y,vs_out_reg5.z,vs_out_reg5.w);
view = vec4(vs_out_reg4.x,vs_out_reg4.y,vs_out_reg4.z,1);
}
void main() {
vs_out_reg0 = vec4(0, 0, 0, 1);
vs_out_reg1 = vec4(0, 0, 0, 1);
vs_out_reg2 = vec4(0, 0, 0, 1);
vs_out_reg3 = vec4(0, 0, 0, 1);
vs_out_reg4 = vec4(0, 0, 0, 1);
vs_out_reg5 = vec4(0, 0, 0, 1);
ExecVS();
EmitVtx();
}
bvec2 bool_regs = bvec2(false);
ivec3 addr_regs = ivec3(0);
bool Vfn8();
bool Vfn10();
bool Vfn7();
bool Vfn14();
bool Vfn4();
bool Vfn5();
bool Vfn6();
bool Vfn3();
bool Vfn1();
bool Vfn9();
bool Vfn2();
bool Vfn15();
bool Vfn11();
bool Vfn13();
bool Vfn0();
vec4 tmp_reg0;
vec4 tmp_reg1;
vec4 tmp_reg2;
vec4 tmp_reg3;
vec4 tmp_reg4;
vec4 tmp_reg5;
vec4 tmp_reg6;
vec4 tmp_reg7;
vec4 tmp_reg8;
vec4 tmp_reg9;
vec4 tmp_reg10;
vec4 tmp_reg11;
vec4 tmp_reg12;
vec4 tmp_reg13;
vec4 tmp_reg14;
vec4 tmp_reg15;
bool ExecVS() {
tmp_reg0 = vec4(0, 0, 0, 1);
tmp_reg1 = vec4(0, 0, 0, 1);
tmp_reg2 = vec4(0, 0, 0, 1);
tmp_reg3 = vec4(0, 0, 0, 1);
tmp_reg4 = vec4(0, 0, 0, 1);
tmp_reg5 = vec4(0, 0, 0, 1);
tmp_reg6 = vec4(0, 0, 0, 1);
tmp_reg7 = vec4(0, 0, 0, 1);
tmp_reg8 = vec4(0, 0, 0, 1);
tmp_reg9 = vec4(0, 0, 0, 1);
tmp_reg10 = vec4(0, 0, 0, 1);
tmp_reg11 = vec4(0, 0, 0, 1);
tmp_reg12 = vec4(0, 0, 0, 1);
tmp_reg13 = vec4(0, 0, 0, 1);
tmp_reg14 = vec4(0, 0, 0, 1);
tmp_reg15 = vec4(0, 0, 0, 1);
Vfn0();
return true;
}

bool Vfn8() {
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn10() {
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
return false;
}
bool Vfn7() {
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn14() {
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn4() {
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
return false;
}
bool Vfn5() {
vs_out_reg1.z = rcp_s(tmp_reg12.x);
vs_out_reg1.xy = (mul_s(tmp_reg13, tmp_reg12)).xy;
return false;
}
bool Vfn6() {
vs_out_reg1.xyz = (vs_pica.f[93].yxxx).xyz;
return false;
}
bool Vfn3() {
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
return false;
}
bool Vfn1() {
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
return false;
}
bool Vfn9() {
uint jmp_to = 105u;
while (true) {
switch (jmp_to) {
case 105u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
default: return false;
}
}
return false;
}
bool Vfn2() {
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
return false;
}
bool Vfn15() {
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
return false;
}
bool Vfn11() {
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
return false;
}
bool Vfn13() {
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
return false;
}
bool Vfn0() {
uint jmp_to = 386u;
while (true) {
switch (jmp_to) {
case 2u:
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12].xyz, tmp_reg12.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
vs_out_reg4 = -tmp_reg9;
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vs_pica.f[93].yyyy + tmp_reg11.zzzz;
tmp_reg13 = mul_s(vs_pica.f[95].zzzz, tmp_reg11);
bool_regs = equal(-vs_pica.f[93].yy, tmp_reg11.zz);
tmp_reg12 = mul_s(vs_pica.f[95].zzzz, tmp_reg12);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
if (!bool_regs.x) {
Vfn5();
} else {
Vfn6();
}
vs_out_reg1.w = (vs_pica.f[93].xxxx).w;
tmp_reg9.w = dot_3(vs_pica.f[93].yyy, tmp_reg9.xyz);
tmp_reg9.w = rcp_s(tmp_reg9.w);
tmp_reg9 = mul_s(tmp_reg9, tmp_reg9.wwww);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.x = (ivec2(tmp_reg12.ww)).x;
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg10.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(tmp_reg10, tmp_reg13);
tmp_reg8.y = dot_s(tmp_reg10, tmp_reg14);
tmp_reg8.z = dot_s(tmp_reg10, tmp_reg15);
tmp_reg0 = floor(tmp_reg12);
tmp_reg1 = floor(tmp_reg13);
tmp_reg2 = floor(tmp_reg14);
tmp_reg8 = mul_s(tmp_reg8, tmp_reg11);
tmp_reg3 = tmp_reg12 + -tmp_reg0;
tmp_reg4 = tmp_reg13 + -tmp_reg1;
tmp_reg5 = tmp_reg14 + -tmp_reg2;
tmp_reg9 = mul_s(tmp_reg9, tmp_reg11);
tmp_reg0 = mul_s(vs_pica.f[93].wwww, tmp_reg0);
tmp_reg1 = mul_s(vs_pica.f[93].wwww, tmp_reg1);
tmp_reg2 = mul_s(vs_pica.f[93].wwww, tmp_reg2);
tmp_reg10.xyz = (fma_s(tmp_reg0, tmp_reg8.xxxx, tmp_reg10)).xyz;
tmp_reg15.x = (mul_s(tmp_reg0.wwww, tmp_reg8.wwww)).x;
tmp_reg15.y = (mul_s(tmp_reg1.wwww, tmp_reg8.wwww)).y;
tmp_reg15.z = (mul_s(tmp_reg2.wwww, tmp_reg8.wwww)).z;
tmp_reg12 = vs_pica.f[95].xxxx;
tmp_reg10.xyz = (fma_s(tmp_reg1, tmp_reg8.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg2, tmp_reg8.zzzz, tmp_reg15)).xyz;
tmp_reg3 = abs(tmp_reg3);
tmp_reg4 = abs(tmp_reg4);
tmp_reg5 = abs(tmp_reg5);
tmp_reg3 = fma_s(tmp_reg3, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg4 = fma_s(tmp_reg4, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg5 = fma_s(tmp_reg5, tmp_reg12, -vs_pica.f[93].yyyy);
tmp_reg10.xyz = (fma_s(tmp_reg3, tmp_reg9.xxxx, tmp_reg10)).xyz;
tmp_reg12.x = (fma_s(tmp_reg3.wwww, tmp_reg9.wwww, tmp_reg15.xxxx)).x;
tmp_reg12.y = (fma_s(tmp_reg4.wwww, tmp_reg9.wwww, tmp_reg15.yyyy)).y;
tmp_reg12.z = (fma_s(tmp_reg5.wwww, tmp_reg9.wwww, tmp_reg15.zzzz)).z;
tmp_reg10.xyz = (fma_s(tmp_reg4, tmp_reg9.yyyy, tmp_reg10)).xyz;
tmp_reg15.xyz = (fma_s(tmp_reg5, tmp_reg9.zzzz, tmp_reg12)).xyz;
tmp_reg10.xyz = (tmp_reg10 + tmp_reg15).xyz;
addr_regs.x = (ivec2(tmp_reg8.xx)).x;
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_pica.f[10 + addr_regs.x], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11 + addr_regs.x], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12 + addr_regs.x], vs_in_reg0);
tmp_reg7.x = dot_3(vs_pica.f[10 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.y = dot_3(vs_pica.f[11 + addr_regs.x].xyz, tmp_reg12.xyz);
tmp_reg7.z = dot_3(vs_pica.f[12 + addr_regs.x].xyz, tmp_reg12.xyz);
if (vs_pica.b[14]) {
jmp_to = 118u; break;
}
Vfn3();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
case 118u:
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
tmp_reg15.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = (tmp_reg11.zzzz).y;
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg15 = mul_s(tmp_reg9, tmp_reg15.xxxx);
tmp_reg13.x = dot_3(-tmp_reg15.xyz, tmp_reg11.xyz);
tmp_reg13.xzw = (fma_s(tmp_reg13.xxxx, tmp_reg12.xxxz, tmp_reg12.yyyw)).xzw;
tmp_reg13 = min_s(vs_pica.f[93].yyyy, tmp_reg13);
tmp_reg13 = max_s(vs_pica.f[93].xxxx, tmp_reg13);
tmp_reg13.xw = (mul_s(tmp_reg13.xwww, tmp_reg13.xwww)).xw;
vs_out_reg2 = tmp_reg13;
Vfn8();
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xyz;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (vs_in_reg1).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
Vfn8();
tmp_reg0 = vs_in_reg1.xyyy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1 = vs_in_reg0.xyyy;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg13 = mul_s(vs_pica.f[18], vs_in_reg2);
vs_out_reg3.x = dot_3(vs_pica.f[14].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[15].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[16].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[17].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, tmp_reg13);
if (vs_pica.b[14]) {
jmp_to = 493u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg1);
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
addr_regs.xy = ivec2(tmp_reg8.xy);
tmp_reg13 = mul_s(vs_pica.f[10 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg14 = mul_s(vs_pica.f[11 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg15 = mul_s(vs_pica.f[12 + addr_regs.x], tmp_reg9.xxxx);
tmp_reg13 = fma_s(tmp_reg9.yyyy, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.yyyy, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.yyyy, vs_pica.f[12 + addr_regs.y], tmp_reg15);
addr_regs.xy = ivec2(tmp_reg8.zw);
tmp_reg13 = fma_s(tmp_reg9.zzzz, vs_pica.f[10 + addr_regs.x], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.zzzz, vs_pica.f[11 + addr_regs.x], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.zzzz, vs_pica.f[12 + addr_regs.x], tmp_reg15);
tmp_reg13 = fma_s(tmp_reg9.wwww, vs_pica.f[10 + addr_regs.y], tmp_reg13);
tmp_reg14 = fma_s(tmp_reg9.wwww, vs_pica.f[11 + addr_regs.y], tmp_reg14);
tmp_reg15 = fma_s(tmp_reg9.wwww, vs_pica.f[12 + addr_regs.y], tmp_reg15);
tmp_reg8.w = (vs_pica.f[93].yyyy).w;
tmp_reg8.x = dot_s(vs_in_reg0, tmp_reg13);
tmp_reg8.y = dot_s(vs_in_reg0, tmp_reg14);
tmp_reg8.z = dot_s(vs_in_reg0, tmp_reg15);
Vfn11();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn11();
tmp_reg7.x = dot_3(tmp_reg12.xyz, tmp_reg13.xyz);
tmp_reg7.y = dot_3(tmp_reg12.xyz, tmp_reg14.xyz);
tmp_reg7.z = dot_3(tmp_reg12.xyz, tmp_reg15.xyz);
tmp_reg9.x = dot_s(vs_pica.f[4], tmp_reg8);
tmp_reg9.y = dot_s(vs_pica.f[5], tmp_reg8);
tmp_reg9.z = dot_s(vs_pica.f[6], tmp_reg8);
tmp_reg9.w = (tmp_reg8.wwww).w;
tmp_reg12 = vec4(dot_3(tmp_reg7.xyz, tmp_reg7.xyz));
vs_out_reg0.x = dot_s(vs_pica.f[0], tmp_reg9);
vs_out_reg0.y = dot_s(vs_pica.f[1], tmp_reg9);
vs_out_reg0.z = dot_s(vs_pica.f[2], tmp_reg9);
vs_out_reg0.w = dot_s(vs_pica.f[3], tmp_reg9);
tmp_reg12 = vec4(rsq_s(tmp_reg12.x));
vs_out_reg4 = -tmp_reg9;
tmp_reg7 = mul_s(tmp_reg7, tmp_reg12);
tmp_reg11.w = (vs_pica.f[93].xxxx).w;
tmp_reg11.x = dot_3(vs_pica.f[4].xyz, tmp_reg7.xyz);
tmp_reg11.y = dot_3(vs_pica.f[5].xyz, tmp_reg7.xyz);
tmp_reg11.z = dot_3(vs_pica.f[6].xyz, tmp_reg7.xyz);
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
Vfn7();
vs_out_reg3.xyz = (vs_in_reg2).xyz;
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg14 = -vs_pica.f[14] + tmp_reg8;
tmp_reg13.y = dot_3(tmp_reg14.xyz, tmp_reg14.xyz);
tmp_reg14.x = rsq_s(tmp_reg13.y);
tmp_reg13.z = (fma_s(tmp_reg14.xxxx, tmp_reg13.yyyy, -vs_pica.f[14].wwww)).z;
tmp_reg13.z = (mul_s(vs_pica.f[16].xxxx, tmp_reg13.zzzz)).z;
tmp_reg13.z = (fma_s(tmp_reg13.zzzz, tmp_reg13.zzzz, -vs_pica.f[16].yyyy)).z;
tmp_reg15 = min_s(vs_pica.f[93].xxxx, tmp_reg13.zzzz);
tmp_reg13.w = (mul_s(tmp_reg15, tmp_reg15)).w;
tmp_reg8 = fma_s(tmp_reg13.wwww, vs_pica.f[15], tmp_reg8);
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 499u; break;
}
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
tmp_reg8.x = dot_s(vs_pica.f[10], vs_in_reg0);
tmp_reg8.y = dot_s(vs_pica.f[11], vs_in_reg0);
tmp_reg8.z = dot_s(vs_pica.f[12], vs_in_reg0);
tmp_reg8.w = dot_s(vs_pica.f[13], vs_in_reg0);
if (vs_pica.b[0]) {
Vfn13();
}
Vfn14();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[17], tmp_reg12);
case 246u:
tmp_reg13.x = dot_3(tmp_reg9.xyz, tmp_reg9.xyz);
tmp_reg13.y = rsq_s(tmp_reg13.x);
tmp_reg0 = mul_s(tmp_reg9, -tmp_reg13.yyyy);
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
tmp_reg14.x = dot_3(tmp_reg0.xyz, tmp_reg11.xyz);
tmp_reg14.y = (mul_s(vs_pica.f[95].xxxx, tmp_reg14.xxxx)).y;
tmp_reg1 = fma_s(tmp_reg14.yyyy, tmp_reg11, -tmp_reg0);
tmp_reg12.xy = (vs_pica.f[93].yyyy + tmp_reg1.xyyy).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[95].zwzw, tmp_reg12.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_in_reg3.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[91], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 246u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 266u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn3();
Vfn10();
if (vs_pica.b[14]) {
jmp_to = 484u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 273u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg12 = vs_pica.f[85];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 487u; break;
}
Vfn7();
tmp_reg12 = vs_pica.f[14];
Vfn15();
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xyz;
if (vs_pica.b[14]) {
jmp_to = 488u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
case 292u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
tmp_reg3 = tmp_reg0 + -tmp_reg8;
tmp_reg14 = max_s(vs_pica.f[93].xxxx, tmp_reg7.zzzz);
tmp_reg15.x = dot_s(tmp_reg3, tmp_reg3);
tmp_reg14 = min_s(vs_pica.f[93].yyyy, tmp_reg14);
Vfn4();
tmp_reg15.x = rsq_s(tmp_reg15.x);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = vec4(dot_3(tmp_reg1.xyz, tmp_reg8.xyz));
tmp_reg15 = mul_s(tmp_reg3, tmp_reg15.xxxx);
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg4 = vec4(dot_3(tmp_reg7.xyz, tmp_reg15.xyz));
tmp_reg14 = mul_s(tmp_reg14, tmp_reg14);
tmp_reg12 = tmp_reg12 + tmp_reg1.wwww;
tmp_reg4 = max_s(vs_pica.f[93].xxxx, tmp_reg4);
tmp_reg14 = mul_s(vs_pica.f[95].zzzz, tmp_reg14);
tmp_reg4 = min_s(vs_pica.f[93].yyyy, tmp_reg4);
vs_out_reg2.w = (vec4(greaterThanEqual(vs_pica.f[93].xxxx, tmp_reg12))).w;
vs_out_reg2.xyz = (fma_s(tmp_reg4, tmp_reg1, tmp_reg14)).xyz;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg12 = vs_pica.f[87];
tmp_reg0 = vs_pica.f[85];
tmp_reg1 = vs_pica.f[86];
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg5.xy = (fma_s(vs_in_reg2.xyyy, vs_pica.f[94].xxxx, tmp_reg12.xyyy)).xy;
vs_out_reg5.zw = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy)).zw;
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[15].xxxx, vs_in_reg3);
vs_out_reg2 = vs_pica.f[15].yyyy + tmp_reg12;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg6 = vs_pica.f[88];
tmp_reg7 = vs_pica.f[90];
case 332u:
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
case 333u:
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = tmp_reg6;
tmp_reg9 = tmp_reg7;
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 266u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[91].xxxx;
tmp_reg8 = vs_pica.f[87];
tmp_reg9 = vs_pica.f[89];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
if (vs_pica.b[14]) {
jmp_to = 273u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg6 = vs_pica.f[86];
tmp_reg7 = vs_pica.f[88];
if (vs_pica.b[14]) {
jmp_to = 333u; break;
}
tmp_reg10 = mul_s(vs_pica.f[95].yyyy, vs_in_reg0);
tmp_reg11 = vs_pica.f[89].xxxx;
tmp_reg8 = vs_pica.f[85];
tmp_reg9 = vs_pica.f[87];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[86];
tmp_reg9 = vs_pica.f[88];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
tmp_reg15.x = (vs_pica.f[93].xxxx).x;
tmp_reg15.y = (vs_pica.f[90].xxxx).y;
tmp_reg0 = vs_pica.f[91];
tmp_reg1 = vs_pica.f[92];
vs_out_reg5 = fma_s(vs_in_reg2.xyxy, vs_pica.f[94].xxxx, tmp_reg15.xyxy);
if (vs_pica.b[14]) {
jmp_to = 292u; break;
}
case 386u:
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg6 = vs_pica.f[89];
tmp_reg7 = vs_pica.f[91];
if (vs_pica.b[14]) {
jmp_to = 332u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
tmp_reg11 = vs_pica.f[92].xxxx;
tmp_reg8 = vs_pica.f[88];
tmp_reg9 = vs_pica.f[90];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg6);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg7);
Vfn1();
tmp_reg8 = vs_pica.f[89];
tmp_reg9 = vs_pica.f[91];
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg8);
tmp_reg13 = mul_s(vs_pica.f[94].yyyy, vs_in_reg9);
tmp_reg14 = mul_s(vs_pica.f[94].yyyy, vs_in_reg10);
Vfn1();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg11);
if (vs_pica.b[14]) {
jmp_to = 410u; break;
}
tmp_reg10 = mul_s(vs_pica.f[93].zzzz, vs_in_reg0);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg5);
case 410u:
tmp_reg8 = vs_in_reg3;
tmp_reg9 = mul_s(vs_pica.f[94].wwww, vs_in_reg4);
tmp_reg12 = mul_s(vs_pica.f[93].wwwy, vs_in_reg1);
Vfn2();
Vfn4();
if (vs_pica.b[0]) {
jmp_to = 246u; break;
}
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
tmp_reg12 = mul_s(vs_pica.f[94].yyyy, vs_in_reg3);
vs_out_reg2 = mul_s(vs_pica.f[14], tmp_reg12);
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
Vfn8();
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg2);
vs_out_reg3.xyz = (mul_s(vs_pica.f[94].xxxx, vs_in_reg1)).xyz;
if (vs_pica.b[14]) {
jmp_to = 486u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
Vfn7();
vs_out_reg3.x = dot_3(vs_pica.f[16].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[17].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[18].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[19].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
tmp_reg9.z = dot_s(vec4(vs_in_reg0.xyz, 1.0), vs_pica.f[6]);
vs_out_reg0 = vs_in_reg0;
vs_out_reg3.yz = (vs_pica.f[93].xxxx).yz;
tmp_reg13.x = (vs_pica.f[14].yyyy + tmp_reg9.zzzz).x;
tmp_reg12.x = rcp_s(-tmp_reg9.z);
tmp_reg13.x = (mul_s(vs_pica.f[14].xxxx, tmp_reg13.xxxx)).x;
tmp_reg12.xy = (mul_s(vs_pica.f[15].ywww, tmp_reg12.xxxx)).xy;
tmp_reg12.xy = (fma_s(tmp_reg12.xyyy, tmp_reg13.xxxx, vs_pica.f[15].xzzz)).xy;
tmp_reg12.xy = (max_s(vs_pica.f[93].xxxx, tmp_reg12.xyyy)).xy;
tmp_reg12.xy = (min_s(vs_pica.f[93].yyyy, tmp_reg12.xyyy)).xy;
vs_out_reg3.x = (min_s(tmp_reg12.xxxx, tmp_reg12.yyyy)).x;
if (vs_pica.b[14]) {
jmp_to = 485u; break;
}
tmp_reg8 = vs_in_reg4.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg0.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2)).xy;
tmp_reg1.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg3)).xy;
tmp_reg0.z = (vs_pica.f[93].yyyy).z;
tmp_reg1.z = (vs_pica.f[93].yyyy).z;
tmp_reg0.y = (-vs_pica.f[93].yyyy + tmp_reg0.yyyy).y;
tmp_reg1.y = (-vs_pica.f[93].yyyy + tmp_reg1.yyyy).y;
tmp_reg8 = vs_in_reg4.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg3.x = dot_3(vs_pica.f[87].xyz, tmp_reg0.xyz);
vs_out_reg3.yz = vec2(dot_3(vs_pica.f[88].xyz, tmp_reg0.xyz));
vs_out_reg5.x = dot_3(vs_pica.f[89].xyz, tmp_reg1.xyz);
vs_out_reg5.y = dot_3(vs_pica.f[90].xyz, tmp_reg1.xyz);
vs_out_reg2 = mul_s(vs_pica.f[94].yyyy, vs_in_reg4);
if (vs_pica.b[14]) {
jmp_to = 495u; break;
}
Vfn7();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
if (vs_pica.b[14]) {
jmp_to = 2u; break;
}
tmp_reg8 = vs_pica.f[95].zzzz + vs_in_reg1.wwww;
tmp_reg12 = mul_s(vs_pica.f[93].wwww, vs_in_reg1);
Vfn9();
Vfn4();
vs_out_reg5 = mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyxy);
vs_out_reg2 = vs_pica.f[93].yyyy;
if (vs_pica.b[14]) {
jmp_to = 500u; break;
}
vs_out_reg0 = vs_pica.f[93].xxxx;
case 484u:
vs_out_reg3.xyz = (vs_pica.f[93].xxxx).xyz;
case 485u:
vs_out_reg2 = vs_pica.f[93].xxxx;
case 486u:
vs_out_reg4 = vs_pica.f[93].xxxx;
case 487u:
vs_out_reg1 = vs_pica.f[93].xxxx;
case 488u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.xy = (vs_pica.f[93].xxxx).xy;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 493u:
vs_out_reg4 = vs_pica.f[93].xxxx;
vs_out_reg1 = vs_pica.f[93].xxxx;
case 495u:
vs_out_reg3.w = (vs_pica.f[93].xxxx).w;
vs_out_reg5.zw = (vs_pica.f[93].xxxx).zw;
return true;
case 499u:
vs_out_reg5.xy = (mul_s(vs_pica.f[94].xxxx, vs_in_reg2.xyyy)).xy;
case 500u:
vs_out_reg3.x = dot_s(-vs_pica.f[7], tmp_reg8);
vs_out_reg3.yz = vec2(dot_s(-vs_pica.f[8], tmp_reg8));
vs_out_reg3.w = dot_s(-vs_pica.f[9], tmp_reg8);
return true;
default: return false;
}
}
return false;
}
// shader: 8B30, 82E75DE03E272DC6
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 5907EEE02B1B2CDC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6A45D1AB9D51E69A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 251B28F68D817C9B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B26DD556EF4865DC
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, A797D69DF99E7B62
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D1FCD60A320A5190
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0F77FB246A493A03
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 505D1F6822E12FBF
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DA2D7E3A252B29F1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 4AD845688008C26D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 42D421B50B7BFAD1
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FFC69F8F8D70E682
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9B469114F3FF6691
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 60DE419C8CA00D8A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 0A17B99BEC8980E4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8ECF666E441EF81A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2E7F4E1D616CE909
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BFE25938FDC48580
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 49C1D54C9BB4AFF0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6DCCD3605F410191
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2C5ACB486C86B533
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 14ED970EEA4028A5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(fma((primary_fragment_color.rgb), (last_tev_out.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((texcolor2.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E02ABD110B6DBEF4
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 25B9C13CF176223D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = (vec3(1) - last_tev_out.rgb);
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 55AC60055A370309
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 760CE54118DA75A3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position + view.xyz);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[1].dist_atten_scale * length(-light_src[1].position - view.xyz) + light_src[1].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product * shadow.rgb) + light_src[1].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value) * shadow.rgb;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 270D710301814E2C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B7C6A3742A1468F5
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7D744EA5F2992D58
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9F957C1F45B6D246
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 8740C93E710C8D1C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F20EDFB193B8E7EA
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7AE060FDD9B123E0
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FABF5C266995752A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3D1D583D2C8E3D9D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 7EBE3CC6CBF09B2C
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, BD6AD2CF24844AA3
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, FFBC46B3AB0BFB24
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9CD647EF85EF7CD8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 63051D9088D1408D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor1.rgb);
float alpha_output_0 = (texcolor1.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(mix((texcolor2.rgb), (last_tev_out.rgb), (vec3(1) - texcolor2.aaa)), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(mix((texcolor2.a), (last_tev_out.a), (texcolor2.a)), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((const_color[2].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp(min((primary_fragment_color.rgb) + (const_color[3].rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp(min((primary_fragment_color.a) + (const_color[3].a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((last_tev_out.rgb) + (rounded_primary_color.rgb), vec3(1)) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = (last_tev_out.rgb);
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, D2085A77D44D517A
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, F3A71ED69D6929ED
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (rounded_primary_color.rgb);
float alpha_output_0 = (rounded_primary_color.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, B859757285851B30
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 2D4084AEB2B4161F
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (const_color[0].a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((primary_fragment_color.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, DDFD33886541B273
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_1 = ByteRound(clamp((const_color[1].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_3 = ByteRound(clamp((last_tev_out.a) * (const_color[3].a), 0.0, 1.0));
last_tev_out = vec4(color_output_3 * 2.0, alpha_output_3 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, ABCDDEF36CE57D3D
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_2 = ByteRound(clamp((const_color[2].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_3 = ByteRound(clamp((primary_fragment_color.rgb) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((last_tev_out.rgb) * (combiner_buffer.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, EE316CD84AB4DBC8
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((last_tev_out.rgb) * (texcolor2.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 976EE145E7686DFB
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor0 = textureLod(tex0, texcoord0.xy, GetLod(texcoord0.xy * vec2(textureSize(tex0, 0))) + tex_lod_bias[0]);
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = (texcolor0.rgb);
float alpha_output_0 = (texcolor0.a);
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp((texcolor1.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp((texcolor1.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((last_tev_out.rgb) * (const_color[2].rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((last_tev_out.a) * (const_color[2].a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((last_tev_out.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (secondary_fragment_color.ggg)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 6885B2A8F9F42002
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((const_color[0].rgb) * (secondary_fragment_color.rrr), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((const_color[0].a) * (secondary_fragment_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((const_color[1].rgb) + (primary_fragment_color.rgb), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = ByteRound(clamp(min((const_color[1].a) + (primary_fragment_color.a), 1.0) * (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp((rounded_primary_color.rgb) + (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_2 = ByteRound(clamp((rounded_primary_color.a) + (last_tev_out.a), 0.0, 1.0));
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((texcolor1.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_3 = (const_color[3].a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_5 = ByteRound(clamp(fma((secondary_fragment_color.ggg), (texcolor2.rgb), (last_tev_out.rgb)), vec3(0), vec3(1)));
float alpha_output_5 = (last_tev_out.a);
last_tev_out = vec4(color_output_5, alpha_output_5);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 3CD6D6389535A415
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 texcolor2 = textureLod(tex2, texcoord12.zw, GetLod(texcoord12.zw * vec2(textureSize(tex2, 0))) + tex_lod_bias[2]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.a *= shadow.a;
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rgb) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.a) * (rounded_primary_color.a), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_1 = ByteRound(clamp(min((primary_fragment_color.rgb) + (secondary_fragment_color.ggg), vec3(1)) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_1 = (last_tev_out.a);
last_tev_out = vec4(color_output_1, alpha_output_1);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_2 = ByteRound(clamp(mix((last_tev_out.rgb), (texcolor2.rgb), (const_color[2].rgb)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = ByteRound(clamp((const_color[5].a) * (texcolor1.a), 0.0, 1.0));
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, 9062E97D0BCDF52E
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[7].position + view.xyz);
spot_dir = light_src[7].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][3];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[7].dist_atten_scale * length(-light_src[7].position - view.xyz) + light_src[7].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[7].diffuse * dot_product) + light_src[7].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[7].specular_0) + (refl_value * light_src[7].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// shader: 8B30, E85EE933CF59566B
layout(location=0) in vec4 primary_color;
layout(location=1) in vec4 texcoord0;
layout(location=2) in vec4 texcoord12;
layout(location=3) in vec4 normquat;
layout(location=4) in vec4 view;
out vec4 color;
layout(binding=0) uniform sampler2D tex0;
layout(binding=1) uniform sampler2D tex1;
layout(binding=2) uniform sampler2D tex2;
layout(binding=3) uniform samplerBuffer tex_lut_l;
layout(binding=4) uniform samplerBuffer tex_lut_f;
layout(binding=5) uniform samplerBuffer tex_lut_rg;
layout(binding=6) uniform samplerBuffer tex_lut_rgba;

layout(binding=0, std140) uniform common_data {
vec3 fog_color;
vec3 tex_lod_bias;
vec4 clip_coef;
vec4 blend_color;
vec4 tev_combiner_color;
vec4 const_color[6];
float scissor_x1;
float scissor_y1;
float scissor_x2;
float scissor_y2;
float alphatest_ref;
float depth_scale;
float depth_offset;
float shadow_bias_constant;
float shadow_bias_linear;
int shadow_texture_bias;
int fog_lut_offset;
};

struct LightSrc {
vec3 specular_0;
vec3 specular_1;
vec3 diffuse;
vec3 ambient;
vec3 position;
vec3 spot_direction;
float dist_atten_bias;
float dist_atten_scale;
};
layout(binding=1, std140) uniform light_data0 {
LightSrc light_src[8];
};
layout(binding=2, std140) uniform light_data1 {
ivec4 lighting_lut_offset[6];
vec3 lighting_global_ambient;
float lut_scale_d0;
float lut_scale_d1;
float lut_scale_sp;
float lut_scale_fr;
float lut_scale_rb;
float lut_scale_rg;
float lut_scale_rr;
};

vec3 QuaternionRotate(vec4 q, vec3 v) {
    return v + 2.0 * cross(q.xyz, cross(q.xyz, v) + q.w * v);
}
float LightingLUTUnsigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 256.0), 0, 255);
    float delta = pos * 256.0 - float(index);
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}
float LightingLUTSigned(int lut_offset, float pos) {
    int index = clamp(int(pos * 128.0), -128, 127);
    float delta = pos * 128.0 - float(index);
    if (index < 0) index += 256;
    vec2 entry = texelFetch(tex_lut_l, lut_offset + index).rg;
    return entry.r + entry.g * delta;
}

float ByteRound(float x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec3 ByteRound(vec3 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
vec4 ByteRound(vec4 x) {
    return round(x * 255.0) * (1.0 / 255.0);
}
float GetLod(vec2 coord) {
    vec2 d = max(abs(dFdx(coord)), abs(dFdy(coord)));
    return log2(max(d.x, d.y));
}

void main() {
vec4 rounded_primary_color = ByteRound(primary_color);
vec4 primary_fragment_color = vec4(0);
vec4 secondary_fragment_color = vec4(0);
float z_over_w = 2.0 * gl_FragCoord.z - 1.0;
float depth = z_over_w * depth_scale + depth_offset;
vec4 texcolor1 = textureLod(tex1, texcoord12.xy, GetLod(texcoord12.xy * vec2(textureSize(tex1, 0))) + tex_lod_bias[1]);
vec4 diffuse_sum = vec4(0, 0, 0, 1);
vec4 specular_sum = vec4(0, 0, 0, 1);
vec3 light_vector = vec3(0);
vec3 refl_value = vec3(0);
vec3 spot_dir = vec3(0);
vec3 half_vector = vec3(0);
float dot_product = 0.0;
float clamp_highlights = 1.0;
int lut_offset = 0;
float lut_value = 1.0;
float d0_value = 1.0;
float d1_value = 1.0;
float dist_value = 1.0;
float geo_factor = 1.0;
vec3 surface_normal = vec3(0, 0, 1);
vec3 surface_tangent = vec3(1, 0, 0);
vec4 normalized_normquat = normalize(normquat);
vec3 normal = QuaternionRotate(normalized_normquat, surface_normal);
vec3 tangent = QuaternionRotate(normalized_normquat, surface_tangent);
vec4 shadow = vec4(1);
light_vector = normalize(light_src[0].position + view.xyz);
spot_dir = light_src[0].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[0].dist_atten_scale * length(-light_src[0].position - view.xyz) + light_src[0].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[0].diffuse * dot_product) + light_src[0].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[0].specular_0) + (refl_value * light_src[0].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[1].position);
spot_dir = light_src[1].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[1].diffuse * dot_product) + light_src[1].ambient) * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[1].specular_0) + (refl_value * light_src[1].specular_1)) * clamp_highlights * 1.0;
light_vector = normalize(light_src[2].position + view.xyz);
spot_dir = light_src[2].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[2][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[4][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[2].dist_atten_scale * length(-light_src[2].position - view.xyz) + light_src[2].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[2].diffuse * dot_product) + light_src[2].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[2].specular_0) + (refl_value * light_src[2].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[3].position + view.xyz);
spot_dir = light_src[3].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[4][3];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[3].dist_atten_scale * length(-light_src[3].position - view.xyz) + light_src[3].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[3].diffuse * dot_product) + light_src[3].ambient) * dist_value * 1.0;
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[3].specular_0) + (refl_value * light_src[3].specular_1)) * clamp_highlights * dist_value * 1.0;
light_vector = normalize(light_src[4].position + view.xyz);
spot_dir = light_src[4].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][0];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][0];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[4].dist_atten_scale * length(-light_src[4].position - view.xyz) + light_src[4].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[4].diffuse * dot_product) + light_src[4].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[4].specular_0) + (refl_value * light_src[4].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[5].position + view.xyz);
spot_dir = light_src[5].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][1];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][1];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[5].dist_atten_scale * length(-light_src[5].position - view.xyz) + light_src[5].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[5].diffuse * dot_product) + light_src[5].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[5].specular_0) + (refl_value * light_src[5].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
light_vector = normalize(light_src[6].position + view.xyz);
spot_dir = light_src[6].spot_direction;
half_vector = normalize(view.xyz) + light_vector;
dot_product = max(dot(light_vector, normal), 0.0);
lut_offset = lighting_lut_offset[1][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(normal, normalize(half_vector)), 0.0));
refl_value.r = lut_scale_rr * lut_value;
refl_value.g = refl_value.r;
refl_value.b = refl_value.r;
lut_offset = lighting_lut_offset[3][2];
lut_value = LightingLUTUnsigned(lut_offset, max(dot(light_vector, spot_dir), 0.0));
lut_offset = lighting_lut_offset[5][2];
dist_value = LightingLUTUnsigned(lut_offset, clamp(light_src[6].dist_atten_scale * length(-light_src[6].position - view.xyz) + light_src[6].dist_atten_bias, 0.0, 1.0));
lut_offset = lighting_lut_offset[0][0];
d0_value = LightingLUTSigned(lut_offset, dot(light_vector, normal));
diffuse_sum.rgb += ((light_src[6].diffuse * dot_product) + light_src[6].ambient) * dist_value * (lut_scale_sp * lut_value);
specular_sum.rgb += (((lut_scale_d0 * d0_value) * light_src[6].specular_0) + (refl_value * light_src[6].specular_1)) * clamp_highlights * dist_value * (lut_scale_sp * lut_value);
diffuse_sum.rgb += lighting_global_ambient;
primary_fragment_color = clamp(diffuse_sum, vec4(0), vec4(1));
secondary_fragment_color = clamp(specular_sum, vec4(0), vec4(1));
vec4 combiner_buffer = vec4(0);
vec4 next_combiner_buffer = tev_combiner_color;
vec4 last_tev_out = vec4(0);
vec3 color_output_0 = ByteRound(clamp((texcolor1.rrr) * (rounded_primary_color.rgb), vec3(0), vec3(1)));
float alpha_output_0 = ByteRound(clamp((texcolor1.g) * (secondary_fragment_color.g), 0.0, 1.0));
last_tev_out = vec4(color_output_0, alpha_output_0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_2 = ByteRound(clamp(fma((last_tev_out.rgb), (primary_fragment_color.rgb), (last_tev_out.aaa)), vec3(0), vec3(1)));
float alpha_output_2 = (last_tev_out.a);
last_tev_out = vec4(color_output_2, alpha_output_2);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
next_combiner_buffer.rgb = last_tev_out.rgb;
next_combiner_buffer.a = last_tev_out.a;
vec3 color_output_3 = ByteRound(clamp((secondary_fragment_color.rrr) * (const_color[3].rgb), vec3(0), vec3(1)));
float alpha_output_3 = (last_tev_out.a);
last_tev_out = vec4(color_output_3, alpha_output_3);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
combiner_buffer = next_combiner_buffer;
vec3 color_output_4 = ByteRound(clamp(min((const_color[4].rgb) + (primary_fragment_color.aaa), vec3(1)) * (vec3(1) - last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_4 = (last_tev_out.a);
last_tev_out = vec4(color_output_4, alpha_output_4);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
vec3 color_output_5 = ByteRound(clamp((combiner_buffer.rgb) * (last_tev_out.rgb), vec3(0), vec3(1)));
float alpha_output_5 = (const_color[5].a);
last_tev_out = vec4(color_output_5 * 2.0, alpha_output_5 * 1.0);
last_tev_out = clamp(last_tev_out, vec4(0), vec4(1));
gl_FragDepth = depth;
color = ByteRound(last_tev_out);
}
// reference: AB5DC5EFA21AFB24, 421CC489DA0D894C
// reference: 43A91D104F09AD7B, 7DCF9122F7A193A0
// reference: AB5DC5EF38C6CBD0, 3113F77E3F9B1FAD
// reference: D76EA6EB4F09AD7B, E9CE7AFC482AF804
// reference: AB5DC5EF05FEE3A1, 1A6183C6E984248F
// reference: 6F656A3BF67BF678, DBE3C296384EA0AC
// reference: AB5DC5EFBDD6B83E, AFA1F04CAC9C714F
// reference: 81F224826932CC66, 5AB7315286C48B40
// reference: AB5DC5EF29CB5688, B1F187F2D4B74547
// reference: 09CA62E66932CC66, 989317BD58EE15F3
// reference: D48482444F09AD7B, 40065C472AE4DC56
// reference: 81F224824F09AD7B, DBA7C834B3E957DA
// reference: AB5DC5EFCD2451C8, EA36FEB391FDE835
// reference: D474D7DF2EE2A5FE, 5ECFC586D92FE738
// reference: AB5DC5EF6C23805E, 090F6261D7EE040A
// reference: 3CFE33006613BD98, 548123F32C891796
// reference: AB5DC5EFEFD03AA3, DB65EA2FBAD032F1
// reference: FABDA349D8FBEEA9, 6696EBFC0AC0717D
// reference: 0A27F7E59334AA21, D511B25408440125
// reference: AB5DC5EF7ACFEFC9, 61A82C16044CBE52
// reference: E07833D8796B756A, 2086CB6D0D2EC954
// reference: 8614A1C90BB83FC7, 478298841EDB4A61
// reference: 768EF56540777B4F, 15B6C909B7264459
// reference: AB5DC5EF1934ECDF, 619E562CDB89F275
// reference: 8B2F65AB665B1202, 771030A3EE84D48A
// reference: 402EEF2251077863, CD0EB4DEA4AE12FC
// reference: 7A57A2396932CC66, 5AB7315286C48B40
// reference: AB5DC5EF5DDE01D5, 83C36A4A2949D5E1
// reference: AB5DC5EF95A06A41, A2B1C50622C4E336
// reference: 16FB4EDE24F04BD4, 13D5052BA6BA4F03
// reference: AB5DC5EF545AF4A5, 90082AE6B5CE434D
// reference: 0B5DF46EC5C9E33F, B4170363C2A69641
// reference: AB5DC5EFBC705A1F, D47C887971654D1D
// reference: 9454BAD4B82A5682, 2F81AEF57032A452
// reference: BB8B699951077863, 6A1B72A2FCCBE987
// reference: AB5DC5EF8C2B39B5, 05E1A4F0384EBF7F
// reference: 9B37B413C5C9E33F, B4170363C2A69641
// reference: F8B2A57451077863, F35472FEFCCBE987
// reference: AB5DC5EF1FD725CC, 2E393891BC8F259B
// reference: AB5DC5EF77087DEF, 82498C00693767D6
// reference: 91937D0187F65318, 93A93BC09551D122
// reference: 855FDF044F09AD7B, F390C0835F45F9F9
// reference: D02979C24F09AD7B, 4F16C56C361A572F
// reference: AB5DC5EF055A3A5C, F0F03E5C52F45639
// reference: AB5DC5EF2A69C311, DEF016CB05491BB4
// reference: AB5DC5EF37A1F7B3, 17FE472057E5AA57
// reference: AB5DC5EFDD8F2EB1, F015ECC7723E6782
// reference: A217CE0CFE1F7640, 0682315691DF22F8
// reference: 920903D9278B9E95, A8CF8742D3FFE418
// reference: 7A83E7066F7A86F3, 1F31D122238544D7
// reference: 5D50DDFE9B68893E, 89F4A144483FDCBF
// reference: ADCA8952D0A7CDB6, 4A53C178DA180688
// reference: 47954D6F3AF812FD, B213DBEA703F4DA9
// reference: 28172AC0482B5850, E6DEB3A176267503
// reference: D88D7E6C03E41CD8, 1B1F65E69DCC2CF2
// reference: B116306967632C43, C6C266BB71A4780A
// reference: D2296ED2B1436DE9, D7ABDD423A9FE0A5
// reference: 367E03B6C465348F, C14C2A01FF8980CD
// reference: 6CADA83243BD1C5E, 084C260CDA2D8394
// reference: 609232A8C5C9E33F, B4170363C2A69641
// reference: 70BB53814F09AD7B, 7178057DA30C6B80
// reference: 15359F794F09AD7B, F390C0835F45F9F9
// reference: AB5DC5EFCB634126, E7691288707F7809
// reference: 69AC8562278B9E95, 1F8B56B6A69D76F6
// reference: 026343A4278B9E95, CCDD8A714D8FD59E
// reference: 404339BF4F09AD7B, 4F16C56C361A572F
// reference: 3B941B7FFC018F45, 20F89F0FD505854F
// reference: 8510DD9F0EFFFEF2, 7741ED8274BA6CC9
// reference: E0D113FC4F09AD7B, 7178057DA30C6B80
// reference: 6D978DB1E38972AE, 7BA8F0614E2EFF77
// reference: 15359F796932CC66, D4762C3B0658B868
// reference: FDFDCDCCE38972AE, BEF3C067C9E0A693
// reference: 3898BFB4BAD529E7, F25393052E5C67E0
// reference: 119864FF6932CC66, 5AB7315286C48B40
// reference: A4B9371644B40F17, A34EF2EBF6540E32
// reference: 32DE8FD45022EF1A, 918C77499A4D914E
// reference: 21A63ED34EAEE7E6, B1AE1D2ED3065873
// reference: D13C6A7F0561A36E, 093F1E3AA6E724B3
// reference: 3B63AE42EF3E7C25, C852EFA5F1B65DC1
// reference: 54E1C9ED9DED3688, C8B980B12AB3A0E1
// reference: A47B9D41D6227200, 2BC9383774C63DD7
// reference: 155A664FF24DF04D, FFEE8AA3792C903B
// reference: 4A88E09B11A35A57, 230A49A38D8D21DC
// reference: CDE0D344B2A5429B, 8081C8610DB056FA
// reference: 7E95A089F24DF04D, CAB25DB4B91FEC6E
// reference: AEDF8DFF64850331, 856E75259E753956
// reference: EEFFE0F4F24DF04D, 5C9841FF918E1C70
// reference: 0675042BBABCE82B, 62DE8C3DC104C851
// reference: D84FD43B917261CF, 304A5CE25080BC6E
// reference: 4E286CF985E481C2, C1FE7459E199B532
// reference: 105B4B1F967B7286, 358240CD08F369E0
// reference: DDF4298C9FFBD61F, 7664CF1BF35D0097
// reference: AB5DC5EFFFDF9C27, 528A088158F377D7
// reference: E2D2803BA2D8DC2F, 6B63BE42E2F6C431
// reference: F4558953C5C9E33F, F935CDD1A6992EF1
// reference: D4E954D951077863, FC945F1AF9B5312E
// reference: BBE6BF044F09AD7B, 4F16C56C361A572F
// reference: EE9019C24F09AD7B, F390C0835F45F9F9
// reference: EE9019C26932CC66, D4762C3B0658B868
// reference: 86488F55A309530B, 363FE38B1D0D9C46
// reference: 855FDF046932CC66, D4762C3B0658B868
// reference: DE2F517ABBE2DA7D, CB4482862F23E816
// reference: BAB55E14BA335559, EC99DA070AAE5A79
// reference: 831CF95ACD05F9AE, 3CCB3012880A4C66
// reference: 7386ADF686CABD26, CDB452FE3708A1B9
// reference: 69433D67275A26E5, 4EFADB1ACF3A60B6
// reference: 93936C2E3FFB8819, E3F3A886765A887F
// reference: AAD6019A6D9304A3, 9D61FEF0BD63709C
// reference: 06C15AC855896C48, 60030B56D96F796A
// reference: 517387214BA865BE, AE43ACCC97EB4103
// reference: F65B0E641E4628C0, 996EFADAB2994BFA
// reference: 477AF56A3A29AA8D, C729D1E6EFA44000
// reference: 18A873BED9C70097, 17604D84BCC15904
// reference: 9FC040617AC1185B, 20922404C46D28CF
// reference: 2CB533AC3A29AA8D, 2E2BDC37BDE53717
// reference: 549DFF26BF9CC020, 5B26FA61DC10A8A8
// reference: B28407834BA865BE, BBDB9E15B4150F1D
// reference: FCFF1EDAACE159F1, BAF1C5B228F95206
// reference: B013910BE7534AE7, 03B41249843C92F4
// reference: 390E6387DA1FD026, EB6DE1D05D2F8ED0
// reference: BCDF73D13A29AA8D, 47D87D9FDC580E42
// reference: 5455970E72D8B2EB, 8230B968DACC8A3C
// reference: B013910B15AD3B50, 03B41249843C92F4
// reference: 6B671CA74ECBC74C, 40065C472AE4DC56
// reference: AAD6019A4BA865BE, AE43ACCC97EB4103
// reference: AAD6019A4ECBC74C, F390C0835F45F9F9
// reference: D2741352E24B1899, 7BA8F0614E2EFF77
// reference: B5C469067501D4E5, 188544D4C4C2AC0C
// reference: 455E3DAA3ECE906D, ACCD4E2B4D747FB2
// reference: 5F9BAD3B9F5E0BAE, 0BC6E5CF774C3EF6
// reference: 39F73F2AED8D4103, B40B1A0497443CCF
// reference: C96D6B86A642058B, 2C73831C512A6BC9
// reference: 9032CF87C8D7DB3A, 91C71CBBFBBA9014
// reference: 2E70E3E261C32DDC, FB992A7C3CB63639
// reference: A918D03DC2C53510, 1DD3AAD26EC56F4C
// reference: FBFD0941C8D7DB3A, 1EDC5A830F863610
// reference: 6BAB9AC40798ED6B, 4FED7CE8B7F510D1
// reference: 2BB724375E1F2846, 4E2CA3FE5E1866A5
// reference: FEC9CC1EDA1FD026, EB6DE1D05D2F8ED0
// reference: 6B97493CC8D7DB3A, 5B8E8CB2390BB6C9
// reference: 831DADE38026C35C, AD9D32803DD4CB16
// reference: 831CF95A543395C4, 45C78DF33D884A46
// reference: 7386ADF61FFCD14C, 733A91686C7182B4
// reference: 69433D67BE6C4A8F, AECADE9018F11BA1
// reference: 93936C2EA6CDE473, 196F65C6FDAD61F4
// reference: AAD6019A6C516E94, 9D61FEF0BD63709C
// reference: 06C15AC8CCBF0022, 8AF11215F3CACD04
// reference: 517387214A6A0F89, AE43ACCC97EB4103
// reference: F65B0E64877044AA, 5AB587C98BDFBBDF
// reference: 477AF56AA31FC6E7, EC97F6751B686B22
// reference: 18A873BE40F16CFD, 6BA6BFE2A4D83669
// reference: 9FC04061E3F77431, ACFB812410DF3A82
// reference: 2CB533ACA31FC6E7, 638CB04DEF54CD26
// reference: 549DFF2626AAAC4A, 58D30BCD74FF4DA6
// reference: B28407834A6A0F89, BBDB9E15B4150F1D
// reference: FCFF1EDA35D7359B, DD976AC845F86ECA
// reference: B013910BE69120D0, 03B41249843C92F4
// reference: 390E63874329BC4C, BD2701B976A9B3CA
// reference: BCDF73D1A31FC6E7, 26DE667CD9D94DFF
// reference: 5455970EEBEEDE81, EEAEEF847BDA184B
// reference: B013910B146F5167, 03B41249843C92F4
// reference: 6B671CA74F09AD7B, 40065C472AE4DC56
// reference: AAD6019A4A6A0F89, AE43ACCC97EB4103
// reference: AAD6019A4F09AD7B, F390C0835F45F9F9
// reference: D2741352E38972AE, 7BA8F0614E2EFF77
// reference: 5C9AB5333DF27C2D, 6C16192D37DDE8F9
// reference: AC00E19F763D38A5, F7F3D1391CCA85F2
// reference: B6C5710ED7ADA366, 07BC3C3DE1BCF81A
// reference: B5C46906CF0C0D9A, F8EBBF49424F6950
// reference: D0A9E31FA57EE9CB, 85FAF61880F90611
// reference: 2033B7B3EEB1AD43, FC72D604255CCBB3
// reference: 796C13B2C6D62A35, 5AE266D590887634
// reference: C72E3FD7B8BEDAB3, 7AF9009B5B7CCCDE
// reference: 40460C083C5E90B3, 8404D6B6BD509D70
// reference: 12A3D574C6D62A35, 5E323051F0B32632
// reference: 82F546F14F6B45A3, 7DF3D08A53F23329
// reference: C2E9F8027E35D7FA, 49DAF06BBC0888D0
// reference: FEC9CC1EBBA200EB, 3B5A3BE54903618C
// reference: 82C99509C6D62A35, D523B1595F7A43C2
// reference: 6A4371D663E0A8BB, 8CF1B785A1FB1E23
// reference: FFA0A75C4F09AD7B, 4F16C56C361A572F
// reference: AAD6019A6932CC66, D4762C3B0658B868
// reference: D94BC1454A6A0F89, 206CABF84BF3F7D6
// reference: AB5DC5EF3D234EA7, E4B1717CEF51482E
// reference: 348C6596C1DF9985, B1B791610D217D05
// reference: 7386ADF63CA412A3, 2DDBCD4EC3302F18
// reference: 06C15AC8EFE7C3CD, 81B4B56064F0494E
// reference: 477AF56A80470508, DFE481A2A5D547B7
// reference: 5455970EC8B61D6E, 05872491DEABFB07
// reference: BCDF73D180470508, AA86134C12919643
// reference: 8A6F471EE378948A, 6BCCD30BE25893A2
// reference: 1C08FFDCF7EE7487, 5B83F6948C4F1252
// reference: 9FC04061C0AFB7DE, B564B0B80C768F77
// reference: 6FCAE7214F09AD7B, 4F16C56C361A572F
// reference: 3ABC41E76932CC66, D4762C3B0658B868
// reference: 3ABC41E74F09AD7B, F390C0835F45F9F9
// reference: 421E532FE38972AE, BEF3C067C9E0A693
// reference: E2E4DC411E767D07, 46A3E37791B2B5FF
// reference: AC00E19F5565FB4A, 82CD91E5FEB0B622
// reference: D0A9E31F86262A24, DA752D74AA8E4C2A
// reference: 796C13B2E58EE9DA, EEBD0A76CDF74133
// reference: 6A4371D640B86B54, 864492417C9B95F1
// reference: 82C99509E58EE9DA, CBB80EF95790E2FF
// reference: B479A1C686B17858, 48B65508C7BB4D2E
// reference: 221E19047FE002BD, 8EB655DE9270E92E
// reference: 49D6F0B2E4354015, 380CB22EF5DCB418
// reference: A15C146DACC45873, 0EFDA5332B364838
// reference: 868F2E9558D657BE, 8083F4F55E4E1C7A
// reference: 9C4ABE04F946CC7D, 0E380EC384E3316F
// reference: F3C8D9AB8B9586D0, E51156508E8F2BCB
// reference: 03528D07C05AC258, 58F083A1507E0D30
// reference: C185E6F586F51F77, A1D47C6D50CD01C9
// reference: 6B0ACA3A725FBB8C, ADCE63B3904CE05A
// reference: B2737609E4354015, 75B2F1F3650A49D9
// reference: EDA1F0DD07DBEA0F, 339CE30E3C6F156E
// reference: D9BCB0CFE4354015, 94B3232E648BCFAB
// reference: A1947C4561802AB8, FADF5F1EDC9A44CC
// reference: 76157A3913191336, C2D140C3B6AD13C7
// reference: 669AEF4DE1E76281, 1C52DFF3C802031C
// reference: 6AC9C302A4DDF2C3, 25D440C063D25193
// reference: 09F69DB972FDB369, 7966BA6B82AA7A6E
// reference: CC07E0E404033ABE, 23FC06BB5547BA21
// reference: 5F328D1F4F09AD7B, 7178057DA30C6B80
// reference: 2DEA9D3A6AFE302A, E272017B162D53A5
// reference: C56079E5220F284C, BC8AF9CE25960F6F
// reference: E2B3431DD61D2781, 0A07275C2603B901
// reference: 97F4B423055EF6EF, E7F57906CBA5BA60
// reference: 676EE08F4E91B267, 4D3CBC260B9A902D
// reference: A5B98B7D083E6F48, FAD70185C76226C4
// reference: 899D9D5589109A30, 6E355ACEE1E8FEBD
// reference: BD80DD476AFE302A, 4ECD907B877A2816
// reference: 0EF5AE8A2A1682FC, 2B740797D5E0C78A
// reference: 122917B19DD26309, EFCD6855F34ACA80
// reference: F876D38C778DBC42, 1002D0E3753BB8D0
// reference: 02A682C56F2C12BE, DBF8847431D3E735
// reference: C5A811CDEF4B5A87, F38F6A8A17534F29
// reference: 6DCAF031FC36C356, 176F8B2D82CA2078
// reference: A83B8D6C8AC84A81, 0707194330CAF8B5
// reference: 2DEA9D3AF3C85C40, 149637F32845305A
// reference: C56079E5BB394426, 3ABC9AEFC8622443
// reference: E2B3431D4F2B4BEB, D09B6CA36D624C77
// reference: 97F4B4239C689A85, 7BCC7E384316547B
// reference: A5B98B7D91080322, 1CF273A0E866E793
// reference: 899D9D551026F65A, B8D501FAAE5A1BDB
// reference: BD80DD47F3C85C40, 5CA5404BAA25FCBC
// reference: 0EF5AE8AB320EE96, 032D8144B499502B
// reference: 122917B104E40F63, 0DC0397A86741CA2
// reference: F876D38CEEBBD028, 6A605BBC42AC52A3
// reference: 02A682C5F61A7ED4, C90A885CAC7E5F80
// reference: 676EE08FD7A7DE0D, 26B8C4D421ADF2B8
// reference: C5A811CD767D36ED, 4E9853FC1D8EDF7E
// reference: 6DCAF0316500AF3C, EF6A803A96113B0F
// reference: A83B8D6C13FE26EB, 14D0DE2A69F00C69
// reference: FAA2A7D701362DF7, 8C752F9065F81B43
// reference: 1228430849C73591, AA9FB349FB761294
// reference: D46BD341F72F66A0, A4FAF336B79A04EF
// reference: A8C2D1C1246CB7CE, 33514A55213666D1
// reference: 9A8FEE9F290C2E69, 43D24D6DE00195F0
// reference: BF450D09A822DB11, E28B81BC5C49BC05
// reference: 6AC8E7AA01362DF7, 20CABE90F4AF60F0
// reference: 24F187EDBCE02228, 71441D4873C08D21
// reference: CEAE43D056BFFD63, 8A3255CECD6B7FEB
// reference: 5858856D6FA3F346, 4E2EAEA44E10AE51
// reference: 382D3ED60B24C3DD, 7F8CBA1EE9526ABA
// reference: FA9E742FCE791BA6, F6E4EA0AEA744A5F
// reference: BA82CADC97FEDE8B, FAB93561073001C2
// reference: 6FFC22F513FE26EB, 14D0DE2A69F00C69
// reference: E896EC441F4B953B, 308CFEFF99927ACF
// reference: 001C089B57BA8D5D, 76BA3EE1066A3754
// reference: 3D0AA2F202381953, E895C073D6A1F89D
// reference: D75566CFE867C618, 71C23213692D1F87
// reference: 27CF3263A3A88290, 6CE8444273D08212
// reference: 4CE1EC2BFCA53F21, DA7DD7FAD6457136
// reference: 78FCAC391F4B953B, 9C336FFF08C5017C
// reference: 00D460B39AFEFF96, F949643F7204FBE4
// reference: C7DAF3BB1A99B7AF, CE9777947027A713
// reference: 5288C55D70EB53FE, 7B8566986E5321C8
// reference: A21291F13B241776, F176590126400FF2
// reference: CB89DFF45FA327ED, 08883FAC689573B4
// reference: A8B6814F89836647, E5A53BDCF8EACAB9
// reference: 90C29A1C4F09AD7B, 40065C472AE4DC56
// reference: 517387214F09AD7B, F390C0835F45F9F9
// reference: D64F1B816AFE302A, AFCC42A686FBAE64
// reference: 29D195E9E38972AE, 7BA8F0614E2EFF77
// reference: 040521E74F09AD7B, 4F16C56C361A572F
// reference: FB0D5CDA4F09AD7B, 40065C472AE4DC56
// reference: 3ABC41E74A6A0F89, AE43ACCC97EB4103
// reference: 2079D176E69120D0, 03B41249843C92F4
// reference: 2079D176146F5167, 03B41249843C92F4
// reference: 877B2157F7A08758, 5BF83C9D8ECC5920
// reference: 79A302AFD6AA63F6, 77F1F437895CF4C6
// reference: 9129E6709E5B7B90, AF957CD4F6E644F8
// reference: B6FADC886A49745D, 1568B4770B642950
// reference: 46608824218630D5, 5C3C63262995706B
// reference: D332BEC24BF4D484, E768A5DED2E2443C
// reference: C3BD2BB6B90AA533, 54AB2A40CB60072C
// reference: 33277F1AF2C5E1BB, 3E87BC4040FF630E
// reference: DDD402C03544C9EC, 2F24B48DCFF04E80
// reference: E9C942D2D6AA63F6, 32A32206BFD1741F
// reference: 91E18E58531F095B, 049CB0B95DDB8C65
// reference: 56EF1D50D3784162, 7D9E496FDCE397EB
// reference: AC3F4C19CBD9EF9E, 158D923174A94888
// reference: 5ABC311F9642D120, 266031E67BF550E5
// reference: 39836FA44062908A, 709424DFEBB8AF06
// reference: 82068414D6AA63F6, BDB8643E4BEDD21B
// reference: A4970BA44F09AD7B, 7178057DA30C6B80
// reference: 22EE47FE4A6A0F89, 206CABF84BF3F7D6
// reference: E896EC44867DF951, CA9B07F340DE7E4F
// reference: 001C089BCE8CE137, A2CB6DCAE82CC430
// reference: 27CF32633A9EEEFA, 524B951A4A96EE6F
// reference: 4CE1EC2B6593534B, 4BD990AFB58CD178
// reference: 78FCAC39867DF951, 82A8704BC2BEB2A9
// reference: 00D460B303C893FC, 545FABB0DEC58449
// reference: D94BC1454BA865BE, 206CABF84BF3F7D6
// reference: D75566CF7151AA72, 0989F9700FB5FB22
// reference: 3D0AA2F29B0E7539, 2FC9831FA22543CF
// reference: C7DAF3BB83AFDBC5, E88E34DB4A65BCEC
// reference: 5288C55DE9DD3F94, 1305C0E7AD44070C
// reference: A21291F1A2127B1C, 4AD3295792D27394
// reference: CB89DFF4C6954B87, A73569471F83475D
// reference: FB0D5CDA4ECBC74C, 40065C472AE4DC56
// reference: 3ABC41E74BA865BE, AE43ACCC97EB4103
// reference: 3ABC41E74ECBC74C, F390C0835F45F9F9
// reference: 421E532FE24B1899, BEF3C067C9E0A693
// reference: AFB496D4748388E6, B979639B4E5CBE41
// reference: D75432763C729080, 78B1FA7A1A353237
// reference: 3FDED6A9748388E6, 15C6F29BDF0BC5F2
// reference: 1117A23F829AC3B1, 906446495FDDEBAA
// reference: 7A397C77DD977E00, A99534D533F2A0F3
// reference: FD514FA87E9166CC, 3659F95569E669EC
// reference: 3FE20551BBCCBEB7, C5205C16D07FE0CA
// reference: E18DF693C9558739, 0AF8150D724C7A87
// reference: 0BD232AE230A5872, 202074F342820CC4
// reference: 6DBEA0BF51D912DF, 85CC0D0479A1DB91
// reference: 9D24F4131A165657, 55B840B50C94CAE4
// reference: 13336AFF1F4B953B, 7D32BD220944870E
// reference: 4BB617B0E69120D0, 03B41249843C92F4
// reference: 2DEA9D3A1F4B953B, CE7D591AA3355056
// reference: C56079E557BA8D5D, B02A2802634D81D5
// reference: F876D38C02381953, E7FAD86521F6EEA5
// reference: 122917B1E867C618, 19E034050E6A2BA9
// reference: 899D9D55FCA53F21, 662A5EAC86BB9B96
// reference: BD80DD471F4B953B, AFC0604FCAC6F1FE
// reference: E2B3431DA3A88290, 9637019C01AD2A67
// reference: 02A682C51A99B7AF, 96AFD1444CBCF159
// reference: 97F4B42370EB53FE, D5F2CDC066856118
// reference: 676EE08F3B241776, 549A8D71DAA01533
// reference: C5A811CD9AFEFF96, E5D706BE44AA6B66
// reference: D64F1B811F4B953B, FD81175823C4FC2F
// reference: 6DCAF03189836647, E2C07F52C06C1C37
// reference: 0EF5AE8A5FA327ED, A95AB0EC0B59B794
// reference: 2DEA9D3A867DF951, C56F74D0D26793FB
// reference: C56079E5CE8CE137, 95A02E3F02A42602
// reference: E2B3431D3A9EEEFA, FEB095FE63F3D6A0
// reference: F876D38C9B0E7539, F0919164CCB23D4F
// reference: 122917B17151AA72, 647E168B12CD5722
// reference: 899D9D556593534B, 36889CA514D1D0F5
// reference: 0EF5AE8AC6954B87, 21835577E328B088
// reference: BD80DD47867DF951, 4267FB1817ED056F
// reference: 02A682C583AFDBC5, C2F79FE44B64DA0C
// reference: 97F4B423E9DD3F94, CF0668AAFF71AA90
// reference: 6DCAF03110B50A2D, 951988FE690A0F2D
// reference: 676EE08FA2127B1C, 6EE7B3E41D236060
// reference: FFA0A75C4ECBC74C, 4F16C56C361A572F
// reference: FAA2A7D7748388E6, 0454952150222268
// reference: 122843083C729080, DA2B88E6B82CE183
// reference: D46BD341829AC3B1, F05D0C0F08BC6441
// reference: CEAE43D0230A5872, CA0429874869A9FB
// reference: 24F187EDC9558739, D9CCD7B3833D6DA2
// reference: A8C2D1C151D912DF, 8C883D39E271E590
// reference: BF450D09DD977E00, 1E0F2C800609E257
// reference: 6AC8E7AA748388E6, 65E9AC7439D183C0
// reference: 382D3ED67E9166CC, BDD817E634126F83
// reference: BA82CADCE24B7B9A, CEA70C7FFBAC405E
// reference: C5A811CD03C893FC, 35477ACB351E5232
// reference: 5858856D1A165657, 17CF9326F21B6D80
// reference: FA9E742FBBCCBEB7, 537E2B69841FCB2F
// reference: A8B6814F10B50A2D, 3392AC068D8B2E0B
// reference: 4BB617B015AD3B50, 03B41249843C92F4
// reference: 90C29A1C4ECBC74C, 40065C472AE4DC56
// reference: 517387214ECBC74C, F390C0835F45F9F9
// reference: 13336AFF867DF951, 15DD6DCDDD8CE00E
// reference: 29D195E9E24B1899, 7BA8F0614E2EFF77
// reference: C47B5012748388E6, 5878B1464FDD3833
// reference: 539E0BD78AC84A81, A4C9D64D0A2BB3FE
// reference: 2079D176E7534AE7, 03B41249843C92F4
// reference: 3851CD1113FE26EB, BBD2EECD76DCB652
// reference: 517387216932CC66, D4762C3B0658B868
// reference: AAD6019A68F0A651, D4762C3B0658B868
// reference: 79A302AFC1577FA3, 6C0A89FA92CE988B
// reference: 9129E67089A667C5, 7243AA812A3709DC
// reference: DDD402C022B9D5B9, 2F93B1A5914B0F85
// reference: E9C942D2C1577FA3, 13D87A405F0032B3
// reference: 46608824367B2C80, 74BE71040E787122
// reference: B6FADC887DB46808, 6886AE9E0F63B38F
// reference: AC3F4C19DC24F3CB, C7E759041668FE49
// reference: 56EF1D50C4855D37, 66451D2BCEAC217D
// reference: C3BD2BB6AEF7B966, 463157331DA8A875
// reference: 33277F1AE538FDEE, A8467B5A02608776
// reference: 5ABC311F81BFCD75, CCF8ED61EEFC5893
// reference: 39836FA4579F8CDF, 12317D031D7CB743
// reference: 22EE47FE4BA865BE, 206CABF84BF3F7D6
// reference: 5F328D1F4ECBC74C, 7178057DA30C6B80
// reference: 122917B1FF9ADA4D, 81D8F50D640C88BA
// reference: E2B3431DB4559EC5, 9AF622B3D42C08AB
// reference: F876D38C15C50506, 142777278370379C
// reference: 676EE08F2CD90B23, 6CD7DC86DA19A840
// reference: 899D9D55EB582374, 5AC6A2C976FC455F
// reference: C5A811CD8D03E3C3, 96CB44EF746D174B
// reference: 6DCAF0319E7E7A12, 408DAD05A6AE634E
// reference: 97F4B42367164FAB, 94B43838F0562E29
// reference: AB5DC5EF089DA87A, 756BED717C247FA1
// reference: AB5DC5EF11B22273, 31884F9ACC9BB5B8
// reference: 886655BEB51D6DD3, 335E815344D524F2
// reference: C5B43CDA4ECBC74C, DBA7C834B3E957DA
// reference: A4970BA44ECBC74C, 7178057DA30C6B80
// reference: 6B0ACA3A739DD1BB, ADCE63B3904CE05A
// reference: 90AF4C81739DD1BB, C940ADE43C90D307
// reference: 3E11BA614ECBC74C, DBA7C834B3E957DA
// reference: 4BB617B0E7534AE7, 03B41249843C92F4
// reference: E2B3431D970D5D2A, 3E5DF7E9441FD60A
// reference: F876D38C369DC6E9, 297520D746B4A868
// reference: 122917B1DCC219A2, 9593C6CC3E5DA92B
// reference: 97F4B423444E8C44, 87BA565CF2549F21
// reference: C5B43CDA68F0A651, 5AB7315286C48B40
// reference: C5A811CDAE5B202C, D681FB06D083631F
// reference: 3E11BA6168F0A651, 5AB7315286C48B40
// reference: 676EE08F0F81C8CC, 4541E50EEF681897
// reference: 899D9D55C800E09B, 4F1EB6D2BF48848D
// reference: 6DCAF031BD26B9FD, 20FBCF3960F5BFFB
// reference: 24D42AF0C40B8908, B4170363C2A69641
// reference: 1CA1EE459645AE3C, D3A75FBD4FA22584
// reference: DF71AC4BC40B8908, B4170363C2A69641
// reference: FFCD71C150C51254, CD0EB4DEA4AE12FC
// reference: 0468F77A50C51254, 6A1B72A2FCCBE987
// reference: DDD402C0AC72A586, 6FD3A46AE9973F33
// reference: 46608824B8B05CBF, B178A293AC5C0318
// reference: 56EF1D504A4E2D08, 7B3D23038FC55AD4
// reference: B6FADC88F37F1837, 7DD56F6A7907270D
// reference: 39836FA4D954FCE0, B3D219A4148246E8
// reference: AB5DC5EFD60FAC79, E80FD09F14049207
// reference: A5B98B7D7D8BCA59, BE947A91108276C3
// reference: 517387216C516E94, 9D61FEF0BD63709C
// reference: 60C5FA037D8BCA59, 02AFFD614C4DF8E5
// reference: 60C5FA03E4BDA633, C37CFFD1CA301C7D
// reference: 4BB617B0146F5167, 03B41249843C92F4
// reference: 539E0BD713FE26EB, 5D5C587782042F20
// reference: 517387216D9304A3, 9D61FEF0BD63709C
// reference: D64F1B81F3C85C40, CBD05DCDB517AE1B
// reference: 3ABC41E76D9304A3, 9D61FEF0BD63709C
// reference: FF96628813FE26EB, BBD2EECD76DCB652
// reference: 3851CD118AC84A81, 6D1921E9DFC3BB48
// reference: 3ABC41E76C516E94, 9D61FEF0BD63709C
// reference: 5FF39FE15CB98B78, D31F462801822E2B
// reference: AB5DC5EFAF79B0FF, AE17434EB03F29DD
// reference: BA68F27AF96412BC, C05114BB49500BC1
// reference: EE216DEF45304160, 1CB643C565E59B0D
// reference: F1F014E8B46A3C94, 2F2E6AB88BB97F23
// reference: ABA385E2B36AD8FF, C65518668DF870D3
// reference: 7CEAEBB60CB793BA, 9590F2693805314C
// reference: 2E7E26702BE64D19, 01C7C9F05ACC52F2
// reference: 6FC589D244468BDC, B742B3A7FC4B467A
// reference: 30170F06A7A821C6, 8B88117287F768A7
// reference: 040A4F1444468BDC, 5E40BE76AE0A316D
// reference: 5B39D14EF8A59C77, 4E360EC9DA99F4D9
// reference: 41FC41DF593507B4, 637CC226B31D0F82
// reference: BB2C10964194A948, C41D0F1C4B01C5F5
// reference: DEE472DC60290991, AB42AAF3228AA53E
// reference: 7C22839EC1F3E171, F34E10C3284B598C
// reference: D4406262D28E78A0, 5823B2EB89C78DDC
// reference: ABA385E290321B10, CFB891FCA13B1FFD
// reference: 7CEAEBB62FEF5055, 5E2DD176DEB910ED
// reference: 2E7E267008BE8EF6, 963D21C841ADF433
// reference: 6FC589D2671E4833, BDAAF968EF1C596D
// reference: 5975BD1D0421D9B1, B39989DA976A047B
// reference: 46608824543395C4, 23979FDE0F062640
// reference: 9129E670EBEEDE81, F8A6A9F0205E1CDB
// reference: 33277F1A877044AA, 5DC7CE8A5C135EF3
// reference: 82068414A31FC6E7, 60EA8DD3B6BE1CAB
// reference: 46608824776B562B, 4EE3168B12D38B36
// reference: C3BD2BB6EFE7C3CD, DE4294CDD4BBF522
// reference: 8206841480470508, 59C6A31D83F60C84
// reference: B4B6B0DBE378948A, D5A7E4BC0EFB30EB
// reference: 79A302AFA31FC6E7, EF2B5A5F794C295D
// reference: 9129E670C8B61D6E, 413FBD0E0AC02C6D
// reference: 79A302AF80470508, 7CC3A7921991AF48
// reference: 33277F1AA4288745, A381EAFFDDDEA8A1
// reference: 42572C3349A6F3C5, 29535BDB1427D02E
// reference: AADDC8EC0157EBA3, F1DFE006DD258F65
// reference: 3967859735B4F0CF, 98B99EDA3AC776B1
// reference: 7D94A6B8BE8AA0E6, 9B31DF4E7C757612
// reference: 97CB628554D57FAD, 5F833221AEE441E6
// reference: F849052A26063500, 9DF1844F38382C3E
// reference: 08D351866DC97188, C51586D9519191BA
// reference: FFCD71C151077863, CD0EB4DEA4AE12FC
// reference: C5B43CDA6932CC66, 5AB7315286C48B40
// reference: E6202C5CAA4859DF, FD5277BE4F0218B1
// reference: AA15A0C4CC139968, 6336FCCFBCA46F36
// reference: DF71AC4BC5C9E33F, B4170363C2A69641
// reference: AB5DC5EFE217A885, A850295C2F806452
// reference: 8D0EF214F545E46E, 68379D7C8FB23E36
// reference: 3E11BA616932CC66, 5AB7315286C48B40
// reference: B9F2AA8849A6F3C5, DD00AE9C683C8CF4
// reference: 4BB617B037379288, F935CDD1A6992EF1
// reference: 122917B145F475C8, F814E23FA052DC08
// reference: E2B3431D0E3B3140, EF8D9B9E29D5DF68
// reference: F876D38CAFABAA83, ACD5947A1CB25EC5
// reference: 3E11BA614F09AD7B, DBA7C834B3E957DA
// reference: 676EE08F96B7A4A6, B4AEE3D1952324F0
// reference: 899D9D5551368CF1, 750C09A44B34AB21
// reference: C5A811CD376D4C46, D8E9E79C78EFB0BA
// reference: 6DCAF0312410D597, A4B61A91080C2C1E
// reference: 2DEA9D3AB2D826EB, AA09CD166B10E237
// reference: D64F1B81B2D826EB, D61291CC9F431770
// reference: 0107216CE7035333, A4F2A6A29454F3C2
// reference: 24F187ED5AD55CEC, 28EEF914E13040DF
// reference: D46BD341111A1864, C9AF16FEFC3AD4CE
// reference: CEAE43D0B08A83A7, 7D57B54F81A0AFFD
// reference: 5858856D89968D82, 428E0A43CB4FFD75
// reference: FA9E742F284C6562, 1C7C13022829074A
// reference: BA82CADC71CBA04F, 084C260CDA2D8394
// reference: CAD755CAD59C70D0, 154DF525B4BBBAD5
// reference: 225DB1159D6D68B6, 29EEAD1A0DF0AC9F
// reference: 148475F0684A7F0F, BC6B3727E713A158
// reference: 682D7770BB09AE61, 1BD4C152E73EBD38
// reference: 98B723DCF0C6EAE9, A59DF2A44E9856BB
// reference: 5999A361E3742F26, 20C2766149E86370
// reference: FEDBB1CD8215A044, FBD507B70AC4D902
// reference: E41E215C23853B87, FE8D017C697AE2E6
// reference: CAEB86321AD34681, 468B0250A1E3CCEC
// reference: BF450D094E17A5D5, F1DF1404B39DB8F0
// reference: 12284308AFF24B55, 809FD9DC1DC4E80E
// reference: FAA2A7D7E7035333, D8E9FA7860070685
// reference: A8C2D1C1C259C90A, C3BE0E8211B57787
// reference: 24D42AF0C5C9E33F, B4170363C2A69641
// reference: 0468F77A51077863, 6A1B72A2FCCBE987
// reference: 2DEA9D3A278B9E95, A8CF8742D3FFE418
// reference: C56079E56F7A86F3, 1F31D122238544D7
// reference: E2B3431D9B68893E, 89F4A144483FDCBF
// reference: 122917B1D0A7CDB6, 4A53C178DA180688
// reference: F876D38C3AF812FD, B213DBEA703F4DA9
// reference: 97F4B423482B5850, E6DEB3A176267503
// reference: 899D9D55C465348F, C14C2A01FF8980CD
// reference: C5A811CDA23EF438, F1316143BEE6133E
// reference: 676EE08F03E41CD8, 1B1F65E69DCC2CF2
// reference: 6DCAF031B1436DE9, D7ABDD423A9FE0A5
// reference: 08D35186F89AC9F6, B742B64B8C53AEB0
// reference: 39678597A0E748B1, C3C007EE6FD8652F
// reference: 7D94A6B82BD91898, D79649B76453A743
// reference: 97CB6285C186C7D3, 06616C9A824872BA
// reference: 8D0EF21460165C10, F948BB19702BB629
// reference: F849052AB3558D7E, 7DA4BCB1A77FB6DC
// reference: AA15A0C459402116, F0279C0DF80A4653
// reference: D64F1B81278B9E95, 1F8B56B6A69D76F6
// reference: BCDF73D1EE6A6858, A3F529DB52C59A0E
// reference: 5455970EA69B703E, D6BBCC3A8262DBC8
// reference: 831CF95A19463B7B, AFC86AA02A918360
// reference: 18A873BE0D84C242, 1A5525F30AD021DD
// reference: 2CB533ACEE6A6858, C4DAB03A05DD8D09
// reference: 7386ADF652897FF3, 0520A7DC982FAF89
// reference: 69433D67F319E430, EA7B1232A923D447
// reference: F65B0E64CA05EA15, 94E2DFF8299B31E1
// reference: 549DFF266BDF02F5, FCD8885AA985C9BF
// reference: FCFF1EDA78A29B24, DFA17D482E0EE59D
// reference: ABA385E20561A36E, 3A434AA5FE25A8E8
// reference: 7CEAEBB6BABCE82B, 79B777ACC885CB3E
// reference: 5B39D14E4EAEE7E6, 1495438F6FF2E6D9
// reference: DEE472DCD6227200, 96FCC944DA7E7893
// reference: 6FC589D2F24DF04D, 04418BF3256A57E0
// reference: 41FC41DFEF3E7C25, A59040C48B828489
// reference: 30170F0611A35A57, 9FAD25E4FED9BF8F
// reference: 7C22839E77F89AE0, 28F58812BB553227
// reference: D440626264850331, CA2CCE4B20904568
// reference: 94600F69F24DF04D, 21448F7CBF0DF42C
// reference: 06C15AC881CAAE9D, 0EC61424FADEBEFA
// reference: 477AF56AEE6A6858, 3D4478AC2C6636D3
// reference: 877B2157BAD529E7, F25393052E5C67E0
// reference: 164ECFBC7334DF2A, 57EE5E6FFCBD85AD
// reference: 2B5D1C917DF04ACE, 931285EA604BD7D2
// reference: BA68F27AB411BC03, CFC38AF4F54CEC7F
// reference: BD80DD47278B9E95, CCDD8A714D8FD59E
// reference: 42572C33DCF54BBB, 8E92B73E90BC849C
// reference: AADDC8EC940453DD, 332875D8C7390273
// reference: 24F187ED68A3E0FD, 28EEF914E13040DF
// reference: BF450D097C6119C4, F1DF1404B39DB8F0
// reference: D46BD341236CA475, C9AF16FEFC3AD4CE
// reference: CEAE43D082FC3FB6, 7D57B54F81A0AFFD
// reference: 5858856DBBE03193, 428E0A43CB4FFD75
// reference: BA82CADC43BD1C5E, 084C260CDA2D8394
// reference: 951F16DE2E0B3A0C, 38C01D22E4BDAB00
// reference: 7D95F20166FA226A, C179CB94438DB62D
// reference: 4B4C36E493DD35D3, 943CD26FF795D9AE
// reference: BBD66248D812715B, 7907E4A3178DE13B
// reference: A113F2D97982EA98, 08DC1E20BC79355C
// reference: C77F60C80B51A035, 9C7B88003532B9B6
// reference: 37E53464409EE4BD, 83A50C25C992E795
// reference: 9523C526E1440C5D, C52FC3BB23F6A8FD
// reference: 0651E07518E365FA, 8CB4F3D4B925F978
// reference: 1D9F6F2780470508, B2AD4961E977EAF1
// reference: F5158BF8C8B61D6E, DA48A50CF1E97BE6
// reference: 225CE5AC776B562B, E45CA51B5904E8BE
// reference: 571B1292A4288745, 4B77215561510279
// reference: B9E86F4863A9AF12, 29D346F38E01A349
// reference: C80321919D348960, AEED1D8629374311
// reference: D2C6B1003CA412A3, D56071BEC34529F6
// reference: A781463EEFE7C3CD, 9951E8E82C8CAC7B
// reference: F5DDE3D005F26FA5, D7302F7E37411493
// reference: E63AE99C80470508, 11DB823D01D566BA
// reference: 122917B1CB3F05F7, 024AEAB5C6855642
// reference: E2B3431D80F0417F, 9430D917EC40878C
// reference: F876D38C2160DABC, 2CB04DE5D9783F12
// reference: 97F4B42353B39011, 1DFEA5C823778DB7
// reference: 676EE08F187CD499, 9B547CA640AF8B12
// reference: 899D9D55DFFDFCCE, BA551FE21EBA7A41
// reference: 6DCAF031AADBA5A8, 4C9E726EF236C3B8
// reference: 2DEA9D3A3C1356D4, C5F3811EEAF6890B
// reference: D64F1B813C1356D4, 80725873B5F358E1
// reference: C5A811CDB9A63C79, 456E36DAC51B5749
// reference: C56079E574E24EB2, D1DF920DA88CC46A
// reference: 3EB42C7F649983A5, 7E48C3120BCCBD19
// reference: AB5DC5EFE4F46196, 32E7001A586B3B1D
// reference: 79A302AF7B39D026, FACB327D23073768
// reference: 9129E67033C8C840, F223F351E532AE2B
// reference: AC3F4C19664A5C4E, F32082337A14C8D0
// reference: 466088248C158305, 43A593E04DCE465C
// reference: B6FADC88C7DAC78D, C036C7E40938EF30
// reference: DDD402C098D77A3C, C19F695D82A9C593
// reference: C3BD2BB6149916E3, AF3AED0565E94454
// reference: 33277F1A5F56526B, 1102F1F824CB480A
// reference: 91E18E58FE8CBA8B, C2D4BB4EEB2ADDA7
// reference: 820684147B39D026, DEC7917316B4C52E
// reference: 39836FA4EDF1235A, 8B1BF48B59DEFA88
// reference: EE216DEFE8A3F2B0, 2A3940C338157CDF
// reference: AB5DC5EFB4F694B3, 7642D41FDAB29524
// reference: 6708A71BA64FBD38, F11B937C0C4A661B
// reference: 3341388EE1653BCA, 680AD87DDC7E44E1
// reference: B4BE6A8DC5C9E33F, B4170363C2A69641
// reference: 9402B70751077863, 1A07AFF423D75A41
// reference: E9C942D27B39D026, FBCE9E78C807B463
// reference: BD80DD473C1356D4, 09F666AF13FD31A7
// reference: C5B43CDA4F09AD7B, DBA7C834B3E957DA
// reference: 66AFC683FC550602, 322AAC6867732982
// reference: 511C7E179C609D1D, 001CB85ED1D72B72
// reference: B9969AC8D491857B, ACABC9428B29917B
// reference: 848030A181131175, 2C4255E482B7EA6E
// reference: 6EDFF49C6B4CCE3E, FAB37C66F14E9845
// reference: 9E45A03020838AB6, A10D8869A98622A7
// reference: 1B9803A2B80F1F50, 63ECF0BCAE76FAFE
// reference: F56B7E787F8E3707, 8040A15D3D695FB7
// reference: B95EF2E019D5F7B0, F37806C91BC80178
// reference: 2A2CD7B3E0729E17, C1DF8B201BCF7ACB
// reference: EB02570EF3C05BD8, 2BB7818AF0FD3F8C
// reference: AAB9F8AC9C609D1D, 7C07E4842584DE35
// reference: 1BFE4436063C76F1, 3FE17C5D2ECA15EE
// reference: 865444FAC9BBE8C5, 2D41AFC161647E3F
// reference: 6EDEA025814AF0A3, 4BEA5E101089BDF3
// reference: 580764C0746DE71A, 8F1AF2680453B77B
// reference: A89D306C3FA2A392, CA79CC22AC305429
// reference: B258A0FD9E323851, 89077029751166C5
// reference: 24AE6640A72E3674, B4688894E0FAC49F
// reference: D43432ECECE172FC, 9A6879E511722ED7
// reference: 8668970206F4DE94, E4B179E03F57C2BB
// reference: 7DF1C241C9BBE8C5, 515AF31B95378B78
// reference: 151AB251FF53B733, 7D33C90A66DA7F14
// reference: 6F0A98CF09332563, 93A6CB48F0DFD045
// reference: 87807C1041C23D05, F3492EF3764FEACB
// reference: FC446E643FDB7A95, 8290CCEFF4ABA1AF
// reference: B159B8F5B4E52ABC, 017FCCDFB8716E75
// reference: 41C3EC59FF2A6E34, EB52FDC99E70BB3A
// reference: 5B067CC85EBAF5F7, 8C5FD235F0FDF93E
// reference: CDF0BA7567A6FBD2, 794DA3A3260DB2E4
// reference: 3D6AEED92C69BF5A, C6F3DD28FA112E9F
// reference: 6F364B37C67C1332, C273F749D6013A6B
// reference: 580764C0461B5B0B, 8F1AF2680453B77B
// reference: B258A0FDAC448440, 89077029751166C5
// reference: D43432ECDE97CEED, 9A6879E511722ED7
// reference: 24AE664095588A65, B4688894E0FAC49F
// reference: 8668970234826285, E4B179E03F57C2BB
// reference: 94AF1E7409332563, ED0567956E6E8132
// reference: 238989FF1514BD76, 02F32E03CFE4725A
// reference: CB036D205DE5A510, C5DFD8D3F4F2331F
// reference: 8173AB457B8163C7, 37E9ECD9B68CE134
// reference: B0C77F5423FCE280, 89A448C2DB45CF32
// reference: FDDAA9C5A8C2B2A9, 49195A0C1A14BD9A
// reference: 17856DF8429D6DE2, 2ECC77E633AAD41E
// reference: 0D40FD69E30DF621, 40C9669508F55F47
// reference: 71E9FFE9304E274F, CE02743DEC978C46
// reference: 23B55A07DA5B8B27, E895E1170A34F77D
// reference: D82C0F441514BD76, 27F62A8C5583D196
// reference: 3172D371D59C70D0, B63B3E795C19369E
// reference: 5999A361118A5E91, 20C2766149E86370
// reference: 148475F09AB40EB8, BC6B3727E713A158
// reference: FEDBB1CD70EBD1F3, FBD507B70AC4D902
// reference: E41E215CD17B4A30, FE8D017C697AE2E6
// reference: 98B723DC02389B5E, A59DF2A44E9856BB
// reference: 682D777049F7DFD6, 1BD4C152E73EBD38
// reference: CAEB8632E82D3736, 468B0250A1E3CCEC
// reference: 5999A361D1029337, 20C2766149E86370
// reference: 148475F05A3CC31E, BC6B3727E713A158
// reference: FEDBB1CDB0631C55, FBD507B70AC4D902
// reference: E41E215C11F38796, FE8D017C697AE2E6
// reference: 98B723DCC2B056F8, A59DF2A44E9856BB
// reference: 682D7770897F1270, 1BD4C152E73EBD38
// reference: CAEB863228A5FA90, 468B0250A1E3CCEC
// reference: D8897528814AF0A3, 03AB830E067B163A
// reference: 92F9B34DA72E3674, A78E93E8239708CB
// reference: CBA6174CC9BBE8C5, CB5E439281C5C7BC
// reference: A34D675CFF53B733, 3C3869862C2E22C4
// reference: EE50B1CD746DE71A, 3E8CA7CCD022A5AF
// reference: 040F75F09E323851, 0B82D5C9DA19BE96
// reference: 6263E7E1ECE172FC, 51F5ED2F405DA13C
// reference: 303F420F06F4DE94, AD84118C72ED5E6B
// reference: D08ADD53E378948A, 6D6618E879297E31
// reference: 82C99509EE6A6858, 4D019CDC7C97A812
// reference: 5C9AB53353BC6787, 8851387C00535DF3
// reference: B6C5710EB9E3B8CC, 9A580F61A565C482
// reference: 2033B7B380FFB6E9, 18167A8F60EB97B8
// reference: D0A9E31FCB30F261, 7D7404D4681D7A43
// reference: C72E3FD7477E9EBE, A1C5D0AE56010655
// reference: 82F546F121255E09, 496B286FC08240B7
// reference: C2E9F80278A29B24, 0A67ABED75033327
// reference: 666E2321BC004B90, D828A5822DD9B34E
// reference: 1CA1EE459787C40B, D3A75FBD4FA22584
// reference: 6A4371D6A69B703E, 6EF564A951F5C91A
// reference: B013910B37379288, F935CDD1A6992EF1
// reference: 796C13B2EE6A6858, D7663F105992AC9D
// reference: 13FC7BE2278B9E95, CFEBBE0C520880BB
// reference: FB769F3D6F7A86F3, D75EEFD3473DC091
// reference: CDAF5BD89A5D914A, 75C07F8885F3BA19
// reference: 27F09FE570024E01, 4B4CCF8C7068368F
// reference: B1065958491E4024, 54FB367DDAA96CDC
// reference: 419C0DF402D104AC, 25D89B17AFEAC1E2
// reference: 561BD13C8E9F6873, FA42D8B00C8D6149
// reference: 13C0A81AE8C4A8C4, 370C64AC7DC58D93
// reference: 53DC16E9B1436DE9, 051130508E7E4420
// reference: AC00E19F1873230F, 3AA3B1383D59987F
// reference: 34CCFB48665B1202, 771030A3EE84D48A
// reference: E859FD59278B9E95, B14812D1CCB9D1CC
// reference: 13FC7BE2D711CA39, 9D3C9A74276E0E8E
// reference: FB769F3D722748B7, 870E0276D5876E0A
// reference: CDAF5BD82C359C21, 28FABD6F85F3BA19
// reference: 27F09FE5C66A436A, 4B4CCF8CC16FFA3C
// reference: B1065958FF764D4F, 54FB367D6BAEA06F
// reference: 419C0DF4B4B909C7, 25D89B17368C94B0
// reference: 561BD13CA9793ABF, 4B4514030C8D6149
// reference: 13C0A81A5EACA5AF, 860BA81F7DC58D93
// reference: 53DC16E96FF237F6, 3D4AB8A0B43C66F7
// reference: A8C2D1C1F02F751B, C3BE0E8211B57787
// reference: 39F73F2A39CE83D6, 478298841EDB4A61
// reference: 7C41CAEBDCF54BBB, DB57D23A7E8575A3
// reference: A212EAD161234464, 2B1A35507E2E8C9C
// reference: DEBBE851B260950A, 49333D4304DB6E4B
// reference: 2E21BCFDF9AFD182, 8BF9C4A1E3B6C57E
// reference: 7C7D191313BA7DEA, 114E27096CF825E1
// reference: 5288BE7D2AEC00EC, 962EFE6335C46664
// reference: 484D2EEC8B7C9B2F, 91EAA257DB033B4E
// reference: EF0F3C40EA1D144D, 52B6BB1A7D1CB5D3
// reference: 94CB2E34940453DD, 073DC5398D2F8209
// reference: FA9E742F1A3AD973, 1C7C13022829074A
// reference: 97F4B423DD78E02E, 1D94191E4341D0F2
// reference: 42572C336AFE302A, 8710D1DCC507BC75
// reference: AADDC8EC220F284C, 1699353C9B03B4A8
// reference: 3967859716EC3320, E3F0A9539D8C7C4F
// reference: 7D94A6B89DD26309, 834317A99C6DA784
// reference: 8D0EF214D61D2781, AFFC956955E56F89
// reference: 97CB6285778DBC42, 548717A829039DB6
// reference: 08D351864E91B267, 8BD601A5A8704438
// reference: AA15A0C4EF4B5A87, 8A5080EA98922B67
// reference: F849052A055EF6EF, D50C55D4F07450BB
// reference: D23D6C4E6AFE302A, F8C2226608C9164D
// reference: CF58CD624F09AD7B, 7178057DA30C6B80
// reference: 7FFEBBA2E24B7B9A, E524B97BF75C82AB
// reference: CFF4D7A2586113C9, 292F2CC68E8E5747
// reference: 277E337D10900BAF, D7EA1E3C15B273E2
// reference: F0375D29AF4D40EA, BA8F8FE399851AC9
// reference: 00AD0985E4820462, 35BB737C3ABE29A9
// reference: 1A68991445129FA1, 92FE783EFAFE8EAC
// reference: 75EAFEBB37C1D50C, 3915A77639209D51
// reference: 6B83D7CDBB8FB9D3, 555682888EB1EAE2
// reference: ECEBE4121889A11F, DF345E9AD55C45CD
// reference: 5F9E97DF586113C9, 6C7DFAF7B803D79E
// reference: E0B8C85D5DB3315D, 6C8E8ADFEC70D581
// reference: 8570AA177C0E9184, 8F45903288B91F9E
// reference: 27B65B55DDD47964, 15DDDC59351D711D
// reference: 8FD4BAA9CEA9E0B5, 5A3E668AC796F6C6
// reference: 79A302AF4F9C0F9C, A7255194768E81F9
// reference: 9129E670076D17FA, A30328DE8C3E6F36
// reference: AC3F4C1952EF83F4, C5EC31AEC4FAC163
// reference: C3BD2BB6203CC959, 21FD17261E430A75
// reference: 33277F1A6BF38DD1, 8E0C0C525145D199
// reference: 5ABC311F0F74BD4A, 4484A1D5E64E0DE7
// reference: E9C942D24F9C0F9C, CED6F03C1733B8AC
// reference: 91E18E58CA296531, BABD66045B0A5603
// reference: 820684144F9C0F9C, 27D4FDED4572CFBB
// reference: 554EBEF9BD627E2B, D060D6AA74463537
// reference: 4661DC9DF593664D, 4C9555D8DD69C654
// reference: AEEB3842BD627E2B, 1A2946A3B6F713EA
// reference: 80224CD44B7B357C, BF815A1972014F87
// reference: FC8B4E549838E412, CB5AAE515ECAE8A6
// reference: 0C111AF8D3F7A09A, E6B86413F3C2E61E
// reference: EB0C929C147688CD, BD674623A62D59C7
// reference: 3E81783FBD627E2B, 5F7B9092807A9333
// reference: AED7EBBA722D487A, CF65C257682D6468
// reference: 70B8187800B471F4, 6F64A103CAC41B04
// reference: 9AE7DC45EAEBAEBF, 149A9327C60CA02C
// reference: 6C64A143B7709001, ACC200A0F35FCB81
// reference: 07D79442369C195D, 67008C4C43447CEB
// reference: 6C185284369C195D, 2C86B6173DA65F61
// reference: CFF4D7A2C1577FA3, D9B8235EF7485317
// reference: 277E337D89A667C5, 6BB432E9EB04BE8F
// reference: 00AD09857DB46808, 61A2479486114892
// reference: 1A689914DC24F3CB, 6290CAF82BB234D8
// reference: 6B83D7CD22B9D5B9, 1777D1866E4F85CC
// reference: 5F9E97DFC1577FA3, B04B82F696F56A42
// reference: 27B65B5544E2150E, 3B40ADBBD52E0553
// reference: F0375D29367B2C80, 02E99D2C9D191AF1
// reference: E0B8C85DC4855D37, EE74B5736FF89CAA
// reference: 75EAFEBBAEF7B966, 3533C9157D8BB9D0
// reference: ECEBE41281BFCD75, 5ECAD5405E3B1FF2
// reference: 8570AA17E538FDEE, 6F6DBFBB101B0ADB
// reference: 88D6AD3233A90E14, 888E790014790C38
// reference: F03609907B581672, 05D248F5E6D67189
// reference: 18BCED4F33A90E14, CDDCAF3122F48CE1
// reference: C6EFCD758E7F01CB, E3FC0AEA5FCE24AE
// reference: 367599D9C5B04543, 0735149896C68B68
// reference: 2CB009486420DE80, 8EE6EEA02CCA1EC7
// reference: 4ADC9B5916F3942D, FCBC42D4C87DB5CA
// reference: BA46CFF55D3CD0A5, AAACB3D564843C09
// reference: 5D5B47919ABDF8F2, 600B57AEA03959ED
// reference: DA33744E39BBE03E, 094636D9CFE7B325
// reference: 18803EB7FCE63845, 374FCC8C94F3E57E
// reference: 34515119586113C9, E366BCCF4C3F719A
// reference: 79A302AF586113C9, 1FB9B6F635F91859
// reference: 9129E67010900BAF, 820FD0D20874ECC2
// reference: AC3F4C1945129FA1, 29708F9D58BF3843
// reference: 46608824AF4D40EA, 5B122C59A056D78B
// reference: B6FADC88E4820462, 01D660806356BD82
// reference: DDD402C0BB8FB9D3, 3A30DA4208A60C25
// reference: E9C942D2586113C9, C51CEF1F7924659F
// reference: 56EF1D505DB3315D, F54EE557FF46A1F3
// reference: C3BD2BB637C1D50C, C90E614B810E1C6B
// reference: 33277F1A7C0E9184, E7190379370572C5
// reference: 91E18E58DDD47964, BD19FA47DEF9CFBE
// reference: 39836FA4CEA9E0B5, FBF7197EACA2AE44
// reference: 91E18E5844E2150E, FFC0E7C325142D8D
// reference: BCDF73D1D6AA63F6, 72226B8157CE6E90
// reference: 5455970E9E5B7B90, 757296D64688F093
// reference: 831CF95A218630D5, 48A0676D0777AF18
// reference: 7386ADF66A49745D, 9B543CECF70FDC63
// reference: 69433D67CBD9EF9E, 257C6F482E85BEE0
// reference: 06C15AC8B90AA533, 2BB91EBDC458987E
// reference: 18A873BE3544C9EC, 84B00EAE0A60C158
// reference: 549DFF26531F095B, 43D33802BDDDF071
// reference: 93936C2ED3784162, CDB7ECA28F51733F
// reference: F65B0E64F2C5E1BB, 138A49A848FF1654
// reference: FCFF1EDA4062908A, 079C686D6B38F368
// reference: B9F2AA886AFE302A, 799849542ABBBA64
// reference: BA68F27AAF897442, B4C8413697EF26D8
// reference: A5B98B7D5ED309B6, 665D6B2478FDF251
// reference: F1F014E819F98F44, 333EC15450F8FA23
// reference: AE7BFA1C6932CC66, 5AB7315286C48B40
// reference: D23D6C4E49A6F3C5, EFB9FF95EA5BF4CF
// reference: 39A6603575E1BD5D, 7D93C85249C334C7
// reference: 4FDC277D8D55F9DA, AE6941B132F4BED2
// reference: 47B5E477EE6A6858, E16CA69F6C9C1295
// reference: D4FB12DCD88237AE, AE0327C31D93756B
// reference: 99E6C44D53BC6787, 871105CF820C63B2
// reference: 697C90E11873230F, 84A6DF418793C6A1
// reference: 73B90070B9E3B8CC, 9C5AC2F7A0BA0D3E
// reference: 15D59261CB30F261, 763323870D554424
// reference: 2798AD3FC6506BC6, E34B9BD15549161B
// reference: E54FC6CD80FFB6E9, 7162C2AA5E2244CB
// reference: 4789378F21255E09, 37BB3C5A7DB87D7A
// reference: AB5DC5EF3BC087B4, 77B41F440BB8269F
// reference: 02524EA9477E9EBE, 6CC786040C326E32
// reference: ABDFFD1D0E5C12F3, 668CD4052C0EF276
// reference: BC1062CCEE6A6858, 09CE2AD4CFEAD9C9
// reference: 13FC7BE2523E3B84, 1BB21AFF1EE80666
// reference: 80B28D4964D66472, 225ECED3E1C4AA08
// reference: CDAF5BD8EFE8345B, 3E8EE698CF4629CD
// reference: 3D350F74A42770D3, AE5DFCC82BEF25A8
// reference: 27F09FE505B7EB10, 5AA1ED401D8EE355
// reference: 419C0DF47764A1BD, 6B6E4A8EF9B7DAEC
// reference: 73D132AA7A04381A, 1339CA1AC2E7935A
// reference: B10659583CABE535, E60A4946F232A1B7
// reference: 13C0A81A9D710DD5, FBB495C0B05A0951
// reference: 561BD13CFB2ACD62, A19EED6FB585A97F
// reference: FF966288B208412F, 4ADABABBD6734FD2
// reference: E859FD59523E3B84, 67A94625EABBF321
// reference: 12A3D574EE6A6858, 48473B67B9291A0E
// reference: FB769F3D1ACF23E2, E8E72786B610A479
// reference: AB5DC5EF9867F867, 278EF0FA5A3ECBE8
// reference: 796C13B260A11867, 82E75DE03E272DC6
// reference: 6A4371D628500001, 5907EEE02B1B2CDC
// reference: 82C9950960A11867, 6A45D1AB9D51E69A
// reference: 5C9AB533DD7717B8, 251B28F68D817C9B
// reference: AC00E19F96B85330, B26DD556EF4865DC
// reference: B6C5710E3728C8F3, A797D69DF99E7B62
// reference: D0A9E31F45FB825E, D1FCD60A320A5190
// reference: E2E4DC41489B1BF9, 0F77FB246A493A03
// reference: 2033B7B30E34C6D6, 505D1F6822E12FBF
// reference: C72E3FD7C9B5EE81, DA2D7E3A252B29F1
// reference: 6EA38C63809762CC, 4AD845688008C26D
// reference: 12A3D57460A11867, 42D421B50B7BFAD1
// reference: 056C4AA5809762CC, FFC69F8F8D70E682
// reference: 9459A44EB208412F, 9B469114F3FF6691
// reference: 118763A256494791, 60DE419C8CA00D8A
// reference: 13FC7BE2A940EEAA, 0A17B99BEC8980E4
// reference: FB769F3DE1B1F6CC, 8ECF666E441EF81A
// reference: 3D350F745F59A5FD, 2E7F4E1D616CE909
// reference: 27F09FE5FEC93E3E, BFE25938FDC48580
// reference: 419C0DF48C1A7493, 49C1D54C9BB4AFF0
// reference: 73D132AA817AED34, 6DCCD3605F410191
// reference: 561BD13C0054184C, 2C5ACB486C86B533
// reference: 80B28D499FA8B15C, 14ED970EEA4028A5
// reference: CDAF5BD81496E175, E02ABD110B6DBEF4
// reference: B1065958C7D5301B, 25B9C13CF176223D
// reference: 9459A44E49769401, 55AC60055A370309
// reference: E859FD59A940EEAA, 760CE54118DA75A3
// reference: 2DEA9D3AA5253ABE, 270D710301814E2C
// reference: C56079E5EDD422D8, B7C6A3742A1468F5
// reference: E2B3431D19C62D15, 7D744EA5F2992D58
// reference: F876D38CB856B6D6, 9F957C1F45B6D246
// reference: 97F4B423CA85FC7B, 8740C93E710C8D1C
// reference: A5B98B7DC7E565DC, F20EDFB193B8E7EA
// reference: 899D9D5546CB90A4, 7AE060FDD9B123E0
// reference: 122917B15209699D, FABF5C266995752A
// reference: 676EE08F814AB8F3, 3D1D583D2C8E3D9D
// reference: 539E0BD745134015, 7EBE3CC6CBF09B2C
// reference: D64F1B81A5253ABE, BD6AD2CF24844AA3
// reference: C5A811CD20905013, FFBC46B3AB0BFB24
// reference: 040521E74ECBC74C, 4F16C56C361A572F
// reference: 5173872168F0A651, D4762C3B0658B868
// reference: 79A302AFE20FBC4C, 9CD647EF85EF7CD8
// reference: 9129E670AAFEA42A, 63051D9088D1408D
// reference: B6FADC885EECABE7, D2085A77D44D517A
// reference: AC3F4C19FF7C3024, F3A71ED69D6929ED
// reference: C3BD2BB68DAF7A89, B859757285851B30
// reference: F1F014E880CFE32E, 2D4084AEB2B4161F
// reference: DDD402C001E11656, DDFD33886541B273
// reference: 466088241523EF6F, ABCDDEF36CE57D3D
// reference: 33277F1AC6603E01, EE316CD84AB4DBC8
// reference: 07D794420239C6E7, 976EE145E7686DFB
// reference: 82068414E20FBC4C, 6885B2A8F9F42002
// reference: 91E18E5867BAD6E1, 3CD6D6389535A415
// reference: 877B2157387B8DCC, 9062E97D0BCDF52E
// reference: D332BEC27F510B3E, E85EE933CF59566B
// reference: B013910B36F5F8BF, F935CDD1A6992EF1
// program: 421CC489DA0D894C, 0000000000000000, 7DCF9122F7A193A0
// program: 3113F77E3F9B1FAD, 0000000000000000, E9CE7AFC482AF804
// program: 1A6183C6E984248F, 0000000000000000, DBE3C296384EA0AC
// program: AFA1F04CAC9C714F, 0000000000000000, 5AB7315286C48B40
// program: B1F187F2D4B74547, 0000000000000000, 989317BD58EE15F3
// program: 3113F77E3F9B1FAD, 0000000000000000, 40065C472AE4DC56
// program: 3113F77E3F9B1FAD, 0000000000000000, DBA7C834B3E957DA
// program: EA36FEB391FDE835, 0000000000000000, 5ECFC586D92FE738
// program: 090F6261D7EE040A, 0000000000000000, 548123F32C891796
// program: DB65EA2FBAD032F1, 0000000000000000, 5ECFC586D92FE738
// program: 1A6183C6E984248F, 0000000000000000, 6696EBFC0AC0717D
// program: 1A6183C6E984248F, 0000000000000000, D511B25408440125
// program: 61A82C16044CBE52, 0000000000000000, 2086CB6D0D2EC954
// program: 61A82C16044CBE52, 0000000000000000, 6696EBFC0AC0717D
// program: 61A82C16044CBE52, 0000000000000000, 478298841EDB4A61
// program: 61A82C16044CBE52, 0000000000000000, 15B6C909B7264459
// program: 619E562CDB89F275, 0000000000000000, 771030A3EE84D48A
// program: 619E562CDB89F275, 0000000000000000, CD0EB4DEA4AE12FC
// program: 83C36A4A2949D5E1, 0000000000000000, CD0EB4DEA4AE12FC
// program: A2B1C50622C4E336, 0000000000000000, 13D5052BA6BA4F03
// program: 90082AE6B5CE434D, 0000000000000000, B4170363C2A69641
// program: D47C887971654D1D, 0000000000000000, 2F81AEF57032A452
// program: 619E562CDB89F275, 0000000000000000, 6A1B72A2FCCBE987
// program: 05E1A4F0384EBF7F, 0000000000000000, B4170363C2A69641
// program: 83C36A4A2949D5E1, 0000000000000000, 6A1B72A2FCCBE987
// program: 83C36A4A2949D5E1, 0000000000000000, F35472FEFCCBE987
// program: 2E393891BC8F259B, 0000000000000000, 2F81AEF57032A452
// program: 82498C00693767D6, 0000000000000000, 93A93BC09551D122
// program: 421CC489DA0D894C, 0000000000000000, F390C0835F45F9F9
// program: 3113F77E3F9B1FAD, 0000000000000000, 4F16C56C361A572F
// program: 1A6183C6E984248F, 0000000000000000, 0682315691DF22F8
// program: EA36FEB391FDE835, 0000000000000000, A8CF8742D3FFE418
// program: 090F6261D7EE040A, 0000000000000000, 1F31D122238544D7
// program: DB65EA2FBAD032F1, 0000000000000000, A8CF8742D3FFE418
// program: 1A6183C6E984248F, 0000000000000000, 89F4A144483FDCBF
// program: 1A6183C6E984248F, 0000000000000000, 4A53C178DA180688
// program: 61A82C16044CBE52, 0000000000000000, B213DBEA703F4DA9
// program: 61A82C16044CBE52, 0000000000000000, 89F4A144483FDCBF
// program: 61A82C16044CBE52, 0000000000000000, E6DEB3A176267503
// program: 61A82C16044CBE52, 0000000000000000, 1B1F65E69DCC2CF2
// program: A2B1C50622C4E336, 0000000000000000, C6C266BB71A4780A
// program: D47C887971654D1D, 0000000000000000, D7ABDD423A9FE0A5
// program: 2E393891BC8F259B, 0000000000000000, D7ABDD423A9FE0A5
// program: 82498C00693767D6, 0000000000000000, C14C2A01FF8980CD
// program: D47C887971654D1D, 0000000000000000, 084C260CDA2D8394
// program: 421CC489DA0D894C, 0000000000000000, 7178057DA30C6B80
// program: EA36FEB391FDE835, 0000000000000000, 1F8B56B6A69D76F6
// program: DB65EA2FBAD032F1, 0000000000000000, CCDD8A714D8FD59E
// program: D47C887971654D1D, 0000000000000000, 20F89F0FD505854F
// program: D47C887971654D1D, 0000000000000000, 7741ED8274BA6CC9
// program: 421CC489DA0D894C, 0000000000000000, 7BA8F0614E2EFF77
// program: AFA1F04CAC9C714F, 0000000000000000, D4762C3B0658B868
// program: 421CC489DA0D894C, 0000000000000000, BEF3C067C9E0A693
// program: 61A82C16044CBE52, 0000000000000000, F25393052E5C67E0
// program: F0F03E5C52F45639, 0000000000000000, A8CF8742D3FFE418
// program: F015ECC7723E6782, 0000000000000000, A8CF8742D3FFE418
// program: DEF016CB05491BB4, 0000000000000000, 1F31D122238544D7
// program: EA36FEB391FDE835, 0000000000000000, A34EF2EBF6540E32
// program: DB65EA2FBAD032F1, 0000000000000000, A34EF2EBF6540E32
// program: 090F6261D7EE040A, 0000000000000000, 918C77499A4D914E
// program: 1A6183C6E984248F, 0000000000000000, B1AE1D2ED3065873
// program: 1A6183C6E984248F, 0000000000000000, 093F1E3AA6E724B3
// program: 61A82C16044CBE52, 0000000000000000, C852EFA5F1B65DC1
// program: 61A82C16044CBE52, 0000000000000000, B1AE1D2ED3065873
// program: 61A82C16044CBE52, 0000000000000000, C8B980B12AB3A0E1
// program: 61A82C16044CBE52, 0000000000000000, 2BC9383774C63DD7
// program: EA36FEB391FDE835, 0000000000000000, FFEE8AA3792C903B
// program: 82498C00693767D6, 0000000000000000, 230A49A38D8D21DC
// program: A2B1C50622C4E336, 0000000000000000, 8081C8610DB056FA
// program: DB65EA2FBAD032F1, 0000000000000000, CAB25DB4B91FEC6E
// program: D47C887971654D1D, 0000000000000000, 856E75259E753956
// program: 2E393891BC8F259B, 0000000000000000, 856E75259E753956
// program: EA36FEB391FDE835, 0000000000000000, 5C9841FF918E1C70
// program: DB65EA2FBAD032F1, 0000000000000000, 5C9841FF918E1C70
// program: F0F03E5C52F45639, 0000000000000000, 5C9841FF918E1C70
// program: F015ECC7723E6782, 0000000000000000, 5C9841FF918E1C70
// program: DEF016CB05491BB4, 0000000000000000, 62DE8C3DC104C851
// program: EA36FEB391FDE835, 0000000000000000, 304A5CE25080BC6E
// program: DB65EA2FBAD032F1, 0000000000000000, 304A5CE25080BC6E
// program: 090F6261D7EE040A, 0000000000000000, C1FE7459E199B532
// program: 090F6261D7EE040A, 0000000000000000, 62DE8C3DC104C851
// program: D47C887971654D1D, 0000000000000000, 358240CD08F369E0
// program: D47C887971654D1D, 0000000000000000, 7664CF1BF35D0097
// program: 528A088158F377D7, 0000000000000000, 6B63BE42E2F6C431
// program: 05E1A4F0384EBF7F, 0000000000000000, F935CDD1A6992EF1
// program: 83C36A4A2949D5E1, 0000000000000000, FC945F1AF9B5312E
// program: A2B1C50622C4E336, 0000000000000000, 363FE38B1D0D9C46
// program: 528A088158F377D7, 0000000000000000, CB4482862F23E816
// program: A2B1C50622C4E336, 0000000000000000, EC99DA070AAE5A79
// program: 1A6183C6E984248F, 0000000000000000, 3CCB3012880A4C66
// program: 1A6183C6E984248F, 0000000000000000, CDB452FE3708A1B9
// program: 1A6183C6E984248F, 0000000000000000, 4EFADB1ACF3A60B6
// program: 1A6183C6E984248F, 0000000000000000, E3F3A886765A887F
// program: 61A82C16044CBE52, 0000000000000000, CDB452FE3708A1B9
// program: 61A82C16044CBE52, 0000000000000000, 4EFADB1ACF3A60B6
// program: 61A82C16044CBE52, 0000000000000000, 3CCB3012880A4C66
// program: 3113F77E3F9B1FAD, 0000000000000000, 9D61FEF0BD63709C
// program: 61A82C16044CBE52, 0000000000000000, 60030B56D96F796A
// program: AFA1F04CAC9C714F, 0000000000000000, AE43ACCC97EB4103
// program: 1A6183C6E984248F, 0000000000000000, 60030B56D96F796A
// program: 1A6183C6E984248F, 0000000000000000, 996EFADAB2994BFA
// program: EA36FEB391FDE835, 0000000000000000, C729D1E6EFA44000
// program: 82498C00693767D6, 0000000000000000, 17604D84BCC15904
// program: A2B1C50622C4E336, 0000000000000000, 20922404C46D28CF
// program: DB65EA2FBAD032F1, 0000000000000000, 2E2BDC37BDE53717
// program: 528A088158F377D7, 0000000000000000, 5B26FA61DC10A8A8
// program: B1F187F2D4B74547, 0000000000000000, BBDB9E15B4150F1D
// program: 2E393891BC8F259B, 0000000000000000, BAF1C5B228F95206
// program: 05E1A4F0384EBF7F, 0000000000000000, 03B41249843C92F4
// program: 83C36A4A2949D5E1, 0000000000000000, EB6DE1D05D2F8ED0
// program: F0F03E5C52F45639, 0000000000000000, 47D87D9FDC580E42
// program: F015ECC7723E6782, 0000000000000000, 47D87D9FDC580E42
// program: DEF016CB05491BB4, 0000000000000000, 8230B968DACC8A3C
// program: EA36FEB391FDE835, 0000000000000000, 47D87D9FDC580E42
// program: DB65EA2FBAD032F1, 0000000000000000, 47D87D9FDC580E42
// program: 090F6261D7EE040A, 0000000000000000, 8230B968DACC8A3C
// program: 0000000000000000, 0000000000000000, F390C0835F45F9F9
// program: 1A6183C6E984248F, 0000000000000000, 188544D4C4C2AC0C
// program: 1A6183C6E984248F, 0000000000000000, ACCD4E2B4D747FB2
// program: 1A6183C6E984248F, 0000000000000000, 0BC6E5CF774C3EF6
// program: 61A82C16044CBE52, 0000000000000000, ACCD4E2B4D747FB2
// program: 61A82C16044CBE52, 0000000000000000, 0BC6E5CF774C3EF6
// program: 61A82C16044CBE52, 0000000000000000, 188544D4C4C2AC0C
// program: 61A82C16044CBE52, 0000000000000000, B40B1A0497443CCF
// program: 1A6183C6E984248F, 0000000000000000, B40B1A0497443CCF
// program: 1A6183C6E984248F, 0000000000000000, 2C73831C512A6BC9
// program: EA36FEB391FDE835, 0000000000000000, 91C71CBBFBBA9014
// program: 82498C00693767D6, 0000000000000000, FB992A7C3CB63639
// program: A2B1C50622C4E336, 0000000000000000, 1DD3AAD26EC56F4C
// program: DB65EA2FBAD032F1, 0000000000000000, 1EDC5A830F863610
// program: 528A088158F377D7, 0000000000000000, 4FED7CE8B7F510D1
// program: 2E393891BC8F259B, 0000000000000000, 4E2CA3FE5E1866A5
// program: F0F03E5C52F45639, 0000000000000000, 5B8E8CB2390BB6C9
// program: F015ECC7723E6782, 0000000000000000, 5B8E8CB2390BB6C9
// program: DEF016CB05491BB4, 0000000000000000, AD9D32803DD4CB16
// program: EA36FEB391FDE835, 0000000000000000, 5B8E8CB2390BB6C9
// program: DB65EA2FBAD032F1, 0000000000000000, 5B8E8CB2390BB6C9
// program: 090F6261D7EE040A, 0000000000000000, AD9D32803DD4CB16
// program: 1A6183C6E984248F, 0000000000000000, 45C78DF33D884A46
// program: 1A6183C6E984248F, 0000000000000000, 733A91686C7182B4
// program: 1A6183C6E984248F, 0000000000000000, AECADE9018F11BA1
// program: 1A6183C6E984248F, 0000000000000000, 196F65C6FDAD61F4
// program: 61A82C16044CBE52, 0000000000000000, 733A91686C7182B4
// program: 61A82C16044CBE52, 0000000000000000, AECADE9018F11BA1
// program: 61A82C16044CBE52, 0000000000000000, 45C78DF33D884A46
// program: 61A82C16044CBE52, 0000000000000000, 8AF11215F3CACD04
// program: 1A6183C6E984248F, 0000000000000000, 8AF11215F3CACD04
// program: 1A6183C6E984248F, 0000000000000000, 5AB587C98BDFBBDF
// program: EA36FEB391FDE835, 0000000000000000, EC97F6751B686B22
// program: 82498C00693767D6, 0000000000000000, 6BA6BFE2A4D83669
// program: A2B1C50622C4E336, 0000000000000000, ACFB812410DF3A82
// program: DB65EA2FBAD032F1, 0000000000000000, 638CB04DEF54CD26
// program: 528A088158F377D7, 0000000000000000, 58D30BCD74FF4DA6
// program: 2E393891BC8F259B, 0000000000000000, DD976AC845F86ECA
// program: 83C36A4A2949D5E1, 0000000000000000, BD2701B976A9B3CA
// program: F0F03E5C52F45639, 0000000000000000, 26DE667CD9D94DFF
// program: F015ECC7723E6782, 0000000000000000, 26DE667CD9D94DFF
// program: DEF016CB05491BB4, 0000000000000000, EEAEEF847BDA184B
// program: EA36FEB391FDE835, 0000000000000000, 26DE667CD9D94DFF
// program: DB65EA2FBAD032F1, 0000000000000000, 26DE667CD9D94DFF
// program: 090F6261D7EE040A, 0000000000000000, EEAEEF847BDA184B
// program: 1A6183C6E984248F, 0000000000000000, 6C16192D37DDE8F9
// program: 1A6183C6E984248F, 0000000000000000, F7F3D1391CCA85F2
// program: 1A6183C6E984248F, 0000000000000000, 07BC3C3DE1BCF81A
// program: 1A6183C6E984248F, 0000000000000000, F8EBBF49424F6950
// program: 61A82C16044CBE52, 0000000000000000, F7F3D1391CCA85F2
// program: 61A82C16044CBE52, 0000000000000000, 07BC3C3DE1BCF81A
// program: 61A82C16044CBE52, 0000000000000000, 6C16192D37DDE8F9
// program: 61A82C16044CBE52, 0000000000000000, 85FAF61880F90611
// program: 1A6183C6E984248F, 0000000000000000, 85FAF61880F90611
// program: 1A6183C6E984248F, 0000000000000000, FC72D604255CCBB3
// program: EA36FEB391FDE835, 0000000000000000, 5AE266D590887634
// program: 82498C00693767D6, 0000000000000000, 7AF9009B5B7CCCDE
// program: A2B1C50622C4E336, 0000000000000000, 8404D6B6BD509D70
// program: DB65EA2FBAD032F1, 0000000000000000, 5E323051F0B32632
// program: 528A088158F377D7, 0000000000000000, 7DF3D08A53F23329
// program: 2E393891BC8F259B, 0000000000000000, 49DAF06BBC0888D0
// program: 83C36A4A2949D5E1, 0000000000000000, 3B5A3BE54903618C
// program: F0F03E5C52F45639, 0000000000000000, D523B1595F7A43C2
// program: F015ECC7723E6782, 0000000000000000, D523B1595F7A43C2
// program: DEF016CB05491BB4, 0000000000000000, 8CF1B785A1FB1E23
// program: EA36FEB391FDE835, 0000000000000000, D523B1595F7A43C2
// program: DB65EA2FBAD032F1, 0000000000000000, D523B1595F7A43C2
// program: 090F6261D7EE040A, 0000000000000000, 8CF1B785A1FB1E23
// program: B1F187F2D4B74547, 0000000000000000, 206CABF84BF3F7D6
// program: E4B1717CEF51482E, 0000000000000000, B1B791610D217D05
// program: 1A6183C6E984248F, 0000000000000000, 2DDBCD4EC3302F18
// program: 1A6183C6E984248F, 0000000000000000, 81B4B56064F0494E
// program: EA36FEB391FDE835, 0000000000000000, DFE481A2A5D547B7
// program: DB65EA2FBAD032F1, 0000000000000000, DFE481A2A5D547B7
// program: F0F03E5C52F45639, 0000000000000000, DFE481A2A5D547B7
// program: F015ECC7723E6782, 0000000000000000, DFE481A2A5D547B7
// program: DEF016CB05491BB4, 0000000000000000, 05872491DEABFB07
// program: F015ECC7723E6782, 0000000000000000, AA86134C12919643
// program: EA36FEB391FDE835, 0000000000000000, 6BCCD30BE25893A2
// program: DB65EA2FBAD032F1, 0000000000000000, 6BCCD30BE25893A2
// program: 090F6261D7EE040A, 0000000000000000, 5B83F6948C4F1252
// program: A2B1C50622C4E336, 0000000000000000, B564B0B80C768F77
// program: E4B1717CEF51482E, 0000000000000000, 46A3E37791B2B5FF
// program: 1A6183C6E984248F, 0000000000000000, 82CD91E5FEB0B622
// program: 1A6183C6E984248F, 0000000000000000, DA752D74AA8E4C2A
// program: EA36FEB391FDE835, 0000000000000000, EEBD0A76CDF74133
// program: DB65EA2FBAD032F1, 0000000000000000, EEBD0A76CDF74133
// program: F0F03E5C52F45639, 0000000000000000, EEBD0A76CDF74133
// program: F015ECC7723E6782, 0000000000000000, EEBD0A76CDF74133
// program: DEF016CB05491BB4, 0000000000000000, 864492417C9B95F1
// program: F015ECC7723E6782, 0000000000000000, CBB80EF95790E2FF
// program: EA36FEB391FDE835, 0000000000000000, 48B65508C7BB4D2E
// program: DB65EA2FBAD032F1, 0000000000000000, 48B65508C7BB4D2E
// program: 090F6261D7EE040A, 0000000000000000, 8EB655DE9270E92E
// program: EA36FEB391FDE835, 0000000000000000, 380CB22EF5DCB418
// program: 090F6261D7EE040A, 0000000000000000, 0EFDA5332B364838
// program: DB65EA2FBAD032F1, 0000000000000000, 380CB22EF5DCB418
// program: 1A6183C6E984248F, 0000000000000000, 8083F4F55E4E1C7A
// program: 1A6183C6E984248F, 0000000000000000, 0E380EC384E3316F
// program: 61A82C16044CBE52, 0000000000000000, 8083F4F55E4E1C7A
// program: 61A82C16044CBE52, 0000000000000000, E51156508E8F2BCB
// program: 61A82C16044CBE52, 0000000000000000, 58F083A1507E0D30
// program: E4B1717CEF51482E, 0000000000000000, A1D47C6D50CD01C9
// program: 619E562CDB89F275, 0000000000000000, ADCE63B3904CE05A
// program: EA36FEB391FDE835, 0000000000000000, 75B2F1F3650A49D9
// program: 82498C00693767D6, 0000000000000000, 339CE30E3C6F156E
// program: DB65EA2FBAD032F1, 0000000000000000, 94B3232E648BCFAB
// program: 528A088158F377D7, 0000000000000000, FADF5F1EDC9A44CC
// program: 1A6183C6E984248F, 0000000000000000, C2D140C3B6AD13C7
// program: 1A6183C6E984248F, 0000000000000000, 1C52DFF3C802031C
// program: 61A82C16044CBE52, 0000000000000000, 0E380EC384E3316F
// program: 61A82C16044CBE52, 0000000000000000, C2D140C3B6AD13C7
// program: 1A6183C6E984248F, 0000000000000000, E51156508E8F2BCB
// program: 1A6183C6E984248F, 0000000000000000, 58F083A1507E0D30
// program: A2B1C50622C4E336, 0000000000000000, 25D440C063D25193
// program: 2E393891BC8F259B, 0000000000000000, 7966BA6B82AA7A6E
// program: 83C36A4A2949D5E1, 0000000000000000, 23FC06BB5547BA21
// program: EA36FEB391FDE835, 0000000000000000, E272017B162D53A5
// program: 090F6261D7EE040A, 0000000000000000, BC8AF9CE25960F6F
// program: DB65EA2FBAD032F1, 0000000000000000, E272017B162D53A5
// program: 61A82C16044CBE52, 0000000000000000, 0A07275C2603B901
// program: 61A82C16044CBE52, 0000000000000000, E7F57906CBA5BA60
// program: 61A82C16044CBE52, 0000000000000000, 4D3CBC260B9A902D
// program: E4B1717CEF51482E, 0000000000000000, FAD70185C76226C4
// program: 82498C00693767D6, 0000000000000000, 6E355ACEE1E8FEBD
// program: DB65EA2FBAD032F1, 0000000000000000, 4ECD907B877A2816
// program: A2B1C50622C4E336, 0000000000000000, 2B740797D5E0C78A
// program: 1A6183C6E984248F, 0000000000000000, EFCD6855F34ACA80
// program: 1A6183C6E984248F, 0000000000000000, 0A07275C2603B901
// program: 1A6183C6E984248F, 0000000000000000, 1002D0E3753BB8D0
// program: 1A6183C6E984248F, 0000000000000000, DBF8847431D3E735
// program: 61A82C16044CBE52, 0000000000000000, 1002D0E3753BB8D0
// program: 61A82C16044CBE52, 0000000000000000, EFCD6855F34ACA80
// program: 1A6183C6E984248F, 0000000000000000, E7F57906CBA5BA60
// program: 1A6183C6E984248F, 0000000000000000, 4D3CBC260B9A902D
// program: 528A088158F377D7, 0000000000000000, F38F6A8A17534F29
// program: 2E393891BC8F259B, 0000000000000000, 176F8B2D82CA2078
// program: 83C36A4A2949D5E1, 0000000000000000, 0707194330CAF8B5
// program: EA36FEB391FDE835, 0000000000000000, 149637F32845305A
// program: 090F6261D7EE040A, 0000000000000000, 3ABC9AEFC8622443
// program: DB65EA2FBAD032F1, 0000000000000000, 149637F32845305A
// program: 61A82C16044CBE52, 0000000000000000, D09B6CA36D624C77
// program: 61A82C16044CBE52, 0000000000000000, 7BCC7E384316547B
// program: E4B1717CEF51482E, 0000000000000000, 1CF273A0E866E793
// program: 82498C00693767D6, 0000000000000000, B8D501FAAE5A1BDB
// program: DB65EA2FBAD032F1, 0000000000000000, 5CA5404BAA25FCBC
// program: A2B1C50622C4E336, 0000000000000000, 032D8144B499502B
// program: 1A6183C6E984248F, 0000000000000000, 0DC0397A86741CA2
// program: 1A6183C6E984248F, 0000000000000000, D09B6CA36D624C77
// program: 1A6183C6E984248F, 0000000000000000, 6A605BBC42AC52A3
// program: 1A6183C6E984248F, 0000000000000000, C90A885CAC7E5F80
// program: 61A82C16044CBE52, 0000000000000000, 6A605BBC42AC52A3
// program: 61A82C16044CBE52, 0000000000000000, 0DC0397A86741CA2
// program: 1A6183C6E984248F, 0000000000000000, 7BCC7E384316547B
// program: 1A6183C6E984248F, 0000000000000000, 26B8C4D421ADF2B8
// program: 528A088158F377D7, 0000000000000000, 4E9853FC1D8EDF7E
// program: 2E393891BC8F259B, 0000000000000000, EF6A803A96113B0F
// program: 83C36A4A2949D5E1, 0000000000000000, 14D0DE2A69F00C69
// program: EA36FEB391FDE835, 0000000000000000, 8C752F9065F81B43
// program: 090F6261D7EE040A, 0000000000000000, AA9FB349FB761294
// program: DB65EA2FBAD032F1, 0000000000000000, 8C752F9065F81B43
// program: 61A82C16044CBE52, 0000000000000000, A4FAF336B79A04EF
// program: 61A82C16044CBE52, 0000000000000000, 33514A55213666D1
// program: E4B1717CEF51482E, 0000000000000000, 43D24D6DE00195F0
// program: 82498C00693767D6, 0000000000000000, E28B81BC5C49BC05
// program: DB65EA2FBAD032F1, 0000000000000000, 20CABE90F4AF60F0
// program: 1A6183C6E984248F, 0000000000000000, 71441D4873C08D21
// program: 1A6183C6E984248F, 0000000000000000, A4FAF336B79A04EF
// program: 1A6183C6E984248F, 0000000000000000, 8A3255CECD6B7FEB
// program: 61A82C16044CBE52, 0000000000000000, 8A3255CECD6B7FEB
// program: 61A82C16044CBE52, 0000000000000000, 71441D4873C08D21
// program: 1A6183C6E984248F, 0000000000000000, 33514A55213666D1
// program: 1A6183C6E984248F, 0000000000000000, 4E2EAEA44E10AE51
// program: A2B1C50622C4E336, 0000000000000000, 7F8CBA1EE9526ABA
// program: 528A088158F377D7, 0000000000000000, F6E4EA0AEA744A5F
// program: 2E393891BC8F259B, 0000000000000000, FAB93561073001C2
// program: EA36FEB391FDE835, 0000000000000000, 308CFEFF99927ACF
// program: 090F6261D7EE040A, 0000000000000000, 76BA3EE1066A3754
// program: DB65EA2FBAD032F1, 0000000000000000, 308CFEFF99927ACF
// program: 1A6183C6E984248F, 0000000000000000, E895C073D6A1F89D
// program: 1A6183C6E984248F, 0000000000000000, 71C23213692D1F87
// program: 1A6183C6E984248F, 0000000000000000, 6CE8444273D08212
// program: 82498C00693767D6, 0000000000000000, DA7DD7FAD6457136
// program: DB65EA2FBAD032F1, 0000000000000000, 9C336FFF08C5017C
// program: 528A088158F377D7, 0000000000000000, F949643F7204FBE4
// program: 1A6183C6E984248F, 0000000000000000, CE9777947027A713
// program: 61A82C16044CBE52, 0000000000000000, 6CE8444273D08212
// program: 61A82C16044CBE52, 0000000000000000, E895C073D6A1F89D
// program: 61A82C16044CBE52, 0000000000000000, 71C23213692D1F87
// program: 61A82C16044CBE52, 0000000000000000, 7B8566986E5321C8
// program: 1A6183C6E984248F, 0000000000000000, 7B8566986E5321C8
// program: 1A6183C6E984248F, 0000000000000000, F176590126400FF2
// program: A2B1C50622C4E336, 0000000000000000, 08883FAC689573B4
// program: 2E393891BC8F259B, 0000000000000000, E5A53BDCF8EACAB9
// program: EA36FEB391FDE835, 0000000000000000, AFCC42A686FBAE64
// program: EA36FEB391FDE835, 0000000000000000, 4ECD907B877A2816
// program: 17FE472057E5AA57, 0000000000000000, 03B41249843C92F4
// program: 61A82C16044CBE52, 0000000000000000, 5BF83C9D8ECC5920
// program: EA36FEB391FDE835, 0000000000000000, 77F1F437895CF4C6
// program: 090F6261D7EE040A, 0000000000000000, AF957CD4F6E644F8
// program: DB65EA2FBAD032F1, 0000000000000000, 77F1F437895CF4C6
// program: 1A6183C6E984248F, 0000000000000000, 1568B4770B642950
// program: 1A6183C6E984248F, 0000000000000000, 5C3C63262995706B
// program: 61A82C16044CBE52, 0000000000000000, E768A5DED2E2443C
// program: 61A82C16044CBE52, 0000000000000000, 5C3C63262995706B
// program: 1A6183C6E984248F, 0000000000000000, 54AB2A40CB60072C
// program: 1A6183C6E984248F, 0000000000000000, 3E87BC4040FF630E
// program: 82498C00693767D6, 0000000000000000, 2F24B48DCFF04E80
// program: DB65EA2FBAD032F1, 0000000000000000, 32A32206BFD1741F
// program: 528A088158F377D7, 0000000000000000, 049CB0B95DDB8C65
// program: 1A6183C6E984248F, 0000000000000000, 7D9E496FDCE397EB
// program: 61A82C16044CBE52, 0000000000000000, 1568B4770B642950
// program: 61A82C16044CBE52, 0000000000000000, 158D923174A94888
// program: A2B1C50622C4E336, 0000000000000000, 266031E67BF550E5
// program: 2E393891BC8F259B, 0000000000000000, 709424DFEBB8AF06
// program: EA36FEB391FDE835, 0000000000000000, BDB8643E4BEDD21B
// program: 61A82C16044CBE52, 0000000000000000, 54AB2A40CB60072C
// program: 1A6183C6E984248F, 0000000000000000, 158D923174A94888
// program: EA36FEB391FDE835, 0000000000000000, 9C336FFF08C5017C
// program: EA36FEB391FDE835, 0000000000000000, CA9B07F340DE7E4F
// program: 090F6261D7EE040A, 0000000000000000, A2CB6DCAE82CC430
// program: DB65EA2FBAD032F1, 0000000000000000, CA9B07F340DE7E4F
// program: 1A6183C6E984248F, 0000000000000000, 524B951A4A96EE6F
// program: 61A82C16044CBE52, 0000000000000000, 524B951A4A96EE6F
// program: 82498C00693767D6, 0000000000000000, 4BD990AFB58CD178
// program: DB65EA2FBAD032F1, 0000000000000000, 82A8704BC2BEB2A9
// program: 528A088158F377D7, 0000000000000000, 545FABB0DEC58449
// program: 1A6183C6E984248F, 0000000000000000, 0989F9700FB5FB22
// program: 1A6183C6E984248F, 0000000000000000, 2FC9831FA22543CF
// program: 1A6183C6E984248F, 0000000000000000, E88E34DB4A65BCEC
// program: 61A82C16044CBE52, 0000000000000000, 2FC9831FA22543CF
// program: 61A82C16044CBE52, 0000000000000000, 0989F9700FB5FB22
// program: 61A82C16044CBE52, 0000000000000000, 1305C0E7AD44070C
// program: 1A6183C6E984248F, 0000000000000000, 1305C0E7AD44070C
// program: 1A6183C6E984248F, 0000000000000000, 4AD3295792D27394
// program: A2B1C50622C4E336, 0000000000000000, A73569471F83475D
// program: EA36FEB391FDE835, 0000000000000000, 82A8704BC2BEB2A9
// program: EA36FEB391FDE835, 0000000000000000, B979639B4E5CBE41
// program: 090F6261D7EE040A, 0000000000000000, 78B1FA7A1A353237
// program: DB65EA2FBAD032F1, 0000000000000000, 15C6F29BDF0BC5F2
// program: 1A6183C6E984248F, 0000000000000000, 906446495FDDEBAA
// program: 61A82C16044CBE52, 0000000000000000, 906446495FDDEBAA
// program: 82498C00693767D6, 0000000000000000, A99534D533F2A0F3
// program: A2B1C50622C4E336, 0000000000000000, 3659F95569E669EC
// program: DB65EA2FBAD032F1, 0000000000000000, B979639B4E5CBE41
// program: 528A088158F377D7, 0000000000000000, C5205C16D07FE0CA
// program: 1A6183C6E984248F, 0000000000000000, 0AF8150D724C7A87
// program: 1A6183C6E984248F, 0000000000000000, 202074F342820CC4
// program: 61A82C16044CBE52, 0000000000000000, 202074F342820CC4
// program: 61A82C16044CBE52, 0000000000000000, 0AF8150D724C7A87
// program: 61A82C16044CBE52, 0000000000000000, 85CC0D0479A1DB91
// program: 1A6183C6E984248F, 0000000000000000, 85CC0D0479A1DB91
// program: 1A6183C6E984248F, 0000000000000000, 55B840B50C94CAE4
// program: EA36FEB391FDE835, 0000000000000000, 7D32BD220944870E
// program: EA36FEB391FDE835, 0000000000000000, CE7D591AA3355056
// program: 090F6261D7EE040A, 0000000000000000, B02A2802634D81D5
// program: DB65EA2FBAD032F1, 0000000000000000, CE7D591AA3355056
// program: 1A6183C6E984248F, 0000000000000000, E7FAD86521F6EEA5
// program: 1A6183C6E984248F, 0000000000000000, 19E034050E6A2BA9
// program: 82498C00693767D6, 0000000000000000, 662A5EAC86BB9B96
// program: DB65EA2FBAD032F1, 0000000000000000, AFC0604FCAC6F1FE
// program: 1A6183C6E984248F, 0000000000000000, 9637019C01AD2A67
// program: 1A6183C6E984248F, 0000000000000000, 96AFD1444CBCF159
// program: 61A82C16044CBE52, 0000000000000000, 9637019C01AD2A67
// program: 1A6183C6E984248F, 0000000000000000, D5F2CDC066856118
// program: 1A6183C6E984248F, 0000000000000000, 549A8D71DAA01533
// program: 528A088158F377D7, 0000000000000000, E5D706BE44AA6B66
// program: EA36FEB391FDE835, 0000000000000000, FD81175823C4FC2F
// program: 2E393891BC8F259B, 0000000000000000, E2C07F52C06C1C37
// program: 61A82C16044CBE52, 0000000000000000, 19E034050E6A2BA9
// program: A2B1C50622C4E336, 0000000000000000, A95AB0EC0B59B794
// program: EA36FEB391FDE835, 0000000000000000, C56F74D0D26793FB
// program: 090F6261D7EE040A, 0000000000000000, 95A02E3F02A42602
// program: DB65EA2FBAD032F1, 0000000000000000, C56F74D0D26793FB
// program: 1A6183C6E984248F, 0000000000000000, FEB095FE63F3D6A0
// program: 1A6183C6E984248F, 0000000000000000, F0919164CCB23D4F
// program: 1A6183C6E984248F, 0000000000000000, 647E168B12CD5722
// program: 61A82C16044CBE52, 0000000000000000, 647E168B12CD5722
// program: 82498C00693767D6, 0000000000000000, 36889CA514D1D0F5
// program: A2B1C50622C4E336, 0000000000000000, 21835577E328B088
// program: DB65EA2FBAD032F1, 0000000000000000, 4267FB1817ED056F
// program: 1A6183C6E984248F, 0000000000000000, C2F79FE44B64DA0C
// program: 61A82C16044CBE52, 0000000000000000, FEB095FE63F3D6A0
// program: 1A6183C6E984248F, 0000000000000000, CF0668AAFF71AA90
// program: 2E393891BC8F259B, 0000000000000000, 951988FE690A0F2D
// program: 1A6183C6E984248F, 0000000000000000, 6EE7B3E41D236060
// program: EA36FEB391FDE835, 0000000000000000, 0454952150222268
// program: 090F6261D7EE040A, 0000000000000000, DA2B88E6B82CE183
// program: DB65EA2FBAD032F1, 0000000000000000, 0454952150222268
// program: 1A6183C6E984248F, 0000000000000000, F05D0C0F08BC6441
// program: 1A6183C6E984248F, 0000000000000000, CA0429874869A9FB
// program: 1A6183C6E984248F, 0000000000000000, D9CCD7B3833D6DA2
// program: 1A6183C6E984248F, 0000000000000000, 8C883D39E271E590
// program: 82498C00693767D6, 0000000000000000, 1E0F2C800609E257
// program: DB65EA2FBAD032F1, 0000000000000000, 65E9AC7439D183C0
// program: 61A82C16044CBE52, 0000000000000000, F05D0C0F08BC6441
// program: 61A82C16044CBE52, 0000000000000000, D9CCD7B3833D6DA2
// program: A2B1C50622C4E336, 0000000000000000, BDD817E634126F83
// program: 2E393891BC8F259B, 0000000000000000, CEA70C7FFBAC405E
// program: 528A088158F377D7, 0000000000000000, 35477ACB351E5232
// program: 1A6183C6E984248F, 0000000000000000, 17CF9326F21B6D80
// program: 528A088158F377D7, 0000000000000000, 537E2B69841FCB2F
// program: 2E393891BC8F259B, 0000000000000000, 3392AC068D8B2E0B
// program: EA36FEB391FDE835, 0000000000000000, 15DD6DCDDD8CE00E
// program: EA36FEB391FDE835, 0000000000000000, 5878B1464FDD3833
// program: 83C36A4A2949D5E1, 0000000000000000, A4C9D64D0A2BB3FE
// program: 83C36A4A2949D5E1, 0000000000000000, BBD2EECD76DCB652
// program: EA36FEB391FDE835, 0000000000000000, 5CA5404BAA25FCBC
// program: EA36FEB391FDE835, 0000000000000000, 6C0A89FA92CE988B
// program: 090F6261D7EE040A, 0000000000000000, 7243AA812A3709DC
// program: DB65EA2FBAD032F1, 0000000000000000, 6C0A89FA92CE988B
// program: 82498C00693767D6, 0000000000000000, 2F93B1A5914B0F85
// program: DB65EA2FBAD032F1, 0000000000000000, 13D87A405F0032B3
// program: 1A6183C6E984248F, 0000000000000000, 74BE71040E787122
// program: 1A6183C6E984248F, 0000000000000000, 6886AE9E0F63B38F
// program: 1A6183C6E984248F, 0000000000000000, C7E759041668FE49
// program: 1A6183C6E984248F, 0000000000000000, 66451D2BCEAC217D
// program: 61A82C16044CBE52, 0000000000000000, 6886AE9E0F63B38F
// program: 61A82C16044CBE52, 0000000000000000, 74BE71040E787122
// program: 1A6183C6E984248F, 0000000000000000, 463157331DA8A875
// program: 1A6183C6E984248F, 0000000000000000, A8467B5A02608776
// program: A2B1C50622C4E336, 0000000000000000, CCF8ED61EEFC5893
// program: 2E393891BC8F259B, 0000000000000000, 12317D031D7CB743
// program: DEF016CB05491BB4, 0000000000000000, 7243AA812A3709DC
// program: F0F03E5C52F45639, 0000000000000000, 6C0A89FA92CE988B
// program: F015ECC7723E6782, 0000000000000000, 6C0A89FA92CE988B
// program: 1A6183C6E984248F, 0000000000000000, 81D8F50D640C88BA
// program: 1A6183C6E984248F, 0000000000000000, 9AF622B3D42C08AB
// program: 1A6183C6E984248F, 0000000000000000, 142777278370379C
// program: 61A82C16044CBE52, 0000000000000000, 142777278370379C
// program: 61A82C16044CBE52, 0000000000000000, 9AF622B3D42C08AB
// program: 1A6183C6E984248F, 0000000000000000, 6CD7DC86DA19A840
// program: 82498C00693767D6, 0000000000000000, 5AC6A2C976FC455F
// program: 528A088158F377D7, 0000000000000000, 96CB44EF746D174B
// program: 2E393891BC8F259B, 0000000000000000, 408DAD05A6AE634E
// program: 1A6183C6E984248F, 0000000000000000, 94B43838F0562E29
// program: 756BED717C247FA1, 0000000000000000, 03B41249843C92F4
// program: 31884F9ACC9BB5B8, 0000000000000000, 335E815344D524F2
// program: 83C36A4A2949D5E1, 0000000000000000, ADCE63B3904CE05A
// program: 83C36A4A2949D5E1, 0000000000000000, C940ADE43C90D307
// program: 61A82C16044CBE52, 0000000000000000, 81D8F50D640C88BA
// program: 61A82C16044CBE52, 0000000000000000, 94B43838F0562E29
// program: 90082AE6B5CE434D, 0000000000000000, 03B41249843C92F4
// program: 1A6183C6E984248F, 0000000000000000, 3E5DF7E9441FD60A
// program: 1A6183C6E984248F, 0000000000000000, 297520D746B4A868
// program: 1A6183C6E984248F, 0000000000000000, 9593C6CC3E5DA92B
// program: 61A82C16044CBE52, 0000000000000000, 9593C6CC3E5DA92B
// program: 61A82C16044CBE52, 0000000000000000, 297520D746B4A868
// program: 61A82C16044CBE52, 0000000000000000, 3E5DF7E9441FD60A
// program: 61A82C16044CBE52, 0000000000000000, 87BA565CF2549F21
// program: 528A088158F377D7, 0000000000000000, D681FB06D083631F
// program: 1A6183C6E984248F, 0000000000000000, 4541E50EEF681897
// program: 82498C00693767D6, 0000000000000000, 4F1EB6D2BF48848D
// program: 2E393891BC8F259B, 0000000000000000, 20FBCF3960F5BFFB
// program: 756BED717C247FA1, 0000000000000000, B4170363C2A69641
// program: 31884F9ACC9BB5B8, 0000000000000000, D3A75FBD4FA22584
// program: 1A6183C6E984248F, 0000000000000000, 87BA565CF2549F21
// program: 82498C00693767D6, 0000000000000000, 6FD3A46AE9973F33
// program: 1A6183C6E984248F, 0000000000000000, B178A293AC5C0318
// program: 1A6183C6E984248F, 0000000000000000, 7B3D23038FC55AD4
// program: 61A82C16044CBE52, 0000000000000000, 7DD56F6A7907270D
// program: 61A82C16044CBE52, 0000000000000000, B178A293AC5C0318
// program: 2E393891BC8F259B, 0000000000000000, B3D219A4148246E8
// program: E80FD09F14049207, 0000000000000000, AE43ACCC97EB4103
// program: 61A82C16044CBE52, 0000000000000000, D5F2CDC066856118
// program: E4B1717CEF51482E, 0000000000000000, BE947A91108276C3
// program: E4B1717CEF51482E, 0000000000000000, 02AFFD614C4DF8E5
// program: E4B1717CEF51482E, 0000000000000000, C37CFFD1CA301C7D
// program: EA36FEB391FDE835, 0000000000000000, 15C6F29BDF0BC5F2
// program: 83C36A4A2949D5E1, 0000000000000000, 5D5C587782042F20
// program: EA36FEB391FDE835, 0000000000000000, CBD05DCDB517AE1B
// program: EA36FEB391FDE835, 0000000000000000, 20CABE90F4AF60F0
// program: 83C36A4A2949D5E1, 0000000000000000, 6D1921E9DFC3BB48
// program: E4B1717CEF51482E, 0000000000000000, D31F462801822E2B
// program: AE17434EB03F29DD, 0000000000000000, C05114BB49500BC1
// program: AE17434EB03F29DD, 0000000000000000, 1CB643C565E59B0D
// program: E4B1717CEF51482E, 0000000000000000, 2F2E6AB88BB97F23
// program: 61A82C16044CBE52, 0000000000000000, E7FAD86521F6EEA5
// program: 1A6183C6E984248F, 0000000000000000, C65518668DF870D3
// program: 090F6261D7EE040A, 0000000000000000, 9590F2693805314C
// program: 1A6183C6E984248F, 0000000000000000, 01C7C9F05ACC52F2
// program: EA36FEB391FDE835, 0000000000000000, B742B3A7FC4B467A
// program: 82498C00693767D6, 0000000000000000, 8B88117287F768A7
// program: DB65EA2FBAD032F1, 0000000000000000, 5E40BE76AE0A316D
// program: 1A6183C6E984248F, 0000000000000000, 4E360EC9DA99F4D9
// program: 1A6183C6E984248F, 0000000000000000, 637CC226B31D0F82
// program: 1A6183C6E984248F, 0000000000000000, C41D0F1C4B01C5F5
// program: 61A82C16044CBE52, 0000000000000000, 4E360EC9DA99F4D9
// program: 1A6183C6E984248F, 0000000000000000, AB42AAF3228AA53E
// program: 528A088158F377D7, 0000000000000000, F34E10C3284B598C
// program: 2E393891BC8F259B, 0000000000000000, 5823B2EB89C78DDC
// program: 1A6183C6E984248F, 0000000000000000, CFB891FCA13B1FFD
// program: 090F6261D7EE040A, 0000000000000000, 5E2DD176DEB910ED
// program: 1A6183C6E984248F, 0000000000000000, 963D21C841ADF433
// program: EA36FEB391FDE835, 0000000000000000, BDAAF968EF1C596D
// program: DB65EA2FBAD032F1, 0000000000000000, BDAAF968EF1C596D
// program: F015ECC7723E6782, 0000000000000000, BDAAF968EF1C596D
// program: DB65EA2FBAD032F1, 0000000000000000, B39989DA976A047B
// program: 1A6183C6E984248F, 0000000000000000, 23979FDE0F062640
// program: 090F6261D7EE040A, 0000000000000000, F8A6A9F0205E1CDB
// program: 1A6183C6E984248F, 0000000000000000, 5DC7CE8A5C135EF3
// program: EA36FEB391FDE835, 0000000000000000, 60EA8DD3B6BE1CAB
// program: DB65EA2FBAD032F1, 0000000000000000, 60EA8DD3B6BE1CAB
// program: 1A6183C6E984248F, 0000000000000000, 4EE3168B12D38B36
// program: 1A6183C6E984248F, 0000000000000000, DE4294CDD4BBF522
// program: F015ECC7723E6782, 0000000000000000, 59C6A31D83F60C84
// program: DB65EA2FBAD032F1, 0000000000000000, D5A7E4BC0EFB30EB
// program: EA36FEB391FDE835, 0000000000000000, EF2B5A5F794C295D
// program: DB65EA2FBAD032F1, 0000000000000000, EF2B5A5F794C295D
// program: 090F6261D7EE040A, 0000000000000000, 413FBD0E0AC02C6D
// program: EA36FEB391FDE835, 0000000000000000, 7CC3A7921991AF48
// program: DB65EA2FBAD032F1, 0000000000000000, 7CC3A7921991AF48
// program: 1A6183C6E984248F, 0000000000000000, A381EAFFDDDEA8A1
// program: EA36FEB391FDE835, 0000000000000000, 59C6A31D83F60C84
// program: DB65EA2FBAD032F1, 0000000000000000, 59C6A31D83F60C84
// program: EA36FEB391FDE835, 0000000000000000, 29535BDB1427D02E
// program: 090F6261D7EE040A, 0000000000000000, F1DFE006DD258F65
// program: DB65EA2FBAD032F1, 0000000000000000, 29535BDB1427D02E
// program: AE17434EB03F29DD, 0000000000000000, 98B99EDA3AC776B1
// program: 1A6183C6E984248F, 0000000000000000, 9B31DF4E7C757612
// program: 1A6183C6E984248F, 0000000000000000, 5F833221AEE441E6
// program: 61A82C16044CBE52, 0000000000000000, 9B31DF4E7C757612
// program: 61A82C16044CBE52, 0000000000000000, 9DF1844F38382C3E
// program: 61A82C16044CBE52, 0000000000000000, C51586D9519191BA
// program: 1A6183C6E984248F, 0000000000000000, 9DF1844F38382C3E
// program: 1A6183C6E984248F, 0000000000000000, C51586D9519191BA
// program: 82498C00693767D6, 0000000000000000, FD5277BE4F0218B1
// program: 528A088158F377D7, 0000000000000000, 6336FCCFBCA46F36
// program: A850295C2F806452, 0000000000000000, B4170363C2A69641
// program: 17FE472057E5AA57, 0000000000000000, B4170363C2A69641
// program: 61A82C16044CBE52, 0000000000000000, 68379D7C8FB23E36
// program: 61A82C16044CBE52, 0000000000000000, 5F833221AEE441E6
// program: EA36FEB391FDE835, 0000000000000000, DD00AE9C683C8CF4
// program: 1A6183C6E984248F, 0000000000000000, 68379D7C8FB23E36
// program: A850295C2F806452, 0000000000000000, F935CDD1A6992EF1
// program: 1A6183C6E984248F, 0000000000000000, F814E23FA052DC08
// program: 1A6183C6E984248F, 0000000000000000, EF8D9B9E29D5DF68
// program: 1A6183C6E984248F, 0000000000000000, ACD5947A1CB25EC5
// program: 61A82C16044CBE52, 0000000000000000, EF8D9B9E29D5DF68
// program: 61A82C16044CBE52, 0000000000000000, ACD5947A1CB25EC5
// program: 1A6183C6E984248F, 0000000000000000, B4AEE3D1952324F0
// program: 82498C00693767D6, 0000000000000000, 750C09A44B34AB21
// program: 528A088158F377D7, 0000000000000000, D8E9E79C78EFB0BA
// program: 2E393891BC8F259B, 0000000000000000, A4B61A91080C2C1E
// program: EA36FEB391FDE835, 0000000000000000, AA09CD166B10E237
// program: DB65EA2FBAD032F1, 0000000000000000, AA09CD166B10E237
// program: EA36FEB391FDE835, 0000000000000000, D61291CC9F431770
// program: DB65EA2FBAD032F1, 0000000000000000, D61291CC9F431770
// program: EA36FEB391FDE835, 0000000000000000, A4F2A6A29454F3C2
// program: DB65EA2FBAD032F1, 0000000000000000, A4F2A6A29454F3C2
// program: 1A6183C6E984248F, 0000000000000000, 28EEF914E13040DF
// program: 1A6183C6E984248F, 0000000000000000, C9AF16FEFC3AD4CE
// program: 1A6183C6E984248F, 0000000000000000, 7D57B54F81A0AFFD
// program: 61A82C16044CBE52, 0000000000000000, C9AF16FEFC3AD4CE
// program: 61A82C16044CBE52, 0000000000000000, 7D57B54F81A0AFFD
// program: 1A6183C6E984248F, 0000000000000000, 428E0A43CB4FFD75
// program: 528A088158F377D7, 0000000000000000, 1C7C13022829074A
// program: 2E393891BC8F259B, 0000000000000000, 084C260CDA2D8394
// program: EA36FEB391FDE835, 0000000000000000, 154DF525B4BBBAD5
// program: 090F6261D7EE040A, 0000000000000000, 29EEAD1A0DF0AC9F
// program: DB65EA2FBAD032F1, 0000000000000000, 154DF525B4BBBAD5
// program: 61A82C16044CBE52, 0000000000000000, BC6B3727E713A158
// program: 61A82C16044CBE52, 0000000000000000, 1BD4C152E73EBD38
// program: 61A82C16044CBE52, 0000000000000000, A59DF2A44E9856BB
// program: AE17434EB03F29DD, 0000000000000000, 20C2766149E86370
// program: 1A6183C6E984248F, 0000000000000000, BC6B3727E713A158
// program: 1A6183C6E984248F, 0000000000000000, FBD507B70AC4D902
// program: 61A82C16044CBE52, 0000000000000000, FE8D017C697AE2E6
// program: 61A82C16044CBE52, 0000000000000000, FBD507B70AC4D902
// program: 1A6183C6E984248F, 0000000000000000, 1BD4C152E73EBD38
// program: 1A6183C6E984248F, 0000000000000000, A59DF2A44E9856BB
// program: 528A088158F377D7, 0000000000000000, 468B0250A1E3CCEC
// program: 82498C00693767D6, 0000000000000000, F1DF1404B39DB8F0
// program: 090F6261D7EE040A, 0000000000000000, 809FD9DC1DC4E80E
// program: DB65EA2FBAD032F1, 0000000000000000, D8E9FA7860070685
// program: 61A82C16044CBE52, 0000000000000000, 28EEF914E13040DF
// program: 61A82C16044CBE52, 0000000000000000, C3BE0E8211B57787
// program: EA36FEB391FDE835, 0000000000000000, D8E9FA7860070685
// program: 61A82C16044CBE52, 0000000000000000, 4A53C178DA180688
// program: 528A088158F377D7, 0000000000000000, F1316143BEE6133E
// program: 1A6183C6E984248F, 0000000000000000, B213DBEA703F4DA9
// program: 1A6183C6E984248F, 0000000000000000, 1B1F65E69DCC2CF2
// program: 61A82C16044CBE52, 0000000000000000, B742B64B8C53AEB0
// program: AE17434EB03F29DD, 0000000000000000, C3C007EE6FD8652F
// program: 1A6183C6E984248F, 0000000000000000, D79649B76453A743
// program: 1A6183C6E984248F, 0000000000000000, 06616C9A824872BA
// program: 61A82C16044CBE52, 0000000000000000, F948BB19702BB629
// program: 61A82C16044CBE52, 0000000000000000, 06616C9A824872BA
// program: 1A6183C6E984248F, 0000000000000000, B742B64B8C53AEB0
// program: 1A6183C6E984248F, 0000000000000000, 7DA4BCB1A77FB6DC
// program: 528A088158F377D7, 0000000000000000, F0279C0DF80A4653
// program: 1A6183C6E984248F, 0000000000000000, E6DEB3A176267503
// program: EA36FEB391FDE835, 0000000000000000, A3F529DB52C59A0E
// program: 090F6261D7EE040A, 0000000000000000, D6BBCC3A8262DBC8
// program: DB65EA2FBAD032F1, 0000000000000000, A3F529DB52C59A0E
// program: 1A6183C6E984248F, 0000000000000000, AFC86AA02A918360
// program: 82498C00693767D6, 0000000000000000, 1A5525F30AD021DD
// program: DB65EA2FBAD032F1, 0000000000000000, C4DAB03A05DD8D09
// program: 1A6183C6E984248F, 0000000000000000, 0520A7DC982FAF89
// program: 1A6183C6E984248F, 0000000000000000, EA7B1232A923D447
// program: 61A82C16044CBE52, 0000000000000000, EA7B1232A923D447
// program: 61A82C16044CBE52, 0000000000000000, 0520A7DC982FAF89
// program: 1A6183C6E984248F, 0000000000000000, 94E2DFF8299B31E1
// program: 528A088158F377D7, 0000000000000000, FCD8885AA985C9BF
// program: 2E393891BC8F259B, 0000000000000000, DFA17D482E0EE59D
// program: 1A6183C6E984248F, 0000000000000000, 3A434AA5FE25A8E8
// program: 090F6261D7EE040A, 0000000000000000, 79B777ACC885CB3E
// program: 61A82C16044CBE52, 0000000000000000, 1495438F6FF2E6D9
// program: 1A6183C6E984248F, 0000000000000000, 96FCC944DA7E7893
// program: EA36FEB391FDE835, 0000000000000000, 04418BF3256A57E0
// program: DB65EA2FBAD032F1, 0000000000000000, 04418BF3256A57E0
// program: 1A6183C6E984248F, 0000000000000000, 1495438F6FF2E6D9
// program: 1A6183C6E984248F, 0000000000000000, A59040C48B828489
// program: 61A82C16044CBE52, 0000000000000000, A59040C48B828489
// program: 82498C00693767D6, 0000000000000000, 9FAD25E4FED9BF8F
// program: 528A088158F377D7, 0000000000000000, 28F58812BB553227
// program: 2E393891BC8F259B, 0000000000000000, CA2CCE4B20904568
// program: EA36FEB391FDE835, 0000000000000000, 21448F7CBF0DF42C
// program: DB65EA2FBAD032F1, 0000000000000000, 21448F7CBF0DF42C
// program: 61A82C16044CBE52, 0000000000000000, AFC86AA02A918360
// program: 1A6183C6E984248F, 0000000000000000, 0EC61424FADEBEFA
// program: EA36FEB391FDE835, 0000000000000000, 3D4478AC2C6636D3
// program: 61A82C16044CBE52, 0000000000000000, 0EC61424FADEBEFA
// program: DB65EA2FBAD032F1, 0000000000000000, 3D4478AC2C6636D3
// program: 61A82C16044CBE52, 0000000000000000, 57EE5E6FFCBD85AD
// program: AE17434EB03F29DD, 0000000000000000, 931285EA604BD7D2
// program: AE17434EB03F29DD, 0000000000000000, CFC38AF4F54CEC7F
// program: EA36FEB391FDE835, 0000000000000000, 8E92B73E90BC849C
// program: DB65EA2FBAD032F1, 0000000000000000, 8E92B73E90BC849C
// program: 090F6261D7EE040A, 0000000000000000, 332875D8C7390273
// program: 61A82C16044CBE52, 0000000000000000, D79649B76453A743
// program: 61A82C16044CBE52, 0000000000000000, 7DA4BCB1A77FB6DC
// program: EA36FEB391FDE835, 0000000000000000, 38C01D22E4BDAB00
// program: 090F6261D7EE040A, 0000000000000000, C179CB94438DB62D
// program: DB65EA2FBAD032F1, 0000000000000000, 38C01D22E4BDAB00
// program: 1A6183C6E984248F, 0000000000000000, 943CD26FF795D9AE
// program: 61A82C16044CBE52, 0000000000000000, 943CD26FF795D9AE
// program: 61A82C16044CBE52, 0000000000000000, 7907E4A3178DE13B
// program: 61A82C16044CBE52, 0000000000000000, 08DC1E20BC79355C
// program: 61A82C16044CBE52, 0000000000000000, 9C7B88003532B9B6
// program: 61A82C16044CBE52, 0000000000000000, 83A50C25C992E795
// program: 1A6183C6E984248F, 0000000000000000, 9C7B88003532B9B6
// program: 1A6183C6E984248F, 0000000000000000, 83A50C25C992E795
// program: 528A088158F377D7, 0000000000000000, C52FC3BB23F6A8FD
// program: AE17434EB03F29DD, 0000000000000000, 8CB4F3D4B925F978
// program: 1A6183C6E984248F, 0000000000000000, 08DC1E20BC79355C
// program: EA36FEB391FDE835, 0000000000000000, B2AD4961E977EAF1
// program: 090F6261D7EE040A, 0000000000000000, DA48A50CF1E97BE6
// program: DB65EA2FBAD032F1, 0000000000000000, B2AD4961E977EAF1
// program: 1A6183C6E984248F, 0000000000000000, E45CA51B5904E8BE
// program: 61A82C16044CBE52, 0000000000000000, 4B77215561510279
// program: 82498C00693767D6, 0000000000000000, 29D346F38E01A349
// program: 1A6183C6E984248F, 0000000000000000, AEED1D8629374311
// program: 1A6183C6E984248F, 0000000000000000, D56071BEC34529F6
// program: 61A82C16044CBE52, 0000000000000000, D56071BEC34529F6
// program: 61A82C16044CBE52, 0000000000000000, AEED1D8629374311
// program: 61A82C16044CBE52, 0000000000000000, E45CA51B5904E8BE
// program: 61A82C16044CBE52, 0000000000000000, 9951E8E82C8CAC7B
// program: 1A6183C6E984248F, 0000000000000000, 4B77215561510279
// program: 1A6183C6E984248F, 0000000000000000, 9951E8E82C8CAC7B
// program: 528A088158F377D7, 0000000000000000, D7302F7E37411493
// program: EA36FEB391FDE835, 0000000000000000, 11DB823D01D566BA
// program: 1A6183C6E984248F, 0000000000000000, 024AEAB5C6855642
// program: 1A6183C6E984248F, 0000000000000000, 9430D917EC40878C
// program: 1A6183C6E984248F, 0000000000000000, 2CB04DE5D9783F12
// program: 61A82C16044CBE52, 0000000000000000, 2CB04DE5D9783F12
// program: 61A82C16044CBE52, 0000000000000000, 024AEAB5C6855642
// program: 61A82C16044CBE52, 0000000000000000, 9430D917EC40878C
// program: 61A82C16044CBE52, 0000000000000000, 1DFEA5C823778DB7
// program: 1A6183C6E984248F, 0000000000000000, 9B547CA640AF8B12
// program: 82498C00693767D6, 0000000000000000, BA551FE21EBA7A41
// program: 2E393891BC8F259B, 0000000000000000, 4C9E726EF236C3B8
// program: EA36FEB391FDE835, 0000000000000000, C5F3811EEAF6890B
// program: DB65EA2FBAD032F1, 0000000000000000, C5F3811EEAF6890B
// program: EA36FEB391FDE835, 0000000000000000, 80725873B5F358E1
// program: DB65EA2FBAD032F1, 0000000000000000, 80725873B5F358E1
// program: 1A6183C6E984248F, 0000000000000000, 1DFEA5C823778DB7
// program: 528A088158F377D7, 0000000000000000, 456E36DAC51B5749
// program: 090F6261D7EE040A, 0000000000000000, D1DF920DA88CC46A
// program: DEF016CB05491BB4, 0000000000000000, B4170363C2A69641
// program: F015ECC7723E6782, 0000000000000000, 80725873B5F358E1
// program: 31884F9ACC9BB5B8, 0000000000000000, 7E48C3120BCCBD19
// program: 32E7001A586B3B1D, 0000000000000000, 7E48C3120BCCBD19
// program: EA36FEB391FDE835, 0000000000000000, FACB327D23073768
// program: 090F6261D7EE040A, 0000000000000000, F223F351E532AE2B
// program: DB65EA2FBAD032F1, 0000000000000000, FACB327D23073768
// program: 61A82C16044CBE52, 0000000000000000, F32082337A14C8D0
// program: 61A82C16044CBE52, 0000000000000000, 43A593E04DCE465C
// program: 61A82C16044CBE52, 0000000000000000, C036C7E40938EF30
// program: 82498C00693767D6, 0000000000000000, C19F695D82A9C593
// program: 1A6183C6E984248F, 0000000000000000, 43A593E04DCE465C
// program: 1A6183C6E984248F, 0000000000000000, C036C7E40938EF30
// program: 1A6183C6E984248F, 0000000000000000, F32082337A14C8D0
// program: 61A82C16044CBE52, 0000000000000000, AF3AED0565E94454
// program: 1A6183C6E984248F, 0000000000000000, 1102F1F824CB480A
// program: 1A6183C6E984248F, 0000000000000000, AF3AED0565E94454
// program: 528A088158F377D7, 0000000000000000, C2D4BB4EEB2ADDA7
// program: F015ECC7723E6782, 0000000000000000, DEC7917316B4C52E
// program: DB65EA2FBAD032F1, 0000000000000000, DEC7917316B4C52E
// program: 2E393891BC8F259B, 0000000000000000, 8B1BF48B59DEFA88
// program: AE17434EB03F29DD, 0000000000000000, 2A3940C338157CDF
// program: 7642D41FDAB29524, 0000000000000000, F11B937C0C4A661B
// program: 7642D41FDAB29524, 0000000000000000, 680AD87DDC7E44E1
// program: 619E562CDB89F275, 0000000000000000, 1A07AFF423D75A41
// program: F015ECC7723E6782, 0000000000000000, FBCE9E78C807B463
// program: DB65EA2FBAD032F1, 0000000000000000, FBCE9E78C807B463
// program: F015ECC7723E6782, 0000000000000000, 09F666AF13FD31A7
// program: DB65EA2FBAD032F1, 0000000000000000, 09F666AF13FD31A7
// program: F015ECC7723E6782, 0000000000000000, DD00AE9C683C8CF4
// program: DB65EA2FBAD032F1, 0000000000000000, DD00AE9C683C8CF4
// program: AE17434EB03F29DD, 0000000000000000, 322AAC6867732982
// program: EA36FEB391FDE835, 0000000000000000, 001CB85ED1D72B72
// program: 090F6261D7EE040A, 0000000000000000, ACABC9428B29917B
// program: DB65EA2FBAD032F1, 0000000000000000, 001CB85ED1D72B72
// program: 1A6183C6E984248F, 0000000000000000, 2C4255E482B7EA6E
// program: 1A6183C6E984248F, 0000000000000000, FAB37C66F14E9845
// program: 61A82C16044CBE52, 0000000000000000, A10D8869A98622A7
// program: 61A82C16044CBE52, 0000000000000000, 2C4255E482B7EA6E
// program: 61A82C16044CBE52, 0000000000000000, 63ECF0BCAE76FAFE
// program: 82498C00693767D6, 0000000000000000, 8040A15D3D695FB7
// program: 528A088158F377D7, 0000000000000000, F37806C91BC80178
// program: AE17434EB03F29DD, 0000000000000000, C1DF8B201BCF7ACB
// program: 61A82C16044CBE52, 0000000000000000, FAB37C66F14E9845
// program: 61A82C16044CBE52, 0000000000000000, 2BB7818AF0FD3F8C
// program: 1A6183C6E984248F, 0000000000000000, 63ECF0BCAE76FAFE
// program: 1A6183C6E984248F, 0000000000000000, 2BB7818AF0FD3F8C
// program: EA36FEB391FDE835, 0000000000000000, 7C07E4842584DE35
// program: 7642D41FDAB29524, 0000000000000000, 3FE17C5D2ECA15EE
// program: 1A6183C6E984248F, 0000000000000000, A10D8869A98622A7
// program: EA36FEB391FDE835, 0000000000000000, 2D41AFC161647E3F
// program: 090F6261D7EE040A, 0000000000000000, 4BEA5E101089BDF3
// program: DB65EA2FBAD032F1, 0000000000000000, 2D41AFC161647E3F
// program: 1A6183C6E984248F, 0000000000000000, 8F1AF2680453B77B
// program: 61A82C16044CBE52, 0000000000000000, CA79CC22AC305429
// program: 61A82C16044CBE52, 0000000000000000, 89077029751166C5
// program: 61A82C16044CBE52, 0000000000000000, B4688894E0FAC49F
// program: 1A6183C6E984248F, 0000000000000000, 9A6879E511722ED7
// program: 1A6183C6E984248F, 0000000000000000, B4688894E0FAC49F
// program: 528A088158F377D7, 0000000000000000, E4B179E03F57C2BB
// program: 1A6183C6E984248F, 0000000000000000, 89077029751166C5
// program: 61A82C16044CBE52, 0000000000000000, 8F1AF2680453B77B
// program: 61A82C16044CBE52, 0000000000000000, 9A6879E511722ED7
// program: EA36FEB391FDE835, 0000000000000000, 515AF31B95378B78
// program: AE17434EB03F29DD, 0000000000000000, 7D33C90A66DA7F14
// program: EA36FEB391FDE835, 0000000000000000, 93A6CB48F0DFD045
// program: 090F6261D7EE040A, 0000000000000000, F3492EF3764FEACB
// program: DB65EA2FBAD032F1, 0000000000000000, 93A6CB48F0DFD045
// program: AE17434EB03F29DD, 0000000000000000, 8290CCEFF4ABA1AF
// program: 1A6183C6E984248F, 0000000000000000, 017FCCDFB8716E75
// program: 61A82C16044CBE52, 0000000000000000, EB52FDC99E70BB3A
// program: 61A82C16044CBE52, 0000000000000000, 8C5FD235F0FDF93E
// program: 61A82C16044CBE52, 0000000000000000, 794DA3A3260DB2E4
// program: 1A6183C6E984248F, 0000000000000000, C6F3DD28FA112E9F
// program: 1A6183C6E984248F, 0000000000000000, 794DA3A3260DB2E4
// program: 528A088158F377D7, 0000000000000000, C273F749D6013A6B
// program: EA36FEB391FDE835, 0000000000000000, ED0567956E6E8132
// program: 1A6183C6E984248F, 0000000000000000, 8C5FD235F0FDF93E
// program: 61A82C16044CBE52, 0000000000000000, 017FCCDFB8716E75
// program: 61A82C16044CBE52, 0000000000000000, C6F3DD28FA112E9F
// program: EA36FEB391FDE835, 0000000000000000, 02F32E03CFE4725A
// program: 090F6261D7EE040A, 0000000000000000, C5DFD8D3F4F2331F
// program: DB65EA2FBAD032F1, 0000000000000000, 02F32E03CFE4725A
// program: 61A82C16044CBE52, 0000000000000000, 37E9ECD9B68CE134
// program: AE17434EB03F29DD, 0000000000000000, 89A448C2DB45CF32
// program: 1A6183C6E984248F, 0000000000000000, 49195A0C1A14BD9A
// program: 1A6183C6E984248F, 0000000000000000, 2ECC77E633AAD41E
// program: 61A82C16044CBE52, 0000000000000000, 49195A0C1A14BD9A
// program: 61A82C16044CBE52, 0000000000000000, 40C9669508F55F47
// program: 61A82C16044CBE52, 0000000000000000, 2ECC77E633AAD41E
// program: 61A82C16044CBE52, 0000000000000000, CE02743DEC978C46
// program: 1A6183C6E984248F, 0000000000000000, 37E9ECD9B68CE134
// program: 1A6183C6E984248F, 0000000000000000, CE02743DEC978C46
// program: 528A088158F377D7, 0000000000000000, E895E1170A34F77D
// program: EA36FEB391FDE835, 0000000000000000, 27F62A8C5583D196
// program: EA36FEB391FDE835, 0000000000000000, B63B3E795C19369E
// program: DB65EA2FBAD032F1, 0000000000000000, B63B3E795C19369E
// program: DB65EA2FBAD032F1, 0000000000000000, 27F62A8C5583D196
// program: 090F6261D7EE040A, 0000000000000000, 03AB830E067B163A
// program: 61A82C16044CBE52, 0000000000000000, A78E93E8239708CB
// program: EA36FEB391FDE835, 0000000000000000, CB5E439281C5C7BC
// program: DB65EA2FBAD032F1, 0000000000000000, CB5E439281C5C7BC
// program: AE17434EB03F29DD, 0000000000000000, 3C3869862C2E22C4
// program: 1A6183C6E984248F, 0000000000000000, 3E8CA7CCD022A5AF
// program: 1A6183C6E984248F, 0000000000000000, 0B82D5C9DA19BE96
// program: 61A82C16044CBE52, 0000000000000000, 3E8CA7CCD022A5AF
// program: 61A82C16044CBE52, 0000000000000000, 51F5ED2F405DA13C
// program: 1A6183C6E984248F, 0000000000000000, A78E93E8239708CB
// program: 1A6183C6E984248F, 0000000000000000, 51F5ED2F405DA13C
// program: 528A088158F377D7, 0000000000000000, AD84118C72ED5E6B
// program: DB65EA2FBAD032F1, 0000000000000000, 11DB823D01D566BA
// program: F015ECC7723E6782, 0000000000000000, 11DB823D01D566BA
// program: DB65EA2FBAD032F1, 0000000000000000, 6D6618E879297E31
// program: 17FE472057E5AA57, 0000000000000000, F935CDD1A6992EF1
// program: EA36FEB391FDE835, 0000000000000000, 4D019CDC7C97A812
// program: DB65EA2FBAD032F1, 0000000000000000, 4D019CDC7C97A812
// program: 1A6183C6E984248F, 0000000000000000, 8851387C00535DF3
// program: 1A6183C6E984248F, 0000000000000000, 9A580F61A565C482
// program: 61A82C16044CBE52, 0000000000000000, 18167A8F60EB97B8
// program: 1A6183C6E984248F, 0000000000000000, 18167A8F60EB97B8
// program: 1A6183C6E984248F, 0000000000000000, 7D7404D4681D7A43
// program: 82498C00693767D6, 0000000000000000, A1C5D0AE56010655
// program: 528A088158F377D7, 0000000000000000, 496B286FC08240B7
// program: 2E393891BC8F259B, 0000000000000000, 0A67ABED75033327
// program: 82498C00693767D6, 0000000000000000, D828A5822DD9B34E
// program: 090F6261D7EE040A, 0000000000000000, 6EF564A951F5C91A
// program: 05E1A4F0384EBF7F, 0000000000000000, 5AB7315286C48B40
// program: EA36FEB391FDE835, 0000000000000000, D7663F105992AC9D
// program: EA36FEB391FDE835, 0000000000000000, CFEBBE0C520880BB
// program: 090F6261D7EE040A, 0000000000000000, D75EEFD3473DC091
// program: DB65EA2FBAD032F1, 0000000000000000, CFEBBE0C520880BB
// program: 1A6183C6E984248F, 0000000000000000, 75C07F8885F3BA19
// program: 1A6183C6E984248F, 0000000000000000, 4B4CCF8C7068368F
// program: 61A82C16044CBE52, 0000000000000000, 54FB367DDAA96CDC
// program: 1A6183C6E984248F, 0000000000000000, 54FB367DDAA96CDC
// program: 1A6183C6E984248F, 0000000000000000, 25D89B17AFEAC1E2
// program: 82498C00693767D6, 0000000000000000, FA42D8B00C8D6149
// program: 528A088158F377D7, 0000000000000000, 370C64AC7DC58D93
// program: 2E393891BC8F259B, 0000000000000000, 051130508E7E4420
// program: 1A6183C6E984248F, 0000000000000000, 3AA3B1383D59987F
// program: 83C36A4A2949D5E1, 0000000000000000, 771030A3EE84D48A
// program: DB65EA2FBAD032F1, 0000000000000000, D7663F105992AC9D
// program: EA36FEB391FDE835, 0000000000000000, B14812D1CCB9D1CC
// program: EA36FEB391FDE835, 0000000000000000, 9D3C9A74276E0E8E
// program: 090F6261D7EE040A, 0000000000000000, 870E0276D5876E0A
// program: DB65EA2FBAD032F1, 0000000000000000, 9D3C9A74276E0E8E
// program: 1A6183C6E984248F, 0000000000000000, 28FABD6F85F3BA19
// program: 1A6183C6E984248F, 0000000000000000, 4B4CCF8CC16FFA3C
// program: 61A82C16044CBE52, 0000000000000000, 54FB367D6BAEA06F
// program: 1A6183C6E984248F, 0000000000000000, 54FB367D6BAEA06F
// program: 1A6183C6E984248F, 0000000000000000, 25D89B17368C94B0
// program: 82498C00693767D6, 0000000000000000, 4B4514030C8D6149
// program: 528A088158F377D7, 0000000000000000, 860BA81F7DC58D93
// program: 2E393891BC8F259B, 0000000000000000, 3D4AB8A0B43C66F7
// program: EA36FEB391FDE835, 0000000000000000, DB57D23A7E8575A3
// program: DB65EA2FBAD032F1, 0000000000000000, DB57D23A7E8575A3
// program: 1A6183C6E984248F, 0000000000000000, 2B1A35507E2E8C9C
// program: 61A82C16044CBE52, 0000000000000000, 49333D4304DB6E4B
// program: 1A6183C6E984248F, 0000000000000000, 8BF9C4A1E3B6C57E
// program: 528A088158F377D7, 0000000000000000, 114E27096CF825E1
// program: 1A6183C6E984248F, 0000000000000000, 49333D4304DB6E4B
// program: 61A82C16044CBE52, 0000000000000000, 962EFE6335C46664
// program: 61A82C16044CBE52, 0000000000000000, 91EAA257DB033B4E
// program: AE17434EB03F29DD, 0000000000000000, 52B6BB1A7D1CB5D3
// program: 090F6261D7EE040A, 0000000000000000, 073DC5398D2F8209
// program: 61A82C16044CBE52, 0000000000000000, 428E0A43CB4FFD75
// program: 1A6183C6E984248F, 0000000000000000, C3BE0E8211B57787
// program: 61A82C16044CBE52, 0000000000000000, B4AEE3D1952324F0
// program: 1A6183C6E984248F, 0000000000000000, 1D94191E4341D0F2
// program: EA36FEB391FDE835, 0000000000000000, 8710D1DCC507BC75
// program: 090F6261D7EE040A, 0000000000000000, 1699353C9B03B4A8
// program: DB65EA2FBAD032F1, 0000000000000000, 8710D1DCC507BC75
// program: AE17434EB03F29DD, 0000000000000000, E3F0A9539D8C7C4F
// program: 1A6183C6E984248F, 0000000000000000, 834317A99C6DA784
// program: 61A82C16044CBE52, 0000000000000000, AFFC956955E56F89
// program: 61A82C16044CBE52, 0000000000000000, 548717A829039DB6
// program: 61A82C16044CBE52, 0000000000000000, 8BD601A5A8704438
// program: 528A088158F377D7, 0000000000000000, 8A5080EA98922B67
// program: 1A6183C6E984248F, 0000000000000000, 548717A829039DB6
// program: 61A82C16044CBE52, 0000000000000000, 834317A99C6DA784
// program: 61A82C16044CBE52, 0000000000000000, D50C55D4F07450BB
// program: 1A6183C6E984248F, 0000000000000000, 8BD601A5A8704438
// program: 1A6183C6E984248F, 0000000000000000, D50C55D4F07450BB
// program: A850295C2F806452, 0000000000000000, 03B41249843C92F4
// program: 05E1A4F0384EBF7F, 0000000000000000, AE43ACCC97EB4103
// program: EA36FEB391FDE835, 0000000000000000, F8C2226608C9164D
// program: 1A6183C6E984248F, 0000000000000000, AFFC956955E56F89
// program: E7691288707F7809, 0000000000000000, 662A5EAC86BB9B96
// program: 2E393891BC8F259B, 0000000000000000, E524B97BF75C82AB
// program: EA36FEB391FDE835, 0000000000000000, 292F2CC68E8E5747
// program: 090F6261D7EE040A, 0000000000000000, D7EA1E3C15B273E2
// program: DB65EA2FBAD032F1, 0000000000000000, 292F2CC68E8E5747
// program: 1A6183C6E984248F, 0000000000000000, BA8F8FE399851AC9
// program: 1A6183C6E984248F, 0000000000000000, 35BB737C3ABE29A9
// program: 1A6183C6E984248F, 0000000000000000, 92FE783EFAFE8EAC
// program: 61A82C16044CBE52, 0000000000000000, 35BB737C3ABE29A9
// program: 61A82C16044CBE52, 0000000000000000, 92FE783EFAFE8EAC
// program: 61A82C16044CBE52, 0000000000000000, BA8F8FE399851AC9
// program: 61A82C16044CBE52, 0000000000000000, 3915A77639209D51
// program: 82498C00693767D6, 0000000000000000, 555682888EB1EAE2
// program: A2B1C50622C4E336, 0000000000000000, DF345E9AD55C45CD
// program: DB65EA2FBAD032F1, 0000000000000000, 6C7DFAF7B803D79E
// program: 1A6183C6E984248F, 0000000000000000, 6C8E8ADFEC70D581
// program: 1A6183C6E984248F, 0000000000000000, 3915A77639209D51
// program: 1A6183C6E984248F, 0000000000000000, 8F45903288B91F9E
// program: 528A088158F377D7, 0000000000000000, 15DDDC59351D711D
// program: 2E393891BC8F259B, 0000000000000000, 5A3E668AC796F6C6
// program: EA36FEB391FDE835, 0000000000000000, A7255194768E81F9
// program: 090F6261D7EE040A, 0000000000000000, A30328DE8C3E6F36
// program: DB65EA2FBAD032F1, 0000000000000000, A7255194768E81F9
// program: 1A6183C6E984248F, 0000000000000000, 7DD56F6A7907270D
// program: 1A6183C6E984248F, 0000000000000000, C5EC31AEC4FAC163
// program: 1A6183C6E984248F, 0000000000000000, 21FD17261E430A75
// program: 1A6183C6E984248F, 0000000000000000, 8E0C0C525145D199
// program: A2B1C50622C4E336, 0000000000000000, 4484A1D5E64E0DE7
// program: DB65EA2FBAD032F1, 0000000000000000, CED6F03C1733B8AC
// program: 528A088158F377D7, 0000000000000000, BABD66045B0A5603
// program: 61A82C16044CBE52, 0000000000000000, C5EC31AEC4FAC163
// program: 61A82C16044CBE52, 0000000000000000, 21FD17261E430A75
// program: EA36FEB391FDE835, 0000000000000000, 27D4FDED4572CFBB
// program: EA36FEB391FDE835, 0000000000000000, D060D6AA74463537
// program: 090F6261D7EE040A, 0000000000000000, 4C9555D8DD69C654
// program: DB65EA2FBAD032F1, 0000000000000000, 1A2946A3B6F713EA
// program: 1A6183C6E984248F, 0000000000000000, BF815A1972014F87
// program: 61A82C16044CBE52, 0000000000000000, BF815A1972014F87
// program: 1A6183C6E984248F, 0000000000000000, CB5AAE515ECAE8A6
// program: 1A6183C6E984248F, 0000000000000000, E6B86413F3C2E61E
// program: 82498C00693767D6, 0000000000000000, BD674623A62D59C7
// program: DB65EA2FBAD032F1, 0000000000000000, 5F7B9092807A9333
// program: 528A088158F377D7, 0000000000000000, CF65C257682D6468
// program: 1A6183C6E984248F, 0000000000000000, 6F64A103CAC41B04
// program: 1A6183C6E984248F, 0000000000000000, 149A9327C60CA02C
// program: 61A82C16044CBE52, 0000000000000000, 149A9327C60CA02C
// program: 61A82C16044CBE52, 0000000000000000, CB5AAE515ECAE8A6
// program: A2B1C50622C4E336, 0000000000000000, ACC200A0F35FCB81
// program: 83C36A4A2949D5E1, 0000000000000000, 67008C4C43447CEB
// program: 83C36A4A2949D5E1, 0000000000000000, 2C86B6173DA65F61
// program: EA36FEB391FDE835, 0000000000000000, 32A32206BFD1741F
// program: EA36FEB391FDE835, 0000000000000000, D9B8235EF7485317
// program: 090F6261D7EE040A, 0000000000000000, 6BB432E9EB04BE8F
// program: DB65EA2FBAD032F1, 0000000000000000, D9B8235EF7485317
// program: 1A6183C6E984248F, 0000000000000000, 61A2479486114892
// program: 61A82C16044CBE52, 0000000000000000, 61A2479486114892
// program: 61A82C16044CBE52, 0000000000000000, 6290CAF82BB234D8
// program: 82498C00693767D6, 0000000000000000, 1777D1866E4F85CC
// program: DB65EA2FBAD032F1, 0000000000000000, B04B82F696F56A42
// program: 528A088158F377D7, 0000000000000000, 3B40ADBBD52E0553
// program: 1A6183C6E984248F, 0000000000000000, 02E99D2C9D191AF1
// program: 1A6183C6E984248F, 0000000000000000, 6290CAF82BB234D8
// program: 1A6183C6E984248F, 0000000000000000, EE74B5736FF89CAA
// program: 61A82C16044CBE52, 0000000000000000, 3533C9157D8BB9D0
// program: A2B1C50622C4E336, 0000000000000000, 5ECAD5405E3B1FF2
// program: EA36FEB391FDE835, 0000000000000000, B04B82F696F56A42
// program: 1A6183C6E984248F, 0000000000000000, 3533C9157D8BB9D0
// program: 1A6183C6E984248F, 0000000000000000, 6F6DBFBB101B0ADB
// program: EA36FEB391FDE835, 0000000000000000, 888E790014790C38
// program: 090F6261D7EE040A, 0000000000000000, 05D248F5E6D67189
// program: DB65EA2FBAD032F1, 0000000000000000, CDDCAF3122F48CE1
// program: 1A6183C6E984248F, 0000000000000000, E3FC0AEA5FCE24AE
// program: 1A6183C6E984248F, 0000000000000000, 0735149896C68B68
// program: 61A82C16044CBE52, 0000000000000000, 0735149896C68B68
// program: 61A82C16044CBE52, 0000000000000000, 8EE6EEA02CCA1EC7
// program: 1A6183C6E984248F, 0000000000000000, FCBC42D4C87DB5CA
// program: 1A6183C6E984248F, 0000000000000000, AAACB3D564843C09
// program: 82498C00693767D6, 0000000000000000, 600B57AEA03959ED
// program: A2B1C50622C4E336, 0000000000000000, 094636D9CFE7B325
// program: DB65EA2FBAD032F1, 0000000000000000, 888E790014790C38
// program: 528A088158F377D7, 0000000000000000, 374FCC8C94F3E57E
// program: 1A6183C6E984248F, 0000000000000000, 8EE6EEA02CCA1EC7
// program: 61A82C16044CBE52, 0000000000000000, FCBC42D4C87DB5CA
// program: EA36FEB391FDE835, 0000000000000000, E366BCCF4C3F719A
// program: EA36FEB391FDE835, 0000000000000000, 1FB9B6F635F91859
// program: 090F6261D7EE040A, 0000000000000000, 820FD0D20874ECC2
// program: DB65EA2FBAD032F1, 0000000000000000, 1FB9B6F635F91859
// program: 1A6183C6E984248F, 0000000000000000, 29708F9D58BF3843
// program: 1A6183C6E984248F, 0000000000000000, 5B122C59A056D78B
// program: 61A82C16044CBE52, 0000000000000000, 01D660806356BD82
// program: 82498C00693767D6, 0000000000000000, 3A30DA4208A60C25
// program: DB65EA2FBAD032F1, 0000000000000000, C51CEF1F7924659F
// program: 1A6183C6E984248F, 0000000000000000, 01D660806356BD82
// program: 1A6183C6E984248F, 0000000000000000, F54EE557FF46A1F3
// program: 1A6183C6E984248F, 0000000000000000, C90E614B810E1C6B
// program: 1A6183C6E984248F, 0000000000000000, E7190379370572C5
// program: 528A088158F377D7, 0000000000000000, BD19FA47DEF9CFBE
// program: 2E393891BC8F259B, 0000000000000000, FBF7197EACA2AE44
// program: 528A088158F377D7, 0000000000000000, FFC0E7C325142D8D
// program: EA36FEB391FDE835, 0000000000000000, 72226B8157CE6E90
// program: 090F6261D7EE040A, 0000000000000000, 757296D64688F093
// program: DB65EA2FBAD032F1, 0000000000000000, 72226B8157CE6E90
// program: 1A6183C6E984248F, 0000000000000000, 48A0676D0777AF18
// program: 1A6183C6E984248F, 0000000000000000, 9B543CECF70FDC63
// program: 1A6183C6E984248F, 0000000000000000, 257C6F482E85BEE0
// program: 1A6183C6E984248F, 0000000000000000, 2BB91EBDC458987E
// program: 82498C00693767D6, 0000000000000000, 84B00EAE0A60C158
// program: 528A088158F377D7, 0000000000000000, 43D33802BDDDF071
// program: 1A6183C6E984248F, 0000000000000000, CDB7ECA28F51733F
// program: 61A82C16044CBE52, 0000000000000000, 9B543CECF70FDC63
// program: 1A6183C6E984248F, 0000000000000000, 138A49A848FF1654
// program: 2E393891BC8F259B, 0000000000000000, 079C686D6B38F368
// program: EA36FEB391FDE835, 0000000000000000, 799849542ABBBA64
// program: DB65EA2FBAD032F1, 0000000000000000, 799849542ABBBA64
// program: E7691288707F7809, 0000000000000000, 6E355ACEE1E8FEBD
// program: AE17434EB03F29DD, 0000000000000000, B4C8413697EF26D8
// program: D47C887971654D1D, 0000000000000000, 4C9E726EF236C3B8
// program: E80FD09F14049207, 0000000000000000, 5AB7315286C48B40
// program: E4B1717CEF51482E, 0000000000000000, 665D6B2478FDF251
// program: E4B1717CEF51482E, 0000000000000000, 333EC15450F8FA23
// program: D47C887971654D1D, 0000000000000000, 8B1BF48B59DEFA88
// program: EA36FEB391FDE835, 0000000000000000, EFB9FF95EA5BF4CF
// program: DB65EA2FBAD032F1, 0000000000000000, B14812D1CCB9D1CC
// program: 1A6183C6E984248F, 0000000000000000, 91EAA257DB033B4E
// program: 61A82C16044CBE52, 0000000000000000, 2B1A35507E2E8C9C
// program: 61A82C16044CBE52, 0000000000000000, 8BF9C4A1E3B6C57E
// program: 82498C00693767D6, 0000000000000000, 7D93C85249C334C7
// program: F015ECC7723E6782, 0000000000000000, D7663F105992AC9D
// program: DB65EA2FBAD032F1, 0000000000000000, AE6941B132F4BED2
// program: EA36FEB391FDE835, 0000000000000000, E16CA69F6C9C1295
// program: DB65EA2FBAD032F1, 0000000000000000, E16CA69F6C9C1295
// program: AE17434EB03F29DD, 0000000000000000, AE0327C31D93756B
// program: 1A6183C6E984248F, 0000000000000000, 871105CF820C63B2
// program: 1A6183C6E984248F, 0000000000000000, 84A6DF418793C6A1
// program: 1A6183C6E984248F, 0000000000000000, 9C5AC2F7A0BA0D3E
// program: 61A82C16044CBE52, 0000000000000000, 9C5AC2F7A0BA0D3E
// program: 61A82C16044CBE52, 0000000000000000, 84A6DF418793C6A1
// program: 61A82C16044CBE52, 0000000000000000, 871105CF820C63B2
// program: 61A82C16044CBE52, 0000000000000000, 763323870D554424
// program: E4B1717CEF51482E, 0000000000000000, E34B9BD15549161B
// program: 1A6183C6E984248F, 0000000000000000, 7162C2AA5E2244CB
// program: 1A6183C6E984248F, 0000000000000000, 763323870D554424
// program: 528A088158F377D7, 0000000000000000, 37BB3C5A7DB87D7A
// program: 77B41F440BB8269F, 0000000000000000, E34B9BD15549161B
// program: 82498C00693767D6, 0000000000000000, 6CC786040C326E32
// program: 83C36A4A2949D5E1, 0000000000000000, 1A07AFF423D75A41
// program: 83C36A4A2949D5E1, 0000000000000000, 668CD4052C0EF276
// program: EA36FEB391FDE835, 0000000000000000, 09CE2AD4CFEAD9C9
// program: DB65EA2FBAD032F1, 0000000000000000, 09CE2AD4CFEAD9C9
// program: EA36FEB391FDE835, 0000000000000000, 1BB21AFF1EE80666
// program: DB65EA2FBAD032F1, 0000000000000000, 1BB21AFF1EE80666
// program: AE17434EB03F29DD, 0000000000000000, 225ECED3E1C4AA08
// program: 1A6183C6E984248F, 0000000000000000, 3E8EE698CF4629CD
// program: 1A6183C6E984248F, 0000000000000000, AE5DFCC82BEF25A8
// program: 1A6183C6E984248F, 0000000000000000, 5AA1ED401D8EE355
// program: 61A82C16044CBE52, 0000000000000000, 5AA1ED401D8EE355
// program: 61A82C16044CBE52, 0000000000000000, AE5DFCC82BEF25A8
// program: 61A82C16044CBE52, 0000000000000000, 3E8EE698CF4629CD
// program: 61A82C16044CBE52, 0000000000000000, 6B6E4A8EF9B7DAEC
// program: E4B1717CEF51482E, 0000000000000000, 1339CA1AC2E7935A
// program: 1A6183C6E984248F, 0000000000000000, E60A4946F232A1B7
// program: 1A6183C6E984248F, 0000000000000000, 6B6E4A8EF9B7DAEC
// program: 528A088158F377D7, 0000000000000000, FBB495C0B05A0951
// program: 77B41F440BB8269F, 0000000000000000, 1339CA1AC2E7935A
// program: 82498C00693767D6, 0000000000000000, A19EED6FB585A97F
// program: 83C36A4A2949D5E1, 0000000000000000, 4ADABABBD6734FD2
// program: EA36FEB391FDE835, 0000000000000000, 67A94625EABBF321
// program: DB65EA2FBAD032F1, 0000000000000000, 67A94625EABBF321
// program: EA36FEB391FDE835, 0000000000000000, 48473B67B9291A0E
// program: 090F6261D7EE040A, 0000000000000000, E8E72786B610A479
// program: EA36FEB391FDE835, 0000000000000000, 82E75DE03E272DC6
// program: 090F6261D7EE040A, 0000000000000000, 5907EEE02B1B2CDC
// program: DB65EA2FBAD032F1, 0000000000000000, 6A45D1AB9D51E69A
// program: 1A6183C6E984248F, 0000000000000000, 251B28F68D817C9B
// program: 1A6183C6E984248F, 0000000000000000, B26DD556EF4865DC
// program: 1A6183C6E984248F, 0000000000000000, A797D69DF99E7B62
// program: 61A82C16044CBE52, 0000000000000000, A797D69DF99E7B62
// program: 61A82C16044CBE52, 0000000000000000, B26DD556EF4865DC
// program: 61A82C16044CBE52, 0000000000000000, 251B28F68D817C9B
// program: 61A82C16044CBE52, 0000000000000000, D1FCD60A320A5190
// program: E4B1717CEF51482E, 0000000000000000, 0F77FB246A493A03
// program: 1A6183C6E984248F, 0000000000000000, 505D1F6822E12FBF
// program: 77B41F440BB8269F, 0000000000000000, 0F77FB246A493A03
// program: 82498C00693767D6, 0000000000000000, DA2D7E3A252B29F1
// program: 83C36A4A2949D5E1, 0000000000000000, 4AD845688008C26D
// program: EA36FEB391FDE835, 0000000000000000, 6A45D1AB9D51E69A
// program: 1A6183C6E984248F, 0000000000000000, D1FCD60A320A5190
// program: EA36FEB391FDE835, 0000000000000000, 42D421B50B7BFAD1
// program: 83C36A4A2949D5E1, 0000000000000000, FFC69F8F8D70E682
// program: 83C36A4A2949D5E1, 0000000000000000, 9B469114F3FF6691
// program: AE17434EB03F29DD, 0000000000000000, 60DE419C8CA00D8A
// program: DB65EA2FBAD032F1, 0000000000000000, 42D421B50B7BFAD1
// program: DB65EA2FBAD032F1, 0000000000000000, 82E75DE03E272DC6
// program: EA36FEB391FDE835, 0000000000000000, 0A17B99BEC8980E4
// program: 090F6261D7EE040A, 0000000000000000, 8ECF666E441EF81A
// program: DB65EA2FBAD032F1, 0000000000000000, 0A17B99BEC8980E4
// program: 1A6183C6E984248F, 0000000000000000, 2E7F4E1D616CE909
// program: 1A6183C6E984248F, 0000000000000000, BFE25938FDC48580
// program: 61A82C16044CBE52, 0000000000000000, BFE25938FDC48580
// program: 61A82C16044CBE52, 0000000000000000, 2E7F4E1D616CE909
// program: 61A82C16044CBE52, 0000000000000000, 49C1D54C9BB4AFF0
// program: E4B1717CEF51482E, 0000000000000000, 6DCCD3605F410191
// program: 77B41F440BB8269F, 0000000000000000, 6DCCD3605F410191
// program: 82498C00693767D6, 0000000000000000, 2C5ACB486C86B533
// program: AE17434EB03F29DD, 0000000000000000, 14ED970EEA4028A5
// program: 1A6183C6E984248F, 0000000000000000, E02ABD110B6DBEF4
// program: 61A82C16044CBE52, 0000000000000000, E02ABD110B6DBEF4
// program: 1A6183C6E984248F, 0000000000000000, 25B9C13CF176223D
// program: 1A6183C6E984248F, 0000000000000000, 49C1D54C9BB4AFF0
// program: 83C36A4A2949D5E1, 0000000000000000, 55AC60055A370309
// program: EA36FEB391FDE835, 0000000000000000, 760CE54118DA75A3
// program: EA36FEB391FDE835, 0000000000000000, 270D710301814E2C
// program: 090F6261D7EE040A, 0000000000000000, B7C6A3742A1468F5
// program: DB65EA2FBAD032F1, 0000000000000000, 270D710301814E2C
// program: 1A6183C6E984248F, 0000000000000000, 7D744EA5F2992D58
// program: 61A82C16044CBE52, 0000000000000000, 9F957C1F45B6D246
// program: 61A82C16044CBE52, 0000000000000000, 7D744EA5F2992D58
// program: 61A82C16044CBE52, 0000000000000000, 8740C93E710C8D1C
// program: E4B1717CEF51482E, 0000000000000000, F20EDFB193B8E7EA
// program: 77B41F440BB8269F, 0000000000000000, F20EDFB193B8E7EA
// program: 82498C00693767D6, 0000000000000000, 7AE060FDD9B123E0
// program: 1A6183C6E984248F, 0000000000000000, FABF5C266995752A
// program: 1A6183C6E984248F, 0000000000000000, 9F957C1F45B6D246
// program: 61A82C16044CBE52, 0000000000000000, FABF5C266995752A
// program: 1A6183C6E984248F, 0000000000000000, 3D1D583D2C8E3D9D
// program: 83C36A4A2949D5E1, 0000000000000000, 7EBE3CC6CBF09B2C
// program: EA36FEB391FDE835, 0000000000000000, BD6AD2CF24844AA3
// program: 528A088158F377D7, 0000000000000000, FFBC46B3AB0BFB24
// program: EA36FEB391FDE835, 0000000000000000, 9CD647EF85EF7CD8
// program: 090F6261D7EE040A, 0000000000000000, 63051D9088D1408D
// program: DB65EA2FBAD032F1, 0000000000000000, 9CD647EF85EF7CD8
// program: 1A6183C6E984248F, 0000000000000000, D2085A77D44D517A
// program: 61A82C16044CBE52, 0000000000000000, F3A71ED69D6929ED
// program: 61A82C16044CBE52, 0000000000000000, D2085A77D44D517A
// program: 61A82C16044CBE52, 0000000000000000, B859757285851B30
// program: E4B1717CEF51482E, 0000000000000000, 2D4084AEB2B4161F
// program: 77B41F440BB8269F, 0000000000000000, 2D4084AEB2B4161F
// program: 82498C00693767D6, 0000000000000000, DDFD33886541B273
// program: 1A6183C6E984248F, 0000000000000000, ABCDDEF36CE57D3D
// program: 1A6183C6E984248F, 0000000000000000, F3A71ED69D6929ED
// program: 61A82C16044CBE52, 0000000000000000, ABCDDEF36CE57D3D
// program: 1A6183C6E984248F, 0000000000000000, EE316CD84AB4DBC8
// program: 83C36A4A2949D5E1, 0000000000000000, 976EE145E7686DFB
// program: EA36FEB391FDE835, 0000000000000000, 6885B2A8F9F42002
// program: 528A088158F377D7, 0000000000000000, 3CD6D6389535A415
// program: 61A82C16044CBE52, 0000000000000000, 9062E97D0BCDF52E
// program: 61A82C16044CBE52, 0000000000000000, E85EE933CF59566B
